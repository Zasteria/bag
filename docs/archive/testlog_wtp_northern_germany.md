# Северная Германия: два прогона, которыми приняли раздачу уровнями

**Живой журнал — [`TESTLOG.md`](../TESTLOG.md).** Здесь два прогона
2026-09-07, после которых раздача уровнями и ступень по сторонам были
приняты; их числа — та база, с которой сравнивался прогон резервации.

**2026-09-07, общий план на северной Германии — раздача уровнями работает, и
числа сходятся до единицы.** 416 локаций, 136 городов, 280 сёл; `placed=1384
rooms=1384`, `laps=87` при пределе 150, `dry_laps=26`.

**Счёт закрывается ровно:** сумма `nt` = **544** (136×4), сумма `nr` = **840**
(280×3), сумма `n` = **1384**. Отвечает на его подозрение, что деревни считаются
по нескольку раз: одна локация держит `cap_rural` зданий разных типов.

**Полосы выгоды больше не решают «сколько», и это измерено.** `iron`, у которого
лучшая выгода **0** на обеих сторонах, взял **41 место из 43**; `stone` (372) —
44. Под старой лестницей ровно такие товары и голодали: 42 домика у тех, кто
где-то касается 1000, против 12 у тех, кто не касается нигде. Все пятнадцать
только городских легли в 16…19 независимо от выгоды: `incense` с выгодой 0
получил 19, `dyes` с 952 — 16, и разницу делает РГО, а не выгода.

**Один вопрос закрыт его ответом.** «1 ТОРГОВАЯ деревня на 1 локацию, но все 4
вида деревень в одной локации стоять могут.» Значит запрет мода на повтор
**одного и того же здания** был верным изначально; выключатель «В локации одна
деревня», построенный на полдня, снят.

**2026-09-07, северная Германия ещё раз, со ступенью по сторонам — план ровен
настолько, насколько земля позволяет.** 416 локаций, 132 города, 284 села (он снял
пометки город/село, поэтому земля не та же самая); `placed=1380 rooms=1380`,
`laps=89`, `dry_laps=27`, доля 36, потолки 15 и 37.

| | разброс |
| --- | --- |
| только городские, 15 товаров | **15…17** |
| умеющие в село, считая РГО, 21 товар | **50…65** |
| два прибрежных | fish 39, naval_supplies 38 |

**Прибрежных локаций 42, рыбацких деревень построено 39** (18 рыбы + 21 припасов)
— 93 %. Больше там взять нечего, и это весь ответ про рыбу.

**Ступень по сторонам оказалась пустышкой на этой земле:** fish 17 → 18,
`naval_supplies` 39 → 38, то есть в пределах разницы между двумя грунтами.
Правка верна по смыслу — `naval_supplies` умеет 174 локации, а в сёлах 42, — но
побережье и без неё было выбрано почти целиком. **Стоила нуля, дала ноль.**

**Счёт деревень в отчёте заработал:** `villages=328 rural_locs=284` — 1.2 деревни
на локацию из четырёх возможных видов, то есть законно и с большим запасом.

**Выгода 72 % зданий при 64.6 % от потолка** — против 72 % / 64.3 % прошлого
прогона. Раздача уровнями стабильна.
