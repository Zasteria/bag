# `where_to_produce` — план практических сессий

Составлен 2026-09-04, после теоретической сессии. **Правила, которые здесь
реализуются, лежат в [`wtp_editor_design.md`](wtp_editor_design.md)** и тут не
повторяются: эта страница про порядок, файлы и то, чем проверять.

**Порядок не случаен.** Шаг 1 — инструмент, которым проверяются все следующие.
Шаг 0 удешевляет сами сессии. Каждый шаг — **одна сборка и один прогон**;
владелец играет, сессия не может запустить игру.

## Общее для каждого шага

- Правится **`mods/where_to_produce/tools/generate.py`**, не сгенерированные
  файлы: они переписываются.
- Пересборка — `python3 tools/refresh.py`; у владельца это `mods.bat`.
- После любого вызова макроса CMM — `python3 tools/check_cmm.py
  mods/where_to_produce/in_game/common`.
- После правки любого окна — `python3 tools/check_script.py`: он разрешает
  каждое имя, которое окно называет.
- Прогон владельца в тот же день идёт в `../TESTLOG.md`.

## Шаги 0, 1, 2 и 2в — закрыты

`tools/code.py`, окно редактора, доля/замок/флаг «не нужен» и перестройка меню —
все подтверждены его прогонами 2026-09-05 и 2026-09-06. Как они были заказаны и
что чинилось по дороге —
[`../archive/wtp_practice_done.md`](../archive/wtp_practice_done.md) и
[`wtp_menu_rebuild.md`](wtp_menu_rebuild.md); правила, которые из них остались, —
в [`../pitfalls/interface.md`](../pitfalls/interface.md) и
[`../pitfalls/diagnosis.md`](../pitfalls/diagnosis.md).

**Из шага 2 живут в коде три вещи**: `_lock<n>` (замок, который снимает новый
план), `_skip<n>` (флаг «не нужен», который новый план не снимает) и `_esh<n>` —
недобор на товар, который читает заливка.

**Два правила интерфейса, которые он задал** и которые старше любого пункта ниже:
**в шапках окон не бывает описаний** (всё в подсказках), и **техническое живёт на
вкладке «Техническая»** —
[`wtp_editor_design.md`](wtp_editor_design.md#решения-по-интерфейсу).

## Шаг 2б — упакованный пикер — снят с повестки им самим

Земля умеет 35 товаров из 47, и двенадцать пустых мест раскиданы по сетке.
**В нынешней конструкции выравнивание и упаковка взаимоисключающи**: ячейки
расписаны по товару, дыра — цена ровных столбиков. Выход — пикер datamodel'ом,
и товар сам себе скоуп (`every_goods`, `Goods.MakeScope`), так что `_pn<n>`
строке достать есть откуда; порядок проверок —
[`../research/interface.md`](../research/interface.md).

**Его слова 2026-09-06:** «в целом похер, что там дырки, когда товаров нет,
разберёмся с этим в другой раз». Записан и не срочен; прогона не стоит.

## Шаг 2а — перетасовка новых домиков между собой — закрыта

**Заливка ставит домик за домиком в ту локацию, которую освободило именно это
нажатие:** *какой* товар решает недобор, а *куда* — случайность порядка. Заливка
помечает локацию (`_fillg`) и кладёт её в `bag_wtp_edit_fillset`; в конце «−1»
проход по парам меняет их товары местами, когда пара после обмена стоит дороже.
Раунд — отдельный эффект, вызываемый трижды за защёлкой, а не `while`; обмен
только при строгом улучшении. **Область — только домики заливки и только их же
локации**, так что ни доля, ни замки, ни покрывающее ограничение не задеты.

**Закрыта тремя прогонами.** Последний: `rounds=46 swaps=11 gain=4608 |
pairs=23270 same=685 nofit=8844 worse=13730`, сходится до единицы. **Проход
работает, а менять по большей части нечего** — 13730 пар из 23270 не дают
выгоды; `swaps=0` был ответом, а не поломкой. **`rounds=0` читалось бы на карте
так же и значило бы «проход не запускался»** — ради этого зонд и стоит.

**И одна ошибка, найденная по дороге:** `_edit_remove_town_<n>` и
`_edit_remove_rural_<n>` не спрашивали, город это или село, а `call()` выпускает
оба и городской идёт первым — на селе «−1» вычитал из `_plan_gain` городскую
выгоду. Сторона теперь спрашивается.

## Доля редактора и РГО — починено 2026-09-06, и это его находка

**План вычитал РГО из доли товара, а заливка редактора — нет.** Его слова и
разбор — [`wtp_editor_design.md`](wtp_editor_design.md#решение-доля-редактора-считает-рго-как-её-считает-план).
У редактора теперь `_eq<n> = max(1, _edit_quota − _nrgo<n>)`, и её читают и
недобор заливки, и признак «выше доли» шага 3. **В диагностике `eq=` рядом с
`q=` и `rgo=` в строке `G<n>`** — одним взглядом видно, сходятся ли две доли.

**Это меняет и то, что проверяет шаг 3**, так что оба надо смотреть одним
прогоном.

## Шаг 3 — равномерная выемка при «+1» — собран 2026-09-06

**Собран, сборка `ebdc8a`, в игре не запускался.** Два прохода, не упаковка
чисел (почему — `wtp_editor_design.md`).

**Как сделано.** Признак «выше своей доли» — `global_var:_pn<n> >
global_var:_edit_quota` — дописан в `limit` внутри `_edit_worst`, который и так
перебирает все товары на каждом кандидате, под флагом `_edit_strict`. Весь скан
вынесен в `_edit_scan_pass` и вызывается дважды: строгим, а если он не нашёл ни
одного кандидата (`_edit_cands = 0`) — открытым. **Свободная комната проходит
оба прохода**: жертвы у неё нет, а перебивает она всё равно всё.

**Зонд:** `EDIT scan fitn= cands= strict=` в диагностике. `strict=1` — выбор
сделала доля, `strict=0` — никто не был выше доли и ответил открытый проход.

**Подтверждён прогоном 2026-09-06 (`a69370f2`): `fitn=23 cands=9 strict=1`** —
жертву выбрал строгий проход. **И десятью нажатиями подряд (`23a56b9e`):** «+1»
по коже десять раз выселило десять **разных** товаров.

**И одна правка сверх собранного.** Жертву выбирал самый дешёвый домик среди
тех, кто выше доли, — а «дешевле всех» не меняется от того, что товара стало
меньше, так что подряд выселялся один и тот же. Теперь первым ключом идёт
**превышение над своей долей** (`_exs<n> = _pn<n> − _eq<n>`), ценой домика
разрешаются только равные. Не прогонялось.

**Цена, принятая заранее:** «+1» стал разборчивее в жертве и потому менее
разборчив в месте, так что суммарная выгода просядет ещё немного.

**Что просить:** пять «+1» подряд на один товар. **Отдавать домики должны разные
товары, каждый раз самый раздутый** — видно в строке нажатия и в «Показать
изменения».

## Шаг 4 — провинции сворачиваются в двух окнах плана

Чистый интерфейс, прогон дешёвый. Образец — поиск по одному товару, он уже так
умеет. `bag_wtp_plan_window.gui` и его типы в `generate.py`.

## Шаг 5 — грамоты в редакторе — собран 2026-09-06, не запускался

**Грамота не домик, и «+1» на ней не может значить «ещё одна».** Каждому городу
готового плана положена ровно одна: раздача обходит пустые города и выдаёт каждому
лучшую, какую держава может. Свободных городов нет, значит нажатие — это
**переезд**, и это ровно то, что он просил 2026-09-04: «снять грамоту с города
вместе со всеми её домиками и посадить в другой».

- **«+1» на грамоте K** забирает город, где K заработает больше всего, у той
  грамоты, что там стоит. Строгий проход — только у тех, у кого городов не меньше
  их доли; если таких нет, открытый — у любой. Те же два прохода и по той же
  причине, что у «+1» на товаре.
- **«−1» на грамоте K** снимает её с города, где она зарабатывает меньше всего, и
  отдаёт город той грамоте, которой до доли не хватает больше всех и которая там
  встанет. **Город без грамоты не остаётся** — такого состояния план не делает.
  Если наследника нет, нажатие отказывает и говорит об этом.

**Связка переезжает целиком, места равняются после.** Связка — от одного до трёх
домиков, и у двух грамот размеры разные, поэтому обмен сперва сажает, потом
равняет нагрузку: сверх лимита уходит худший обычный домик, под лимитом место
занимает товар с наибольшим недобором. По три раза в каждую сторону — три
домика самая большая связка в игре.

**Доля грамоты — та же, по которой раздавал план** (`_rquota`: города, делённые
на число грамот державы). Замена подстановки (`glass` → `sand`) зеркалит раздачу,
а не решает заново.

**Первая сборка не работала ни одной кнопкой, и причина — чужой вопрос.** Обе
стороны спрашивали `_plan_right_fits_<k>`, а он собран из `_plan_can_town_<n>`, у
которого в условии стоит свободная комната: верно для раздачи по пустым городам,
ложно в готовом плане всегда. У редактора теперь свой `_edit_right_fits_<k>` —
город и `var:_pm<n> > 0` хотя бы у одного товара связки. Подстановка
`glass` → `sand` решала тем же чужим триггером и тоже переведена на `_pm<n>`.
Правило — [`../PITFALLS.md`](../PITFALLS.md).

**И недобор грамоты больше не прижат к нулю:** при доле 5 грамота с шестью
городами и грамота с пятью обе давали «недобора нет», и «−1» мог отдать город
тому, у кого их больше всех.

**Что просить:** «−1» на самой частой грамоте 3–4 раза, потом «+1» на самой
редкой столько же. **Числа между кнопками должны сойтись**, строка нажатия —
называть обе грамоты и город, а домики связки — переехать вместе с ней.

## Шаг 6 — новая земля к готовому плану

План на землю X заморожен, земля Y застраивается с учётом X и не трогает X.
Связано с замком из шага 2: замок спадает, когда новая доля его перерастает.

## Шаги 7 и 8 — чужие моды, и они последние

**Его порядок, 2026-09-04: «доработать все начатые функции мода и потом уже
пытаться интегрировать его в функционал CM и glorp».** Шаги 1–6 — свои файлы
мода, 7–8 — чужие. Не начинать 7 или 8 раньше, каким бы интересным оно ни
казалось.

**И прочитанное про них устаревает быстрее всего остального.** Замеры этой
сессии сняты с Glorp UI 10.08.26 и CM 2.2.12; владелец обновляет `reference/`
без предупреждения. Сессия, которая берётся за 7 или 8, **перечитывает их файлы
заново** — `python3 tools/refs.py` за версией, грепом за именем, — а не верит
номеру строки, записанному здесь.

## Шаг 7 — план в панели локации

Показать, что план назначил этой локации. **Встраиваться в интерфейс Glorp UI**,
не искать нетронутый угол — его нет. Работа по файлам Glorp UI.

## Шаг 8 — проставить план в Construction Manager

**Функция доступна только при CM и скрыта без него** — его требование.

- **Куда писать:** `cm_auto_expand_registered_building_types`, список переменных
  **на локации**. Разбор и вторая ветка (массовый режим) —
  [`../research/cmf.md`](../research/cmf.md).
- **Писать список самим, а не звать `cm_apply_auto_expand_toggle`.** Имя списка —
  строка, и без CM запись просто лежит непрочитанной; вызов чужого эффекта — имя,
  которое надо разрешить. **Не проверено, ломает ли неразрешённое имя загрузку**,
  и такой выбор снимает вопрос вместо того, чтобы на него ставить.
- **Чем отличать «CM есть»:** `has_global_variable = cm_goods_iron` — CM ставит
  эти глобалки при загрузке игры (`cm_game_load_setup_effects.txt`). Договор по
  наблюдению, а не по макросу — как `cmm_group_collapsed`, на который мод уже
  опирается. **Отказ безопасный: переименуют — кнопка просто исчезнет.**
