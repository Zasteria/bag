# Mark the mod active so it shows up in the framework's mod list. There are no
# settings any more: the province and location variants of the filter are chips
# of their own, which puts the choice in the panel where the filtering happens
# rather than three menus away.
# Scope: country
bag_rgo_register = {
	cmf_register_mod = { mod_id = bag_rgo }
}
