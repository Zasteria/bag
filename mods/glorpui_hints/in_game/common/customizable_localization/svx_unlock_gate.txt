# Auto-generated by mods/glorpui_hints/tools/generate.py from the EU5 game files. Do not edit by hand.
# Glorp UI recommends a privilege its own filter thinks is takeable.
# These are the ones an advance locks from the other side: the
# privilege's own `potential` is empty, so `glorpui_svh_privilege_takeable`
# lets it through. The list is scanned out of common/advances rather
# than written down, so a patch that locks another one is picked up.
#
# The hint's own localization key is rewritten to print
# [Player.Custom('svx_unlock_<key>')] and the words moved to
# SVX_UNLOCK_<key>, because a customizable_localization cannot be
# overridden — the first definition read wins and a later duplicate is
# dropped with `gamedatabase.h: Duplicated key ... will not be created
# from file`. Localization keys, on the other hand, do override, and
# this mod rewrites every one of Glorp UI's already.

# ayans_privilege, locked by ayans
svx_unlock_glorp_ui_svh_decentralization_pv_ayans_privilege = {
	type = country
	text = {
		trigger = { has_advance = ayans }
		localization_key = SVX_UNLOCK_GLORP_UI_SVH_DECENTRALIZATION_PV_AYANS_PRIVILEGE
	}
	text = {
		localization_key = empty_text
	}
}

# ghazi_privilege, locked by ghazi
svx_unlock_glorp_ui_svh_decentralization_pv_ghazi_privilege = {
	type = country
	text = {
		trigger = { has_advance = ghazi }
		localization_key = SVX_UNLOCK_GLORP_UI_SVH_DECENTRALIZATION_PV_GHAZI_PRIVILEGE
	}
	text = {
		localization_key = empty_text
	}
}

# peasants_yeomanry, locked by unlock_early_longbowmen_advance
svx_unlock_glorp_ui_svh_free_subjects_pv_peasants_yeomanry = {
	type = country
	text = {
		trigger = { has_advance = unlock_early_longbowmen_advance }
		localization_key = SVX_UNLOCK_GLORP_UI_SVH_FREE_SUBJECTS_PV_PEASANTS_YEOMANRY
	}
	text = {
		localization_key = empty_text
	}
}

# ghazi_privilege, locked by ghazi
svx_unlock_glorp_ui_svh_quality_pv_ghazi_privilege = {
	type = country
	text = {
		trigger = { has_advance = ghazi }
		localization_key = SVX_UNLOCK_GLORP_UI_SVH_QUALITY_PV_GHAZI_PRIVILEGE
	}
	text = {
		localization_key = empty_text
	}
}

# jaysh_armies, locked by jaysh_armies_advance
svx_unlock_glorp_ui_svh_quantity_pv_jaysh_armies = {
	type = country
	text = {
		trigger = { has_advance = jaysh_armies_advance }
		localization_key = SVX_UNLOCK_GLORP_UI_SVH_QUANTITY_PV_JAYSH_ARMIES
	}
	text = {
		localization_key = empty_text
	}
}
