# Both panels that list buildings ask for the `building` filter tag, so all four
# chips are offered in both. Each pair is only meaningful in its own panel: the
# building_type ones narrow a location's building list, the location ones narrow
# the list of places to put a chosen building.
#
# Province and location are separate chips rather than one chip and a setting,
# so the choice sits where the filtering happens.
# Localization lives under search_filter_<key>_name / _desc.

bag_rgo_local_bonus = {
	scope = building_type
	tag = building
	group = 9
	exclusive_group = no
	trigger = { bag_rgo_has_local_bonus = yes }
}

bag_rgo_local_bonus_here = {
	scope = building_type
	tag = building
	group = 9
	exclusive_group = no
	trigger = { bag_rgo_has_local_bonus_here = yes }
}

bag_rgo_build_local_bonus = {
	scope = location
	tag = building
	group = 9
	exclusive_group = no
	trigger = { bag_rgo_location_suits_build_type = yes }
}

bag_rgo_build_local_bonus_here = {
	scope = location
	tag = building
	group = 9
	exclusive_group = no
	trigger = { bag_rgo_location_suits_build_type_here = yes }
}
