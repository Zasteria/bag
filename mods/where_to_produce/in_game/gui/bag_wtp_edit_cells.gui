# Generated by mods/where_to_produce/tools/generate.py. Do not edit by hand.
#
# The plan editor's picker, a cell a good. `bag_wtp_edit_window.gui` draws the
# five rows; this file only declares them, so it has no `window` and needs no
# line in `gui/scripted_widgets/`.

types BagWtpEditCells {

	# Goods 1..10 of the plan's own order.
	type bag_wtp_edit_row1 = hbox {
		spacing = 6

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool1').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_1').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool1').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_1"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool1').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_1').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool1').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip1').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_1').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool2').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_2').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool2').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_2"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool2').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_2').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool2').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip2').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_2').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool3').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_3').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool3').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_3"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool3').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_3').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool3').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip3').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_3').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool4').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_4').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool4').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_4"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool4').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_4').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool4').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip4').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_4').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool5').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_5').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool5').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_5"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool5').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_5').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool5').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip5').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_5').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool6').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_6').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool6').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_6"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool6').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_6').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool6').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip6').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_6').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool7').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_7').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool7').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_7"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool7').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_7').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool7').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip7').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_7').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool8').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_8').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool8').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_8"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool8').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_8').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool8').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip8').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_8').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool9').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_9').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool9').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_9"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool9').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_9').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool9').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip9').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_9').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool10').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_10').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool10').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_10"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool10').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_10').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool10').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip10').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_10').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}
	}

	# Goods 11..20 of the plan's own order.
	type bag_wtp_edit_row2 = hbox {
		spacing = 6

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool11').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_11').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool11').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_11"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool11').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_11').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool11').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip11').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_11').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool12').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_12').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool12').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_12"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool12').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_12').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool12').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip12').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_12').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool13').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_13').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool13').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_13"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool13').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_13').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool13').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip13').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_13').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool14').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_14').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool14').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_14"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool14').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_14').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool14').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip14').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_14').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool15').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_15').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool15').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_15"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool15').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_15').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool15').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip15').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_15').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool16').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_16').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool16').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_16"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool16').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_16').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool16').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip16').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_16').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool17').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_17').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool17').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_17"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool17').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_17').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool17').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip17').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_17').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool18').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_18').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool18').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_18"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool18').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_18').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool18').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip18').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_18').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool19').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_19').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool19').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_19"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool19').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_19').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool19').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip19').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_19').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool20').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_20').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool20').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_20"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool20').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_20').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool20').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip20').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_20').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}
	}

	# Goods 21..30 of the plan's own order.
	type bag_wtp_edit_row3 = hbox {
		spacing = 6

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool21').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_21').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool21').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_21"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool21').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_21').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool21').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip21').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_21').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool22').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_22').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool22').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_22"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool22').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_22').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool22').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip22').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_22').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool23').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_23').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool23').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_23"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool23').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_23').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool23').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip23').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_23').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool24').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_24').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool24').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_24"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool24').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_24').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool24').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip24').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_24').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool25').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_25').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool25').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_25"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool25').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_25').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool25').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip25').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_25').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool26').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_26').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool26').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_26"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool26').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_26').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool26').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip26').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_26').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool27').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_27').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool27').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_27"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool27').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_27').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool27').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip27').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_27').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool28').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_28').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool28').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_28"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool28').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_28').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool28').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip28').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_28').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool29').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_29').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool29').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_29"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool29').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_29').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool29').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip29').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_29').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool30').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_30').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool30').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_30"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool30').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_30').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool30').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip30').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_30').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}
	}

	# Goods 31..40 of the plan's own order.
	type bag_wtp_edit_row4 = hbox {
		spacing = 6

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool31').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_31').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool31').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_31"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool31').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_31').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool31').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip31').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_31').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool32').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_32').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool32').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_32"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool32').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_32').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool32').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip32').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_32').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool33').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_33').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool33').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_33"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool33').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_33').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool33').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip33').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_33').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool34').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_34').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool34').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_34"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool34').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_34').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool34').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip34').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_34').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool35').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_35').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool35').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_35"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool35').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_35').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool35').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip35').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_35').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool36').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_36').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool36').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_36"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool36').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_36').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool36').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip36').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_36').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool37').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_37').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool37').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_37"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool37').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_37').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool37').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip37').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_37').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool38').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_38').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool38').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_38"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool38').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_38').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool38').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip38').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_38').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool39').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_39').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool39').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_39"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool39').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_39').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool39').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip39').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_39').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool40').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_40').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool40').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_40"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool40').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_40').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool40').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip40').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_40').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}
	}

	# Goods 41..47 of the plan's own order.
	type bag_wtp_edit_row5 = hbox {
		spacing = 6

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool41').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_41').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool41').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_41"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool41').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_41').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool41').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip41').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_41').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool42').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_42').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool42').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_42"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool42').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_42').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool42').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip42').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_42').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool43').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_43').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool43').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_43"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool43').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_43').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool43').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip43').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_43').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool44').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_44').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool44').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_44"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool44').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_44').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool44').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip44').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_44').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool45').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_45').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool45').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_45"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool45').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_45').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool45').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip45').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_45').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool46').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_46').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool46').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_46"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool46').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_46').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool46').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip46').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_46').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}

		hbox = {
			size = { 127 28 }
			spacing = 1

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool47').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_minus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_minus_47').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_minus"
					}
				}
			}

			widget = {
				size = { 42 28 }
				alwaystransparent = no
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool47').IsSet]"
				tooltip = "bag_wtp_cell_tt"
				text_single = {
					parentanchor = center
					widgetanchor = center
					autoresize = yes
					fontsize = 16
					text = "bag_wtp_cell_47"
				}
			}

			widget = {
				size = { 28 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool47').IsSet]"
				button_regular = {
					size = { 26 26 }
					parentanchor = center
					widgetanchor = center
					tooltip = "bag_wtp_edit_plus_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_plus_47').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
					text_single = {
						parentanchor = center
						widgetanchor = center
						autoresize = yes
						fontsize = 14
						text = "bag_wtp_edit_plus"
					}
				}
			}

			# **«не нужен» is the game's own checkbox, and that is not a
			# decoration.** It was two buttons carrying «×» and «✔», and the
			# second rendered as an empty box: the game's fonts have no
			# checkmark, and its own localization uses no such character
			# anywhere -- so picking a different glyph would have been another
			# guess. `checkbutton_round_alt` draws both states from a texture
			# (`upframe` / `downframe`) and takes the state from `down`, which
			# is how the outliner's own category toggles work. One control, no
			# glyph, and the state is legible.
			widget = {
				size = { 26 28 }
				visible = "[GetPlayer.MakeScope.GetVariable('bag_wtp_pool47').IsSet]"
				checkbutton_round_alt = {
					size = { 24 24 }
					parentanchor = center
					widgetanchor = center
					down = "[GetGlobalVariable('bag_wtp_skip47').IsSet]"
					tooltip = "bag_wtp_edit_skip_tt"
					onclick = "[GetScriptedGui('bag_wtp_pick_skip_47').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				}
			}
		}
	}
}
