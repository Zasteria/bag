# Generated by mods/where_to_produce/tools/generate.py. Do not edit by hand.
#
# A list reaches nothing but its own `_on_changed`: bool, dropdown, numeric,
# slider and button settings auto-apply and reach `cmf_on_callback`, and a list
# reaches neither until `cmm_apply_list_change` is called here.

bag_wtp__region_africa_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__region_africa
		}
		bag_wtp_zone_changed = yes
	}
}

bag_wtp__region_asia_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__region_asia
		}
		bag_wtp_zone_changed = yes
	}
}

bag_wtp__region_oceania_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__region_oceania
		}
		bag_wtp_zone_changed = yes
	}
}

bag_wtp__region_america_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__region_america
		}
		bag_wtp_zone_changed = yes
	}
}

bag_wtp__region_europe_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__region_europe
		}
		bag_wtp_zone_changed = yes
	}
}

bag_wtp__continent_on_changed = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		cmm_apply_list_change = {
			setting = bag_wtp__continent
		}
		bag_wtp_zone_changed = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for clay, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:clay` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_1 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for clay, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:clay` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_1 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for clay, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:clay` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_1 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for coal, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:coal` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_2 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for coal, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:coal` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_2 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for coal, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:coal` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_2 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cotton, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cotton` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_3 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cotton, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cotton` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_3 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cotton, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cotton` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_3 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for dyes, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:dyes` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_4 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for dyes, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:dyes` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_4 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for dyes, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:dyes` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_4 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fiber_crops, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fiber_crops` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_5 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fiber_crops, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fiber_crops` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_5 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fiber_crops, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fiber_crops` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_5 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fish, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fish` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_6 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fish, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fish` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_6 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fish, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fish` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_6 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fruit, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fruit` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_7 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fruit, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fruit` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_7 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fruit, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fruit` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_7 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for horses, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:horses` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_8 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for horses, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:horses` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_8 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for horses, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:horses` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_8 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for incense, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:incense` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_9 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for incense, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:incense` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_9 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for incense, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:incense` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_9 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for iron, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:iron` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_10 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for iron, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:iron` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_10 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for iron, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:iron` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_10 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for ivory, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:ivory` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_11 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for ivory, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:ivory` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_11 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for ivory, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:ivory` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_11 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for livestock, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:livestock` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_12 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for livestock, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:livestock` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_12 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for livestock, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:livestock` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_12 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lumber, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lumber` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_13 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lumber, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lumber` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_13 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lumber, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lumber` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_13 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for medicaments, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:medicaments` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_14 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_14 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for medicaments, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:medicaments` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_14 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_14 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for medicaments, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:medicaments` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_14 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_14 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for salt, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:salt` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_15 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_15 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for salt, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:salt` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_15 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_15 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for salt, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:salt` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_15 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_15 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for saltpeter, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:saltpeter` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_16 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_16 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for saltpeter, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:saltpeter` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_16 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_16 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for saltpeter, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:saltpeter` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_16 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_16 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sand, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sand` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_17 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_17 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sand, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sand` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_17 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_17 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sand, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sand` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_17 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_17 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silk, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silk` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_18 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_18 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silk, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silk` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_18 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_18 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silk, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silk` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_18 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_18 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silver, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silver` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_19 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_19 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silver, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silver` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_19 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_19 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for silver, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:silver` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_19 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_19 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for stone, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:stone` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_20 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_20 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for stone, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:stone` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_20 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_20 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for stone, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:stone` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_20 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_20 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sugar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sugar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_21 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_21 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sugar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sugar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_21 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_21 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for sugar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:sugar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_21 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_21 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tobacco, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tobacco` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_22 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_22 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tobacco, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tobacco` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_22 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_22 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tobacco, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tobacco` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_22 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_22 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wild_game, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wild_game` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_23 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_23 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wild_game, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wild_game` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_23 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_23 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wild_game, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wild_game` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_23 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_23 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wine, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wine` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_24 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_24 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wine, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wine` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_24 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_24 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wine, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wine` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_24 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_24 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wool, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wool` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_25 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_25 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wool, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wool` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_25 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_25 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for wool, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:wool` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_25 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_25 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for beer, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:beer` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_26 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_26 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for beer, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:beer` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_26 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_26 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for beer, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:beer` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_26 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_26 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for books, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:books` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_27 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_27 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for books, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:books` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_27 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_27 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for books, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:books` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_27 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_27 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cannons, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cannons` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_28 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_28 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cannons, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cannons` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_28 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_28 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cannons, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cannons` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_28 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_28 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_29 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_29 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_29 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_29 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_29 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_29 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fine_cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fine_cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_30 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_30 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fine_cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fine_cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_30 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_30 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for fine_cloth, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:fine_cloth` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_30 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_30 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for firearms, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:firearms` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_31 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_31 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for firearms, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:firearms` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_31 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_31 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for firearms, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:firearms` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_31 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_31 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for furniture, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:furniture` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_32 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_32 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for furniture, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:furniture` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_32 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_32 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for furniture, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:furniture` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_32 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_32 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for glass, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:glass` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_33 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_33 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for glass, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:glass` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_33 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_33 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for glass, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:glass` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_33 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_33 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for jewelry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:jewelry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_34 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_34 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for jewelry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:jewelry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_34 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_34 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for jewelry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:jewelry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_34 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_34 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lacquerware, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lacquerware` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_35 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_35 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lacquerware, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lacquerware` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_35 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_35 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for lacquerware, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:lacquerware` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_35 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_35 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for leather, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:leather` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_36 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_36 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for leather, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:leather` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_36 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_36 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for leather, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:leather` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_36 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_36 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for liquor, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:liquor` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_37 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_37 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for liquor, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:liquor` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_37 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_37 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for liquor, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:liquor` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_37 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_37 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for masonry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:masonry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_38 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_38 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for masonry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:masonry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_38 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_38 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for masonry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:masonry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_38 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_38 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for naval_supplies, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:naval_supplies` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_39 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_39 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for naval_supplies, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:naval_supplies` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_39 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_39 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for naval_supplies, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:naval_supplies` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_39 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_39 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for paper, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:paper` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_40 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_40 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for paper, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:paper` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_40 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_40 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for paper, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:paper` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_40 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_40 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for porcelain, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:porcelain` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_41 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_41 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for porcelain, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:porcelain` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_41 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_41 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for porcelain, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:porcelain` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_41 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_41 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for pottery, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:pottery` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_42 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_42 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for pottery, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:pottery` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_42 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_42 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for pottery, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:pottery` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_42 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_42 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for slaves_goods, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:slaves_goods` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_43 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_43 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for slaves_goods, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:slaves_goods` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_43 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_43 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for slaves_goods, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:slaves_goods` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_43 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_43 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for steel, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:steel` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_44 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_44 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for steel, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:steel` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_44 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_44 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for steel, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:steel` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_44 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_44 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_45 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_45 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_45 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_45 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tar, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tar` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_45 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_45 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tools, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tools` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_46 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_46 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tools, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tools` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_46 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_46 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for tools, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:tools` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_46 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_46 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for weaponry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:weaponry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_plus_47 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_plus_47 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for weaponry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:weaponry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_minus_47 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_minus_47 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}.get(what, 'не нужен')» for weaponry, from the picker's own cell.
#
# **No saved scope.** The cell is written out and knows its number, so the
# bridge that carried `goods:weaponry` as a scope is gone -- and with it the one
# thing between the press and the plan that could quietly not arrive.
bag_wtp_pick_skip_47 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_skip_47 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the constantinopolitan_silk_monopoly_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_1 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the constantinopolitan_silk_monopoly_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_1 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the flemish_cloth_industries_right charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_2 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the flemish_cloth_industries_right charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_2 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_artisan_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_3 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_artisan_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_3 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_book_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_4 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_book_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_4 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_brewing_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_5 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_brewing_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_5 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_jewelry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_6 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_jewelry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_6 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_masonry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_7 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_masonry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_7 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_naval_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_8 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_naval_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_8 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_textile_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_9 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_textile_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_9 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_tooling_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_10 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_tooling_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_10 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_weaponry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_11 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the royal_weaponry_rights charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_11 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the scandinavian_bergslag_privileges charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_12 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the scandinavian_bergslag_privileges charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_12 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the scandinavian_tar_privileges charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_plus_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_plus_13 = yes
		bag_wtp_recompute_live = yes
	}
}

# «{'plus': '+1', 'minus': '−1'}[what]» on the scandinavian_tar_privileges charter, from its own
# cell in the editor. **A charter is moved, never added**: «+1» takes the town
# where it would earn most away from a charter that has more than its share,
# «−1» hands its worst town to whichever charter is furthest below its own.
bag_wtp_right_minus_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_edit_right_minus_13 = yes
		bag_wtp_recompute_live = yes
	}
}

# clay in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_1 = yes
	}
}

# coal in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_2 = yes
	}
}

# cotton in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_3 = yes
	}
}

# dyes in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_4 = yes
	}
}

# fiber_crops in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_5 = yes
	}
}

# fish in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_6 = yes
	}
}

# fruit in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_7 = yes
	}
}

# horses in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_8 = yes
	}
}

# incense in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_9 = yes
	}
}

# iron in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_10 = yes
	}
}

# ivory in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_11 = yes
	}
}

# livestock in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_12 = yes
	}
}

# lumber in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_13 = yes
	}
}

# medicaments in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_14 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_14 = yes
	}
}

# salt in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_15 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_15 = yes
	}
}

# saltpeter in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_16 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_16 = yes
	}
}

# sand in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_17 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_17 = yes
	}
}

# silk in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_18 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_18 = yes
	}
}

# silver in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_19 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_19 = yes
	}
}

# stone in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_20 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_20 = yes
	}
}

# sugar in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_21 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_21 = yes
	}
}

# tobacco in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_22 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_22 = yes
	}
}

# wild_game in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_23 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_23 = yes
	}
}

# wine in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_24 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_24 = yes
	}
}

# wool in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_25 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_25 = yes
	}
}

# beer in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_26 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_26 = yes
	}
}

# books in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_27 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_27 = yes
	}
}

# cannons in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_28 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_28 = yes
	}
}

# cloth in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_29 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_29 = yes
	}
}

# fine_cloth in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_30 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_30 = yes
	}
}

# firearms in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_31 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_31 = yes
	}
}

# furniture in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_32 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_32 = yes
	}
}

# glass in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_33 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_33 = yes
	}
}

# jewelry in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_34 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_34 = yes
	}
}

# lacquerware in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_35 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_35 = yes
	}
}

# leather in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_36 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_36 = yes
	}
}

# liquor in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_37 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_37 = yes
	}
}

# masonry in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_38 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_38 = yes
	}
}

# naval_supplies in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_39 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_39 = yes
	}
}

# paper in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_40 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_40 = yes
	}
}

# porcelain in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_41 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_41 = yes
	}
}

# pottery in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_42 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_42 = yes
	}
}

# slaves_goods in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_43 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_43 = yes
	}
}

# steel in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_44 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_44 = yes
	}
}

# tar in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_45 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_45 = yes
	}
}

# tools in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_46 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_46 = yes
	}
}

# weaponry in the ranking window's picker. One circle, one number.
bag_wtp_pick_good_47 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_good_47 = yes
	}
}

# constantinopolitan_silk_monopoly_rights in the ranking window's picker.
bag_wtp_pick_right_1 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_1 = yes
	}
}

# flemish_cloth_industries_right in the ranking window's picker.
bag_wtp_pick_right_2 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_2 = yes
	}
}

# royal_artisan_rights in the ranking window's picker.
bag_wtp_pick_right_3 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_3 = yes
	}
}

# royal_book_rights in the ranking window's picker.
bag_wtp_pick_right_4 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_4 = yes
	}
}

# royal_brewing_rights in the ranking window's picker.
bag_wtp_pick_right_5 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_5 = yes
	}
}

# royal_jewelry_rights in the ranking window's picker.
bag_wtp_pick_right_6 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_6 = yes
	}
}

# royal_masonry_rights in the ranking window's picker.
bag_wtp_pick_right_7 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_7 = yes
	}
}

# royal_naval_rights in the ranking window's picker.
bag_wtp_pick_right_8 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_8 = yes
	}
}

# royal_textile_rights in the ranking window's picker.
bag_wtp_pick_right_9 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_9 = yes
	}
}

# royal_tooling_rights in the ranking window's picker.
bag_wtp_pick_right_10 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_10 = yes
	}
}

# royal_weaponry_rights in the ranking window's picker.
bag_wtp_pick_right_11 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_11 = yes
	}
}

# scandinavian_bergslag_privileges in the ranking window's picker.
bag_wtp_pick_right_12 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_12 = yes
	}
}

# scandinavian_tar_privileges in the ranking window's picker.
bag_wtp_pick_right_13 = {
	scope = country

	is_shown = {
		always = yes
	}

	effect = {
		bag_wtp_set_right_13 = yes
	}
}
