# Generated by mods/where_to_produce/tools/generate.py. Do not edit by hand.
#
# Scope: country
bag_wtp_plan_run = {
	save_scope_as = bag_wtp_country
	bag_wtp_sync = yes
	bag_wtp_collect_candidates = yes
	bag_wtp_rebuild_browse = yes
	bag_wtp_plan_clear = yes
	bag_wtp_plan_prepare = yes
	bag_wtp_plan_score = yes
	if = {
		limit = { has_global_variable = bag_wtp_plan_rights }
		bag_wtp_plan_place_rights = yes
	}
	bag_wtp_plan_allocate = yes
	bag_wtp_plan_build = yes
	bag_wtp_plan_rank = yes
	cmf_log = { action = bag_wtp_log_plan }
}

# What the last plan left on the map.
#
# `_plan_touched` is every location the last run wrote anything to, its own
# province's neighbours included. **`bag_wtp_force_town` and `_force_rural` -- the player's own
# "count this one as a town" -- is deliberately not among the things removed**:
# it is an answer he gave, not one the plan computed.
# Scope: country
bag_wtp_plan_clear = {
	every_in_global_list = {
		variable = bag_wtp_plan_touched
		remove_variable = bag_wtp_plan_dirty
		remove_variable = bag_wtp_plan_rank
		remove_variable = bag_wtp_plan_prank
		remove_variable = bag_wtp_plan_town_n
		remove_variable = bag_wtp_plan_rural_n
		remove_variable = bag_wtp_plan_prov_load
		remove_variable = bag_wtp_load
		remove_variable = bag_wtp_plan_right
		remove_variable = bag_wtp_plan_town_row
		clear_variable_list = bag_wtp_plan_town
		clear_variable_list = bag_wtp_plan_rural
		clear_variable_list = bag_wtp_plan_town_b
		clear_variable_list = bag_wtp_plan_rural_b
		clear_variable_list = bag_wtp_plan_goods
		clear_variable_list = bag_wtp_plan_builds
	}
	clear_global_variable_list = bag_wtp_plan_touched
	clear_global_variable_list = bag_wtp_plan_prov_locs
	clear_global_variable_list = bag_wtp_plan_ranked
	clear_global_variable_list = bag_wtp_plan_results
	set_global_variable = { name = bag_wtp_plan_placed value = 0 }
	set_global_variable = { name = bag_wtp_plan_scored value = 0 }
	set_global_variable = { name = bag_wtp_plan_found value = 0 }
	set_global_variable = { name = bag_wtp_plan_shown value = 0 }
	set_global_variable = { name = bag_wtp_plan_rooms value = 0 }
	set_global_variable = { name = bag_wtp_plan_towns value = 0 }
	set_global_variable = { name = bag_wtp_plan_provn value = 0 }
	set_global_variable = { name = bag_wtp_plan_lists value = 0 }
	set_global_variable = { name = bag_wtp_pn1 value = 0 }
	set_global_variable = { name = bag_wtp_pn2 value = 0 }
	set_global_variable = { name = bag_wtp_pn3 value = 0 }
	set_global_variable = { name = bag_wtp_pn4 value = 0 }
	set_global_variable = { name = bag_wtp_pn5 value = 0 }
	set_global_variable = { name = bag_wtp_pn6 value = 0 }
	set_global_variable = { name = bag_wtp_pn7 value = 0 }
	set_global_variable = { name = bag_wtp_pn8 value = 0 }
	set_global_variable = { name = bag_wtp_pn9 value = 0 }
	set_global_variable = { name = bag_wtp_pn10 value = 0 }
	set_global_variable = { name = bag_wtp_pn11 value = 0 }
	set_global_variable = { name = bag_wtp_pn12 value = 0 }
	set_global_variable = { name = bag_wtp_pn13 value = 0 }
	set_global_variable = { name = bag_wtp_pn14 value = 0 }
	set_global_variable = { name = bag_wtp_pn15 value = 0 }
	set_global_variable = { name = bag_wtp_pn16 value = 0 }
	set_global_variable = { name = bag_wtp_pn17 value = 0 }
	set_global_variable = { name = bag_wtp_pn18 value = 0 }
	set_global_variable = { name = bag_wtp_pn19 value = 0 }
	set_global_variable = { name = bag_wtp_pn20 value = 0 }
	set_global_variable = { name = bag_wtp_pn21 value = 0 }
	set_global_variable = { name = bag_wtp_pn22 value = 0 }
	set_global_variable = { name = bag_wtp_pn23 value = 0 }
	set_global_variable = { name = bag_wtp_pn24 value = 0 }
	set_global_variable = { name = bag_wtp_pn25 value = 0 }
	set_global_variable = { name = bag_wtp_pn26 value = 0 }
	set_global_variable = { name = bag_wtp_pn27 value = 0 }
	set_global_variable = { name = bag_wtp_pn28 value = 0 }
	set_global_variable = { name = bag_wtp_pn29 value = 0 }
	set_global_variable = { name = bag_wtp_pn30 value = 0 }
	set_global_variable = { name = bag_wtp_pn31 value = 0 }
	set_global_variable = { name = bag_wtp_pn32 value = 0 }
	set_global_variable = { name = bag_wtp_pn33 value = 0 }
	set_global_variable = { name = bag_wtp_pn34 value = 0 }
	set_global_variable = { name = bag_wtp_pn35 value = 0 }
	set_global_variable = { name = bag_wtp_pn36 value = 0 }
	set_global_variable = { name = bag_wtp_pn37 value = 0 }
	set_global_variable = { name = bag_wtp_pn38 value = 0 }
	set_global_variable = { name = bag_wtp_pn39 value = 0 }
	set_global_variable = { name = bag_wtp_pn40 value = 0 }
	set_global_variable = { name = bag_wtp_pn41 value = 0 }
	set_global_variable = { name = bag_wtp_pn42 value = 0 }
	set_global_variable = { name = bag_wtp_pn43 value = 0 }
	set_global_variable = { name = bag_wtp_pn44 value = 0 }
	set_global_variable = { name = bag_wtp_pn45 value = 0 }
	set_global_variable = { name = bag_wtp_pn46 value = 0 }
	set_global_variable = { name = bag_wtp_pn47 value = 0 }
}

# Every province of the chosen ground, made ready, once each.
#
# **Every counter the allocation reads has to exist on every location it can
# reach before the first round.** A comparison against a variable that is not
# there is the failure that logs nothing -- and it is not enough to prepare the
# candidates, because a good added to a province is written onto *all* of that
# province's locations, candidates or not.
#
# The province is reached through the first of its candidates and the rest of
# them fall out on the dirty flag, so this is one pass per province rather than
# one per location. That first candidate is the province's stand-in afterwards:
# `_plan_prov_locs` is one location per province and is what the ranking walks.

# Scope: country
bag_wtp_plan_prepare = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		if = {
			limit = { NOT = { has_variable = bag_wtp_plan_dirty } }
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_dirty value = 1 }
					set_variable = { name = bag_wtp_plan_town_n value = 0 }
					set_variable = { name = bag_wtp_plan_rural_n value = 0 }
					set_variable = { name = bag_wtp_plan_prov_load value = 0 }
					set_variable = { name = bag_wtp_plan_prank value = 9999 }
					set_variable = { name = bag_wtp_load value = 0 }
					remove_variable = bag_wtp_plan_right
					remove_variable = bag_wtp_plan_town_row
					clear_variable_list = bag_wtp_plan_town
					clear_variable_list = bag_wtp_plan_rural
					clear_variable_list = bag_wtp_plan_town_b
					clear_variable_list = bag_wtp_plan_rural_b
					clear_variable_list = bag_wtp_plan_goods
					clear_variable_list = bag_wtp_plan_builds
					add_to_global_variable_list = { name = bag_wtp_plan_touched target = this }
				}
			}
			add_to_global_variable_list = { name = bag_wtp_plan_prov_locs target = this }
			change_global_variable = { name = bag_wtp_plan_provn add = 1 }
		}
	}
	bag_wtp_plan_count_rooms = yes
}

# The capacity of the chosen ground: every candidate's cap, added up. Under the
# province model it is also what a finished plan should come to, so «мест»
# against «зданий» is a completeness reading. `_plan_towns` beside it is how many
# of the candidates the plan counts as built-up -- the first thing to look at if
# a plan comes back empty, because 0 or all of them says the rank test is the
# fault and nothing else can.
#
# The two caps arrive through their `_show_` script values rather than as
# `add = global_var:`: `add` takes a script value for certain, and everything
# else about the bare form is inference.
# Scope: country
bag_wtp_plan_count_rooms = {
	set_global_variable = { name = bag_wtp_plan_rooms value = 0 }
	set_global_variable = { name = bag_wtp_plan_towns value = 0 }
	every_in_global_list = {
		variable = bag_wtp_candidates
		if = {
			limit = { bag_wtp_plan_is_town = yes }
			change_global_variable = { name = bag_wtp_plan_rooms add = bag_wtp_show_plan_cap_urban }
			change_global_variable = { name = bag_wtp_plan_towns add = 1 }
		}
		else = {
			change_global_variable = { name = bag_wtp_plan_rooms add = bag_wtp_show_plan_cap_rural }
		}
	}
}

# One scoring pass per good, and the harvest that makes the answers comparable.
#
# **This is the expensive half of the mod and the reason the plan is a button of
# its own.** A single-good run reads about five methods on each candidate; this
# reads all 241 of them, 47 passes over the same ground.
# Scope: country
bag_wtp_plan_score = {
	bag_wtp_score_1 = yes
	bag_wtp_plan_harvest_1 = yes
	bag_wtp_score_2 = yes
	bag_wtp_plan_harvest_2 = yes
	bag_wtp_score_3 = yes
	bag_wtp_plan_harvest_3 = yes
	bag_wtp_score_4 = yes
	bag_wtp_plan_harvest_4 = yes
	bag_wtp_score_5 = yes
	bag_wtp_plan_harvest_5 = yes
	bag_wtp_score_6 = yes
	bag_wtp_plan_harvest_6 = yes
	bag_wtp_score_7 = yes
	bag_wtp_plan_harvest_7 = yes
	bag_wtp_score_8 = yes
	bag_wtp_plan_harvest_8 = yes
	bag_wtp_score_9 = yes
	bag_wtp_plan_harvest_9 = yes
	bag_wtp_score_10 = yes
	bag_wtp_plan_harvest_10 = yes
	bag_wtp_score_11 = yes
	bag_wtp_plan_harvest_11 = yes
	bag_wtp_score_12 = yes
	bag_wtp_plan_harvest_12 = yes
	bag_wtp_score_13 = yes
	bag_wtp_plan_harvest_13 = yes
	bag_wtp_score_14 = yes
	bag_wtp_plan_harvest_14 = yes
	bag_wtp_score_15 = yes
	bag_wtp_plan_harvest_15 = yes
	bag_wtp_score_16 = yes
	bag_wtp_plan_harvest_16 = yes
	bag_wtp_score_17 = yes
	bag_wtp_plan_harvest_17 = yes
	bag_wtp_score_18 = yes
	bag_wtp_plan_harvest_18 = yes
	bag_wtp_score_19 = yes
	bag_wtp_plan_harvest_19 = yes
	bag_wtp_score_20 = yes
	bag_wtp_plan_harvest_20 = yes
	bag_wtp_score_21 = yes
	bag_wtp_plan_harvest_21 = yes
	bag_wtp_score_22 = yes
	bag_wtp_plan_harvest_22 = yes
	bag_wtp_score_23 = yes
	bag_wtp_plan_harvest_23 = yes
	bag_wtp_score_24 = yes
	bag_wtp_plan_harvest_24 = yes
	bag_wtp_score_25 = yes
	bag_wtp_plan_harvest_25 = yes
	bag_wtp_score_26 = yes
	bag_wtp_plan_harvest_26 = yes
	bag_wtp_score_27 = yes
	bag_wtp_plan_harvest_27 = yes
	bag_wtp_score_28 = yes
	bag_wtp_plan_harvest_28 = yes
	bag_wtp_score_29 = yes
	bag_wtp_plan_harvest_29 = yes
	bag_wtp_score_30 = yes
	bag_wtp_plan_harvest_30 = yes
	bag_wtp_score_31 = yes
	bag_wtp_plan_harvest_31 = yes
	bag_wtp_score_32 = yes
	bag_wtp_plan_harvest_32 = yes
	bag_wtp_score_33 = yes
	bag_wtp_plan_harvest_33 = yes
	bag_wtp_score_34 = yes
	bag_wtp_plan_harvest_34 = yes
	bag_wtp_score_35 = yes
	bag_wtp_plan_harvest_35 = yes
	bag_wtp_score_36 = yes
	bag_wtp_plan_harvest_36 = yes
	bag_wtp_score_37 = yes
	bag_wtp_plan_harvest_37 = yes
	bag_wtp_score_38 = yes
	bag_wtp_plan_harvest_38 = yes
	bag_wtp_score_39 = yes
	bag_wtp_plan_harvest_39 = yes
	bag_wtp_score_40 = yes
	bag_wtp_plan_harvest_40 = yes
	bag_wtp_score_41 = yes
	bag_wtp_plan_harvest_41 = yes
	bag_wtp_score_42 = yes
	bag_wtp_plan_harvest_42 = yes
	bag_wtp_score_43 = yes
	bag_wtp_plan_harvest_43 = yes
	bag_wtp_score_44 = yes
	bag_wtp_plan_harvest_44 = yes
	bag_wtp_score_45 = yes
	bag_wtp_plan_harvest_45 = yes
	bag_wtp_score_46 = yes
	bag_wtp_plan_harvest_46 = yes
	bag_wtp_score_47 = yes
	bag_wtp_plan_harvest_47 = yes
}

# clay: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_1 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p1 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm1 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr1 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm1 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p1 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm1 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr1 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm1 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax1
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax1 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p1 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p1 multiply = 1000 }
			change_variable = { name = bag_wtp_pr1 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr1 multiply = 1000 }
		}
	}
}

# coal: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_2 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p2 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm2 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr2 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm2 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p2 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm2 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr2 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm2 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax2
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax2 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p2 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p2 multiply = 1000 }
			change_variable = { name = bag_wtp_pr2 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr2 multiply = 1000 }
		}
	}
}

# cotton: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_3 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p3 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm3 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr3 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm3 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p3 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm3 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr3 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm3 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax3
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax3 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p3 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p3 multiply = 1000 }
			change_variable = { name = bag_wtp_pr3 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr3 multiply = 1000 }
		}
	}
}

# dyes: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_4 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p4 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm4 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr4 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm4 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p4 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm4 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr4 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm4 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax4
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax4 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p4 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p4 multiply = 1000 }
			change_variable = { name = bag_wtp_pr4 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr4 multiply = 1000 }
		}
	}
}

# fiber_crops: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_5 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p5 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm5 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr5 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm5 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p5 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm5 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr5 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm5 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax5
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax5 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p5 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p5 multiply = 1000 }
			change_variable = { name = bag_wtp_pr5 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr5 multiply = 1000 }
		}
	}
}

# fish: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_6 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p6 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm6 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr6 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm6 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p6 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm6 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr6 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm6 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax6
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax6 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p6 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p6 multiply = 1000 }
			change_variable = { name = bag_wtp_pr6 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr6 multiply = 1000 }
		}
	}
}

# fruit: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_7 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p7 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm7 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr7 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm7 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p7 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm7 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr7 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm7 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax7
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax7 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p7 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p7 multiply = 1000 }
			change_variable = { name = bag_wtp_pr7 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr7 multiply = 1000 }
		}
	}
}

# horses: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_8 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p8 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm8 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr8 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm8 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p8 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm8 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr8 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm8 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax8
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax8 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p8 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p8 multiply = 1000 }
			change_variable = { name = bag_wtp_pr8 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr8 multiply = 1000 }
		}
	}
}

# incense: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_9 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p9 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm9 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr9 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm9 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p9 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm9 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr9 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm9 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax9
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax9 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p9 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p9 multiply = 1000 }
			change_variable = { name = bag_wtp_pr9 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr9 multiply = 1000 }
		}
	}
}

# iron: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_10 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p10 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm10 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr10 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm10 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p10 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm10 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr10 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm10 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax10
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax10 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p10 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p10 multiply = 1000 }
			change_variable = { name = bag_wtp_pr10 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr10 multiply = 1000 }
		}
	}
}

# ivory: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_11 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p11 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm11 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr11 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm11 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p11 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm11 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr11 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm11 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax11
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax11 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p11 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p11 multiply = 1000 }
			change_variable = { name = bag_wtp_pr11 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr11 multiply = 1000 }
		}
	}
}

# livestock: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_12 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p12 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm12 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr12 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm12 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p12 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm12 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr12 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm12 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax12
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax12 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p12 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p12 multiply = 1000 }
			change_variable = { name = bag_wtp_pr12 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr12 multiply = 1000 }
		}
	}
}

# lumber: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_13 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p13 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm13 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr13 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm13 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p13 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm13 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr13 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm13 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax13
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax13 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p13 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p13 multiply = 1000 }
			change_variable = { name = bag_wtp_pr13 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr13 multiply = 1000 }
		}
	}
}

# medicaments: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_14 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p14 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm14 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr14 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm14 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p14 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm14 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr14 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm14 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax14
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax14 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p14 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p14 multiply = 1000 }
			change_variable = { name = bag_wtp_pr14 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr14 multiply = 1000 }
		}
	}
}

# salt: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_15 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p15 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm15 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr15 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm15 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p15 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm15 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr15 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm15 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax15
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax15 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p15 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p15 multiply = 1000 }
			change_variable = { name = bag_wtp_pr15 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr15 multiply = 1000 }
		}
	}
}

# saltpeter: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_16 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p16 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm16 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr16 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm16 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p16 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm16 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr16 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm16 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax16
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax16 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p16 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p16 multiply = 1000 }
			change_variable = { name = bag_wtp_pr16 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr16 multiply = 1000 }
		}
	}
}

# sand: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_17 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p17 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm17 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr17 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm17 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p17 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm17 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr17 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm17 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax17
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax17 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p17 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p17 multiply = 1000 }
			change_variable = { name = bag_wtp_pr17 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr17 multiply = 1000 }
		}
	}
}

# silk: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_18 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p18 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm18 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr18 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm18 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p18 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm18 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr18 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm18 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax18
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax18 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p18 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p18 multiply = 1000 }
			change_variable = { name = bag_wtp_pr18 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr18 multiply = 1000 }
		}
	}
}

# silver: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_19 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p19 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm19 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr19 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm19 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p19 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm19 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr19 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm19 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax19
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax19 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p19 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p19 multiply = 1000 }
			change_variable = { name = bag_wtp_pr19 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr19 multiply = 1000 }
		}
	}
}

# stone: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_20 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p20 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm20 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr20 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm20 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p20 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm20 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr20 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm20 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax20
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax20 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p20 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p20 multiply = 1000 }
			change_variable = { name = bag_wtp_pr20 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr20 multiply = 1000 }
		}
	}
}

# sugar: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_21 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p21 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm21 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr21 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm21 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p21 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm21 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr21 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm21 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax21
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax21 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p21 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p21 multiply = 1000 }
			change_variable = { name = bag_wtp_pr21 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr21 multiply = 1000 }
		}
	}
}

# tobacco: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_22 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p22 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm22 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr22 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm22 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p22 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm22 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr22 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm22 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax22
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax22 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p22 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p22 multiply = 1000 }
			change_variable = { name = bag_wtp_pr22 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr22 multiply = 1000 }
		}
	}
}

# wild_game: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_23 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p23 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm23 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr23 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm23 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p23 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm23 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr23 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm23 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax23
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax23 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p23 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p23 multiply = 1000 }
			change_variable = { name = bag_wtp_pr23 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr23 multiply = 1000 }
		}
	}
}

# wine: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_24 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p24 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm24 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr24 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm24 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p24 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm24 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr24 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm24 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax24
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax24 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p24 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p24 multiply = 1000 }
			change_variable = { name = bag_wtp_pr24 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr24 multiply = 1000 }
		}
	}
}

# wool: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_25 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p25 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm25 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr25 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm25 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p25 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm25 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr25 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm25 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax25
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax25 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p25 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p25 multiply = 1000 }
			change_variable = { name = bag_wtp_pr25 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr25 multiply = 1000 }
		}
	}
}

# beer: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_26 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p26 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm26 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr26 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm26 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p26 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm26 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr26 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm26 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax26
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax26 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p26 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p26 multiply = 1000 }
			change_variable = { name = bag_wtp_pr26 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr26 multiply = 1000 }
		}
	}
}

# books: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_27 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p27 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm27 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr27 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm27 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p27 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm27 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr27 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm27 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax27
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax27 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p27 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p27 multiply = 1000 }
			change_variable = { name = bag_wtp_pr27 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr27 multiply = 1000 }
		}
	}
}

# cannons: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_28 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p28 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm28 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr28 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm28 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p28 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm28 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr28 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm28 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax28
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax28 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p28 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p28 multiply = 1000 }
			change_variable = { name = bag_wtp_pr28 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr28 multiply = 1000 }
		}
	}
}

# cloth: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_29 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p29 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm29 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr29 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm29 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p29 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm29 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr29 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm29 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax29
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax29 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p29 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p29 multiply = 1000 }
			change_variable = { name = bag_wtp_pr29 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr29 multiply = 1000 }
		}
	}
}

# fine_cloth: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_30 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p30 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm30 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr30 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm30 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p30 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm30 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr30 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm30 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax30
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax30 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p30 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p30 multiply = 1000 }
			change_variable = { name = bag_wtp_pr30 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr30 multiply = 1000 }
		}
	}
}

# firearms: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_31 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p31 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm31 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr31 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm31 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p31 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm31 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr31 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm31 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax31
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax31 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p31 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p31 multiply = 1000 }
			change_variable = { name = bag_wtp_pr31 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr31 multiply = 1000 }
		}
	}
}

# furniture: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_32 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p32 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm32 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr32 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm32 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p32 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm32 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr32 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm32 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax32
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax32 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p32 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p32 multiply = 1000 }
			change_variable = { name = bag_wtp_pr32 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr32 multiply = 1000 }
		}
	}
}

# glass: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_33 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p33 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm33 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr33 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm33 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p33 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm33 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr33 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm33 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax33
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax33 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p33 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p33 multiply = 1000 }
			change_variable = { name = bag_wtp_pr33 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr33 multiply = 1000 }
		}
	}
}

# jewelry: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_34 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p34 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm34 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr34 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm34 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p34 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm34 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr34 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm34 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax34
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax34 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p34 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p34 multiply = 1000 }
			change_variable = { name = bag_wtp_pr34 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr34 multiply = 1000 }
		}
	}
}

# lacquerware: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_35 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p35 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm35 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr35 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm35 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p35 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm35 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr35 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm35 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax35
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax35 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p35 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p35 multiply = 1000 }
			change_variable = { name = bag_wtp_pr35 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr35 multiply = 1000 }
		}
	}
}

# leather: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_36 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p36 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm36 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr36 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm36 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p36 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm36 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr36 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm36 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax36
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax36 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p36 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p36 multiply = 1000 }
			change_variable = { name = bag_wtp_pr36 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr36 multiply = 1000 }
		}
	}
}

# liquor: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_37 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p37 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm37 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr37 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm37 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p37 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm37 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr37 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm37 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax37
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax37 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p37 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p37 multiply = 1000 }
			change_variable = { name = bag_wtp_pr37 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr37 multiply = 1000 }
		}
	}
}

# masonry: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_38 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p38 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm38 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr38 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm38 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p38 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm38 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr38 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm38 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax38
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax38 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p38 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p38 multiply = 1000 }
			change_variable = { name = bag_wtp_pr38 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr38 multiply = 1000 }
		}
	}
}

# naval_supplies: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_39 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p39 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm39 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr39 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm39 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p39 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm39 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr39 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm39 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax39
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax39 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p39 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p39 multiply = 1000 }
			change_variable = { name = bag_wtp_pr39 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr39 multiply = 1000 }
		}
	}
}

# paper: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_40 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p40 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm40 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr40 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm40 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p40 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm40 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr40 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm40 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax40
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax40 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p40 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p40 multiply = 1000 }
			change_variable = { name = bag_wtp_pr40 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr40 multiply = 1000 }
		}
	}
}

# porcelain: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_41 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p41 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm41 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr41 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm41 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p41 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm41 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr41 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm41 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax41
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax41 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p41 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p41 multiply = 1000 }
			change_variable = { name = bag_wtp_pr41 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr41 multiply = 1000 }
		}
	}
}

# pottery: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_42 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p42 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm42 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr42 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm42 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p42 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm42 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr42 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm42 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax42
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax42 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p42 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p42 multiply = 1000 }
			change_variable = { name = bag_wtp_pr42 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr42 multiply = 1000 }
		}
	}
}

# slaves_goods: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_43 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p43 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm43 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr43 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm43 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p43 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm43 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr43 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm43 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax43
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax43 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p43 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p43 multiply = 1000 }
			change_variable = { name = bag_wtp_pr43 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr43 multiply = 1000 }
		}
	}
}

# steel: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_44 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p44 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm44 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr44 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm44 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p44 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm44 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr44 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm44 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax44
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax44 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p44 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p44 multiply = 1000 }
			change_variable = { name = bag_wtp_pr44 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr44 multiply = 1000 }
		}
	}
}

# tar: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_45 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p45 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm45 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr45 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm45 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p45 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm45 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr45 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm45 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax45
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax45 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p45 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p45 multiply = 1000 }
			change_variable = { name = bag_wtp_pr45 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr45 multiply = 1000 }
		}
	}
}

# tools: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_46 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p46 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm46 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr46 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm46 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p46 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm46 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr46 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm46 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax46
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax46 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p46 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p46 multiply = 1000 }
			change_variable = { name = bag_wtp_pr46 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr46 multiply = 1000 }
		}
	}
}

# weaponry: keep both sides, then divide the ground by the better of them, so that
# 1000 means "the province this good wants most" for every good alike. A good
# nothing in this ground can make stays at zero and never picks.
#
# **The method that won is kept, and it has to be**: a location holds one
# building of a type, so what the allocation must not repeat in a province is
# the building, and only the method knows which one that is.
# Scope: country
bag_wtp_plan_harvest_47 = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		# **The two sides are "where may this building stand", not "is it a
		# village".** Thirty production buildings declare `rural_settlement` and
		# only four of them are villages, so the ranking's category split would
		# leave a rural province with nothing but villages to be offered -- which
		# is what the thirtieth load showed. `_pnowbest_*` and `_pendbest_*` are
		# the plan's own accumulators, split on the building's own rank gates.
		if = {
			limit = { has_global_variable = bag_wtp_plan_by_end }
			set_variable = { name = bag_wtp_p47 value = var:bag_wtp_pendbest_t }
			set_variable = { name = bag_wtp_pm47 value = var:bag_wtp_pendbest_method_t }
			set_variable = { name = bag_wtp_pr47 value = var:bag_wtp_pendbest_r }
			set_variable = { name = bag_wtp_prm47 value = var:bag_wtp_pendbest_method_r }
		}
		else = {
			set_variable = { name = bag_wtp_p47 value = var:bag_wtp_pnowbest_t }
			set_variable = { name = bag_wtp_pm47 value = var:bag_wtp_pnowbest_method_t }
			set_variable = { name = bag_wtp_pr47 value = var:bag_wtp_pnowbest_r }
			set_variable = { name = bag_wtp_prm47 value = var:bag_wtp_pnowbest_method_r }
		}
	}
	set_global_variable = { name = bag_wtp_plan_top value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		order_by = bag_wtp_ordmax47
		max = 1
		check_range_bounds = no
		set_global_variable = { name = bag_wtp_plan_top value = bag_wtp_ordmax47 }
	}
	if = {
		limit = { global_var:bag_wtp_plan_top > 0 }
		change_global_variable = { name = bag_wtp_plan_scored add = 1 }
		every_in_global_list = {
			variable = bag_wtp_candidates
			# Divided before it is scaled and never after: the top of the ground is
			# already a scaled number, and multiplying first would ask the engine's
			# fixed point for a million. One divisor for both sides, so the village
			# keeps its honest fifth of the workshop.
			change_variable = { name = bag_wtp_p47 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_p47 multiply = 1000 }
			change_variable = { name = bag_wtp_pr47 divide = bag_wtp_plan_top_value }
			change_variable = { name = bag_wtp_pr47 multiply = 1000 }
		}
	}
}

# One good, onto every location of the province the candidate belongs to.
#
# **Guarded, and called from the location rather than the province.** The same
# `_plan_can_*` trigger the ordered walk used as its `limit` is asked again here,
# so the urban-rights round can call this on its own without repeating a word of
# it -- and so that a right whose second good wants a building the first already
# took is simply not given it.
#
# The building goes into a list of its own beside the good, because that is what
# a location can only hold one of. Which building it is comes off the method the
# harvest kept.
# clay, town side.
# Scope: location
bag_wtp_plan_try_town_1 = {
	if = {
		limit = { bag_wtp_plan_can_town_1 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm1 = 39
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:clay }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:clay_pit }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn1 add = 1 }
		}
	}
}
# coal, town side.
# Scope: location
bag_wtp_plan_try_town_2 = {
	if = {
		limit = { bag_wtp_plan_can_town_2 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm2 = 56
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:coal }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:improved_charcoal_maker }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn2 add = 1 }
		}
	}
}
# cotton, town side.
# Scope: location
bag_wtp_plan_try_town_3 = {
	if = {
		limit = { bag_wtp_plan_can_town_3 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm3 = 57
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cotton }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cotton_plantation }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn3 add = 1 }
		}
	}
}
# dyes, town side.
# Scope: location
bag_wtp_plan_try_town_4 = {
	if = {
		limit = { bag_wtp_plan_can_town_4 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm4 = 58
				var:bag_wtp_pm4 = 59
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:dyes }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:dyes_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn4 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm4 = 60
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:dyes }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:dyes_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn4 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm4 = 61
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:dyes }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:dyes_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn4 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm4 = 62
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:dyes }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:dyesworks }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn4 add = 1 }
		}
	}
}
# fiber_crops, town side.
# Scope: location
bag_wtp_plan_try_town_5 = {
	if = {
		limit = { bag_wtp_plan_can_town_5 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm5 = 63
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fiber_crops }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fiber_crops_farm }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn5 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm5 = 64
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fiber_crops }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn5 add = 1 }
		}
	}
}
# fruit, town side.
# Scope: location
bag_wtp_plan_try_town_7 = {
	if = {
		limit = { bag_wtp_plan_can_town_7 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm7 = 110
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fruit }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fruit_orchard }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn7 add = 1 }
		}
	}
}
# horses, town side.
# Scope: location
bag_wtp_plan_try_town_8 = {
	if = {
		limit = { bag_wtp_plan_can_town_8 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm8 = 121
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:horses }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:horse_breeders }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn8 add = 1 }
		}
	}
}
# incense, town side.
# Scope: location
bag_wtp_plan_try_town_9 = {
	if = {
		limit = { bag_wtp_plan_can_town_9 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm9 = 122
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:incense }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:perfumery }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn9 add = 1 }
		}
	}
}
# iron, town side.
# Scope: location
bag_wtp_plan_try_town_10 = {
	if = {
		limit = { bag_wtp_plan_can_town_10 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm10 = 123
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:iron }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:bog_iron_smelter }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn10 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm10 = 124
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:iron }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn10 add = 1 }
		}
	}
}
# lumber, town side.
# Scope: location
bag_wtp_plan_try_town_13 = {
	if = {
		limit = { bag_wtp_plan_can_town_13 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm13 = 172
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:lumber }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn13 add = 1 }
		}
	}
}
# medicaments, town side.
# Scope: location
bag_wtp_plan_try_town_14 = {
	if = {
		limit = { bag_wtp_plan_can_town_14 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm14 = 176
				var:bag_wtp_pm14 = 177
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:medicaments }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:apothecary }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn14 add = 1 }
		}
	}
}
# salt, town side.
# Scope: location
bag_wtp_plan_try_town_15 = {
	if = {
		limit = { bag_wtp_plan_can_town_15 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm15 = 199
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:salt }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:salt_collector }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn15 add = 1 }
		}
	}
}
# saltpeter, town side.
# Scope: location
bag_wtp_plan_try_town_16 = {
	if = {
		limit = { bag_wtp_plan_can_town_16 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm16 = 200
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:saltpeter }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:putrefaction_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn16 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm16 = 201
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:saltpeter }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:putrefaction_works }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn16 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm16 = 202
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:saltpeter }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:saltpeter_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn16 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm16 = 203
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:saltpeter }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:saltpeter_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn16 add = 1 }
		}
	}
}
# sand, town side.
# Scope: location
bag_wtp_plan_try_town_17 = {
	if = {
		limit = { bag_wtp_plan_can_town_17 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm17 = 204
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:sand }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:sand_pit }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn17 add = 1 }
		}
	}
}
# silk, town side.
# Scope: location
bag_wtp_plan_try_town_18 = {
	if = {
		limit = { bag_wtp_plan_can_town_18 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm18 = 205
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:silk }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:ablaq_palace }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn18 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm18 = 206
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:silk }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:lucca_silk_production_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn18 add = 1 }
		}
	}
}
# silver, town side.
# Scope: location
bag_wtp_plan_try_town_19 = {
	if = {
		limit = { bag_wtp_plan_can_town_19 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm19 = 207
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:silver }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:schwaz_mine }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn19 add = 1 }
		}
	}
}
# stone, town side.
# Scope: location
bag_wtp_plan_try_town_20 = {
	if = {
		limit = { bag_wtp_plan_can_town_20 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm20 = 210
				var:bag_wtp_pm20 = 211
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:stone }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:stone_quarry }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn20 add = 1 }
		}
	}
}
# sugar, town side.
# Scope: location
bag_wtp_plan_try_town_21 = {
	if = {
		limit = { bag_wtp_plan_can_town_21 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm21 = 212
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:sugar }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:sugar_plantation }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn21 add = 1 }
		}
	}
}
# tobacco, town side.
# Scope: location
bag_wtp_plan_try_town_22 = {
	if = {
		limit = { bag_wtp_plan_can_town_22 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm22 = 214
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tobacco }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tobacco_plantation }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn22 add = 1 }
		}
	}
}
# wine, town side.
# Scope: location
bag_wtp_plan_try_town_24 = {
	if = {
		limit = { bag_wtp_plan_can_town_24 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm24 = 235
				var:bag_wtp_pm24 = 236
				var:bag_wtp_pm24 = 237
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:wine }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:winery }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn24 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm24 = 238
				var:bag_wtp_pm24 = 239
				var:bag_wtp_pm24 = 240
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:wine }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:winery_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn24 add = 1 }
		}
	}
}
# beer, town side.
# Scope: location
bag_wtp_plan_try_town_26 = {
	if = {
		limit = { bag_wtp_plan_can_town_26 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm26 = 1
				var:bag_wtp_pm26 = 2
				var:bag_wtp_pm26 = 3
				var:bag_wtp_pm26 = 4
				var:bag_wtp_pm26 = 5
				var:bag_wtp_pm26 = 6
				var:bag_wtp_pm26 = 7
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:beer }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:beer_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn26 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm26 = 8
				var:bag_wtp_pm26 = 9
				var:bag_wtp_pm26 = 10
				var:bag_wtp_pm26 = 11
				var:bag_wtp_pm26 = 12
				var:bag_wtp_pm26 = 13
				var:bag_wtp_pm26 = 14
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:beer }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:brewery }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn26 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm26 = 15
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:beer }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:brewery_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn26 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm26 = 16
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:beer }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:brewery_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn26 add = 1 }
		}
	}
}
# books, town side.
# Scope: location
bag_wtp_plan_try_town_27 = {
	if = {
		limit = { bag_wtp_plan_can_town_27 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm27 = 18
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:books }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:printing_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn27 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm27 = 19
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:books }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:printing_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn27 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm27 = 20
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:books }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:printing_press_shop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn27 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm27 = 21
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:books }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:scriptorium }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn27 add = 1 }
		}
	}
}
# cannons, town side.
# Scope: location
bag_wtp_plan_try_town_28 = {
	if = {
		limit = { bag_wtp_plan_can_town_28 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm28 = 22
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cannons }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cannon_foundry }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn28 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm28 = 23
				var:bag_wtp_pm28 = 24
				var:bag_wtp_pm28 = 25
				var:bag_wtp_pm28 = 26
				var:bag_wtp_pm28 = 27
				var:bag_wtp_pm28 = 28
				var:bag_wtp_pm28 = 29
				var:bag_wtp_pm28 = 30
				var:bag_wtp_pm28 = 31
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cannons }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cannon_maker }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn28 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm28 = 32
				var:bag_wtp_pm28 = 33
				var:bag_wtp_pm28 = 34
				var:bag_wtp_pm28 = 35
				var:bag_wtp_pm28 = 36
				var:bag_wtp_pm28 = 37
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cannons }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cannon_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn28 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm28 = 38
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cannons }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cannons_factory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn28 add = 1 }
		}
	}
}
# cloth, town side.
# Scope: location
bag_wtp_plan_try_town_29 = {
	if = {
		limit = { bag_wtp_plan_can_town_29 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm29 = 40
				var:bag_wtp_pm29 = 41
				var:bag_wtp_pm29 = 42
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cloth_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn29 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm29 = 43
				var:bag_wtp_pm29 = 44
				var:bag_wtp_pm29 = 45
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cloth_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn29 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm29 = 46
				var:bag_wtp_pm29 = 47
				var:bag_wtp_pm29 = 48
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:cloth_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn29 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm29 = 52
				var:bag_wtp_pm29 = 53
				var:bag_wtp_pm29 = 54
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:textile_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn29 add = 1 }
		}
	}
}
# fine_cloth, town side.
# Scope: location
bag_wtp_plan_try_town_30 = {
	if = {
		limit = { bag_wtp_plan_can_town_30 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm30 = 65
				var:bag_wtp_pm30 = 66
				var:bag_wtp_pm30 = 67
				var:bag_wtp_pm30 = 68
				var:bag_wtp_pm30 = 69
				var:bag_wtp_pm30 = 70
				var:bag_wtp_pm30 = 71
				var:bag_wtp_pm30 = 72
				var:bag_wtp_pm30 = 73
				var:bag_wtp_pm30 = 74
				var:bag_wtp_pm30 = 75
				var:bag_wtp_pm30 = 76
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fine_cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fine_cloth_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn30 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm30 = 77
				var:bag_wtp_pm30 = 78
				var:bag_wtp_pm30 = 79
				var:bag_wtp_pm30 = 80
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fine_cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fine_cloth_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn30 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm30 = 81
				var:bag_wtp_pm30 = 82
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fine_cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fine_cloth_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn30 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm30 = 83
				var:bag_wtp_pm30 = 84
				var:bag_wtp_pm30 = 85
				var:bag_wtp_pm30 = 86
				var:bag_wtp_pm30 = 87
				var:bag_wtp_pm30 = 88
				var:bag_wtp_pm30 = 89
				var:bag_wtp_pm30 = 90
				var:bag_wtp_pm30 = 91
				var:bag_wtp_pm30 = 92
				var:bag_wtp_pm30 = 93
				var:bag_wtp_pm30 = 94
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:fine_cloth }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:fine_cloth_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn30 add = 1 }
		}
	}
}
# firearms, town side.
# Scope: location
bag_wtp_plan_try_town_31 = {
	if = {
		limit = { bag_wtp_plan_can_town_31 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm31 = 95
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:firearms }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:firearms_factory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn31 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm31 = 96
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:firearms }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:firearms_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn31 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm31 = 97
				var:bag_wtp_pm31 = 98
				var:bag_wtp_pm31 = 99
				var:bag_wtp_pm31 = 100
				var:bag_wtp_pm31 = 101
				var:bag_wtp_pm31 = 102
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:firearms }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:gun_smith }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn31 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm31 = 103
				var:bag_wtp_pm31 = 104
				var:bag_wtp_pm31 = 105
				var:bag_wtp_pm31 = 106
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:firearms }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:guns_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn31 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm31 = 107
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:firearms }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:hand_cannon_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn31 add = 1 }
		}
	}
}
# furniture, town side.
# Scope: location
bag_wtp_plan_try_town_32 = {
	if = {
		limit = { bag_wtp_plan_can_town_32 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm32 = 111
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:furniture }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:furniture_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn32 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm32 = 112
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:furniture }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:furniture_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn32 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm32 = 113
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:furniture }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:furniture_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn32 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm32 = 114
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:furniture }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:furniture_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn32 add = 1 }
		}
	}
}
# glass, town side.
# Scope: location
bag_wtp_plan_try_town_33 = {
	if = {
		limit = { bag_wtp_plan_can_town_33 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm33 = 115
				var:bag_wtp_pm33 = 116
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:glass }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:glass_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn33 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm33 = 117
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:glass }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:glass_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn33 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm33 = 118
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:glass }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:glass_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn33 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm33 = 119
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:glass }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:glassworks }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn33 add = 1 }
		}
	}
}
# jewelry, town side.
# Scope: location
bag_wtp_plan_try_town_34 = {
	if = {
		limit = { bag_wtp_plan_can_town_34 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm34 = 126
				var:bag_wtp_pm34 = 127
				var:bag_wtp_pm34 = 128
				var:bag_wtp_pm34 = 129
				var:bag_wtp_pm34 = 130
				var:bag_wtp_pm34 = 131
				var:bag_wtp_pm34 = 132
				var:bag_wtp_pm34 = 133
				var:bag_wtp_pm34 = 134
				var:bag_wtp_pm34 = 135
				var:bag_wtp_pm34 = 136
				var:bag_wtp_pm34 = 137
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:jewelry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:jewelry_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn34 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm34 = 139
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:jewelry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:pounamou_carver }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn34 add = 1 }
		}
	}
}
# lacquerware, town side.
# Scope: location
bag_wtp_plan_try_town_35 = {
	if = {
		limit = { bag_wtp_plan_can_town_35 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm35 = 140
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:lacquerware }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:lacquerware_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn35 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm35 = 141
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:lacquerware }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:lacquerware_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn35 add = 1 }
		}
	}
}
# leather, town side.
# Scope: location
bag_wtp_plan_try_town_36 = {
	if = {
		limit = { bag_wtp_plan_can_town_36 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm36 = 143
				var:bag_wtp_pm36 = 144
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:leather }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tannery }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn36 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm36 = 145
				var:bag_wtp_pm36 = 146
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:leather }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tannery_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn36 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm36 = 147
				var:bag_wtp_pm36 = 148
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:leather }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tannery_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn36 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm36 = 149
				var:bag_wtp_pm36 = 150
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:leather }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tanning_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn36 add = 1 }
		}
	}
}
# liquor, town side.
# Scope: location
bag_wtp_plan_try_town_37 = {
	if = {
		limit = { bag_wtp_plan_can_town_37 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm37 = 151
				var:bag_wtp_pm37 = 152
				var:bag_wtp_pm37 = 153
				var:bag_wtp_pm37 = 154
				var:bag_wtp_pm37 = 155
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:liquor }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:distiller_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn37 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm37 = 156
				var:bag_wtp_pm37 = 157
				var:bag_wtp_pm37 = 158
				var:bag_wtp_pm37 = 159
				var:bag_wtp_pm37 = 160
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:liquor }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:distiller_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn37 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm37 = 161
				var:bag_wtp_pm37 = 162
				var:bag_wtp_pm37 = 163
				var:bag_wtp_pm37 = 164
				var:bag_wtp_pm37 = 165
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:liquor }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:distillers_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn37 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm37 = 166
				var:bag_wtp_pm37 = 167
				var:bag_wtp_pm37 = 168
				var:bag_wtp_pm37 = 169
				var:bag_wtp_pm37 = 170
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:liquor }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:distillers_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn37 add = 1 }
		}
	}
}
# masonry, town side.
# Scope: location
bag_wtp_plan_try_town_38 = {
	if = {
		limit = { bag_wtp_plan_can_town_38 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm38 = 173
				var:bag_wtp_pm38 = 174
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:masonry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:mason }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn38 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm38 = 175
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:masonry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn38 add = 1 }
		}
	}
}
# naval_supplies, town side.
# Scope: location
bag_wtp_plan_try_town_39 = {
	if = {
		limit = { bag_wtp_plan_can_town_39 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm39 = 179
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:naval_supplies }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:naval_supplies_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn39 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm39 = 180
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:naval_supplies }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:naval_supplies_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn39 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm39 = 181
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:naval_supplies }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:naval_supplies_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn39 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm39 = 182
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:naval_supplies }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:naval_supplies_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn39 add = 1 }
		}
	}
}
# paper, town side.
# Scope: location
bag_wtp_plan_try_town_40 = {
	if = {
		limit = { bag_wtp_plan_can_town_40 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm40 = 183
				var:bag_wtp_pm40 = 184
				var:bag_wtp_pm40 = 185
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:paper }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:paper_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn40 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm40 = 186
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:paper }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:paper_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn40 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm40 = 187
				var:bag_wtp_pm40 = 188
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:paper }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:paper_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn40 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm40 = 189
				var:bag_wtp_pm40 = 190
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:paper }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:paper_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn40 add = 1 }
		}
	}
}
# porcelain, town side.
# Scope: location
bag_wtp_plan_try_town_41 = {
	if = {
		limit = { bag_wtp_plan_can_town_41 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm41 = 191
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:porcelain }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:porcelain_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn41 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm41 = 192
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:porcelain }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:porcelain_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn41 add = 1 }
		}
	}
}
# pottery, town side.
# Scope: location
bag_wtp_plan_try_town_42 = {
	if = {
		limit = { bag_wtp_plan_can_town_42 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm42 = 195
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:pottery_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm42 = 196
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:pottery_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm42 = 197
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:pottery_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm42 = 198
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:pottery_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
	}
}
# slaves_goods, town side.
# Scope: location
bag_wtp_plan_try_town_43 = {
	if = {
		limit = { bag_wtp_plan_can_town_43 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm43 = 208
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:slaves_goods }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:slave_center }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn43 add = 1 }
		}
	}
}
# steel, town side.
# Scope: location
bag_wtp_plan_try_town_44 = {
	if = {
		limit = { bag_wtp_plan_can_town_44 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm44 = 209
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:steel }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:steel_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn44 add = 1 }
		}
	}
}
# tar, town side.
# Scope: location
bag_wtp_plan_try_town_45 = {
	if = {
		limit = { bag_wtp_plan_can_town_45 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm45 = 213
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tar }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tar_kiln }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn45 add = 1 }
		}
	}
}
# tools, town side.
# Scope: location
bag_wtp_plan_try_town_46 = {
	if = {
		limit = { bag_wtp_plan_can_town_46 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm46 = 216
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:iron_foundry }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm46 = 217
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:iron_mill }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm46 = 219
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tatara }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm46 = 220
				var:bag_wtp_pm46 = 221
				var:bag_wtp_pm46 = 222
				var:bag_wtp_pm46 = 223
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tools_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm46 = 224
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tools_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
	}
}
# weaponry, town side.
# Scope: location
bag_wtp_plan_try_town_47 = {
	if = {
		limit = { bag_wtp_plan_can_town_47 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_pm47 = 227
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:tatara }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm47 = 228
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:weapon_factory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm47 = 229
				var:bag_wtp_pm47 = 230
				var:bag_wtp_pm47 = 231
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:weapon_guild }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm47 = 232
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:weapon_manufactory }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_pm47 = 233
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_town target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_town_b target = building_type:weapon_workshop }
					change_variable = { name = bag_wtp_plan_town_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
	}
}
# clay, village side.
# Scope: location
bag_wtp_plan_try_rural_1 = {
	if = {
		limit = { bag_wtp_plan_can_rural_1 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm1 = 39
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:clay }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:clay_pit }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn1 add = 1 }
		}
	}
}
# coal, village side.
# Scope: location
bag_wtp_plan_try_rural_2 = {
	if = {
		limit = { bag_wtp_plan_can_rural_2 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm2 = 55
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:coal }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:charcoal_maker }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn2 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm2 = 56
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:coal }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:improved_charcoal_maker }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn2 add = 1 }
		}
	}
}
# cotton, village side.
# Scope: location
bag_wtp_plan_try_rural_3 = {
	if = {
		limit = { bag_wtp_plan_can_rural_3 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm3 = 57
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:cotton }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:cotton_plantation }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn3 add = 1 }
		}
	}
}
# fiber_crops, village side.
# Scope: location
bag_wtp_plan_try_rural_5 = {
	if = {
		limit = { bag_wtp_plan_can_rural_5 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm5 = 63
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:fiber_crops }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:fiber_crops_farm }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn5 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm5 = 64
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:fiber_crops }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn5 add = 1 }
		}
	}
}
# fish, village side.
# Scope: location
bag_wtp_plan_try_rural_6 = {
	if = {
		limit = { bag_wtp_plan_can_rural_6 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm6 = 108
				var:bag_wtp_prm6 = 109
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:fish }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:fishing_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn6 add = 1 }
		}
	}
}
# fruit, village side.
# Scope: location
bag_wtp_plan_try_rural_7 = {
	if = {
		limit = { bag_wtp_plan_can_rural_7 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm7 = 110
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:fruit }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:fruit_orchard }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn7 add = 1 }
		}
	}
}
# horses, village side.
# Scope: location
bag_wtp_plan_try_rural_8 = {
	if = {
		limit = { bag_wtp_plan_can_rural_8 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm8 = 121
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:horses }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:horse_breeders }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn8 add = 1 }
		}
	}
}
# iron, village side.
# Scope: location
bag_wtp_plan_try_rural_10 = {
	if = {
		limit = { bag_wtp_plan_can_rural_10 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm10 = 123
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:iron }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:bog_iron_smelter }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn10 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm10 = 124
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:iron }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn10 add = 1 }
		}
	}
}
# ivory, village side.
# Scope: location
bag_wtp_plan_try_rural_11 = {
	if = {
		limit = { bag_wtp_plan_can_rural_11 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm11 = 125
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:ivory }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:elephant_hunting_grounds }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn11 add = 1 }
		}
	}
}
# livestock, village side.
# Scope: location
bag_wtp_plan_try_rural_12 = {
	if = {
		limit = { bag_wtp_plan_can_rural_12 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm12 = 171
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:livestock }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:farming_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn12 add = 1 }
		}
	}
}
# lumber, village side.
# Scope: location
bag_wtp_plan_try_rural_13 = {
	if = {
		limit = { bag_wtp_plan_can_rural_13 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm13 = 172
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:lumber }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn13 add = 1 }
		}
	}
}
# salt, village side.
# Scope: location
bag_wtp_plan_try_rural_15 = {
	if = {
		limit = { bag_wtp_plan_can_rural_15 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm15 = 199
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:salt }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:salt_collector }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn15 add = 1 }
		}
	}
}
# sand, village side.
# Scope: location
bag_wtp_plan_try_rural_17 = {
	if = {
		limit = { bag_wtp_plan_can_rural_17 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm17 = 204
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:sand }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:sand_pit }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn17 add = 1 }
		}
	}
}
# silver, village side.
# Scope: location
bag_wtp_plan_try_rural_19 = {
	if = {
		limit = { bag_wtp_plan_can_rural_19 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm19 = 207
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:silver }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:schwaz_mine }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn19 add = 1 }
		}
	}
}
# stone, village side.
# Scope: location
bag_wtp_plan_try_rural_20 = {
	if = {
		limit = { bag_wtp_plan_can_rural_20 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm20 = 210
				var:bag_wtp_prm20 = 211
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:stone }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:stone_quarry }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn20 add = 1 }
		}
	}
}
# sugar, village side.
# Scope: location
bag_wtp_plan_try_rural_21 = {
	if = {
		limit = { bag_wtp_plan_can_rural_21 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm21 = 212
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:sugar }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:sugar_plantation }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn21 add = 1 }
		}
	}
}
# tobacco, village side.
# Scope: location
bag_wtp_plan_try_rural_22 = {
	if = {
		limit = { bag_wtp_plan_can_rural_22 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm22 = 214
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:tobacco }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:tobacco_plantation }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn22 add = 1 }
		}
	}
}
# wild_game, village side.
# Scope: location
bag_wtp_plan_try_rural_23 = {
	if = {
		limit = { bag_wtp_plan_can_rural_23 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm23 = 234
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:wild_game }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:eng_royal_forest }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn23 add = 1 }
		}
	}
}
# wool, village side.
# Scope: location
bag_wtp_plan_try_rural_25 = {
	if = {
		limit = { bag_wtp_plan_can_rural_25 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm25 = 241
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:wool }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:sheep_farms }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn25 add = 1 }
		}
	}
}
# beer, village side.
# Scope: location
bag_wtp_plan_try_rural_26 = {
	if = {
		limit = { bag_wtp_plan_can_rural_26 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm26 = 17
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:beer }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:market_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn26 add = 1 }
		}
	}
}
# cloth, village side.
# Scope: location
bag_wtp_plan_try_rural_29 = {
	if = {
		limit = { bag_wtp_plan_can_rural_29 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm29 = 49
				var:bag_wtp_prm29 = 50
				var:bag_wtp_prm29 = 51
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:cloth }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:rural_clothmaker }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn29 add = 1 }
		}
	}
}
# glass, village side.
# Scope: location
bag_wtp_plan_try_rural_33 = {
	if = {
		limit = { bag_wtp_plan_can_rural_33 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm33 = 120
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:glass }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:rural_glassmaker }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn33 add = 1 }
		}
	}
}
# jewelry, village side.
# Scope: location
bag_wtp_plan_try_rural_34 = {
	if = {
		limit = { bag_wtp_plan_can_rural_34 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm34 = 138
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:jewelry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:market_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn34 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm34 = 139
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:jewelry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:pounamou_carver }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn34 add = 1 }
		}
	}
}
# leather, village side.
# Scope: location
bag_wtp_plan_try_rural_36 = {
	if = {
		limit = { bag_wtp_plan_can_rural_36 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm36 = 142
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:leather }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:forest_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn36 add = 1 }
		}
	}
}
# masonry, village side.
# Scope: location
bag_wtp_plan_try_rural_38 = {
	if = {
		limit = { bag_wtp_plan_can_rural_38 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm38 = 173
				var:bag_wtp_prm38 = 174
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:masonry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:mason }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn38 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm38 = 175
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:masonry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:shoen }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn38 add = 1 }
		}
	}
}
# naval_supplies, village side.
# Scope: location
bag_wtp_plan_try_rural_39 = {
	if = {
		limit = { bag_wtp_plan_can_rural_39 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm39 = 178
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:naval_supplies }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:fishing_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn39 add = 1 }
		}
	}
}
# pottery, village side.
# Scope: location
bag_wtp_plan_try_rural_42 = {
	if = {
		limit = { bag_wtp_plan_can_rural_42 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm42 = 193
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:fishing_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm42 = 194
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:pottery }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:market_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn42 add = 1 }
		}
	}
}
# slaves_goods, village side.
# Scope: location
bag_wtp_plan_try_rural_43 = {
	if = {
		limit = { bag_wtp_plan_can_rural_43 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm43 = 208
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:slaves_goods }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:slave_center }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn43 add = 1 }
		}
	}
}
# tar, village side.
# Scope: location
bag_wtp_plan_try_rural_45 = {
	if = {
		limit = { bag_wtp_plan_can_rural_45 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm45 = 213
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:tar }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:tar_kiln }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn45 add = 1 }
		}
	}
}
# tools, village side.
# Scope: location
bag_wtp_plan_try_rural_46 = {
	if = {
		limit = { bag_wtp_plan_can_rural_46 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm46 = 215
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:forest_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm46 = 218
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:market_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm46 = 219
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:tools }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:tatara }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn46 add = 1 }
		}
	}
}
# weaponry, village side.
# Scope: location
bag_wtp_plan_try_rural_47 = {
	if = {
		limit = { bag_wtp_plan_can_rural_47 = yes }
		if = {
			limit = { OR = {
				var:bag_wtp_prm47 = 225
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:forest_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm47 = 226
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:market_village }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
		if = {
			limit = { OR = {
				var:bag_wtp_prm47 = 227
			} }
			province_definition = {
				every_location_in_province_definition = {
					add_to_variable_list = { name = bag_wtp_plan_rural target = goods:weaponry }
					add_to_variable_list = { name = bag_wtp_plan_rural_b target = building_type:tatara }
					change_variable = { name = bag_wtp_plan_rural_n add = 1 }
				}
			}
			change_global_variable = { name = bag_wtp_plan_lists add = 1 }
			change_global_variable = { name = bag_wtp_plan_added add = 1 }
			change_global_variable = { name = bag_wtp_pn47 add = 1 }
		}
	}
}

# Urban rights, before any good touches a town list.
#
# **A right is chosen per province, not by rights taking turns.** There are
# 12 of them and rarely that many provinces in a plan, so an order over rights
# would decide the whole outcome by its own arbitrariness. Asked the other way
# round -- which right does this ground suit best -- every province gets the
# answer that is true about it, and the rights that go unused are the ones no
# ground here wanted.
#
# One location per province, from `_plan_prov_locs`, and only where the province
# has somewhere built-up to put a right at all.
# Scope: country
bag_wtp_plan_place_rights = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		limit = {
			bag_wtp_plan_is_town = yes
			var:bag_wtp_plan_town_n = 0
		}
		set_variable = { name = bag_wtp_rbest value = 0 }
		set_variable = { name = bag_wtp_rbest_k value = 0 }
		# constantinopolitan_silk_monopoly_rights: cloth, fine_cloth, silk
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq1 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_1 = yes
				scope:bag_wtp_country = { AND = { OR = { has_or_had_tag = BYZ has_or_had_tag = ROM } } }
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 1 }
		}
		# royal_artisan_rights: furniture, leather, pottery
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq2 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_2 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 2 }
		}
		# royal_book_rights: books, dyes, paper
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq3 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_3 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 3 }
		}
		# royal_brewing_rights: beer, liquor, wine
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq4 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_4 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 4 }
		}
		# royal_jewelry_rights: jewelry
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq5 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_5 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 5 }
		}
		# royal_masonry_rights: glass, masonry
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq6 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_6 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 6 }
		}
		# royal_naval_rights: naval_supplies, tar
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq7 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_7 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 7 }
		}
		# royal_textile_rights: cloth, dyes, fine_cloth
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq8 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_8 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 8 }
		}
		# royal_tooling_rights: tools
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq9 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_9 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 9 }
		}
		# royal_weaponry_rights: cannons, firearms, weaponry
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq10 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_10 = yes
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 10 }
		}
		# scandinavian_bergslag_privileges: tools
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq11 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_11 = yes
				scope:bag_wtp_country = { AND = { culture = { has_culture_group = culture_group:scandinavian_group } } }
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 11 }
		}
		# scandinavian_tar_privileges: naval_supplies, tar
		set_variable = { name = bag_wtp_rtry value = bag_wtp_rq12 }
		if = {
			limit = {
				var:bag_wtp_rtry > var:bag_wtp_rbest
				bag_wtp_plan_right_fits_12 = yes
				scope:bag_wtp_country = { AND = { culture = { has_culture_group = culture_group:scandinavian_group } } }
			}
			set_variable = { name = bag_wtp_rbest value = var:bag_wtp_rtry }
			set_variable = { name = bag_wtp_rbest_k value = 12 }
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 1 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_29 = yes
			bag_wtp_plan_try_town_30 = yes
			bag_wtp_plan_try_town_18 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 1 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 2 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_32 = yes
			bag_wtp_plan_try_town_36 = yes
			bag_wtp_plan_try_town_42 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 2 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 3 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_27 = yes
			bag_wtp_plan_try_town_4 = yes
			bag_wtp_plan_try_town_40 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 3 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 4 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_26 = yes
			bag_wtp_plan_try_town_37 = yes
			bag_wtp_plan_try_town_24 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 4 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 5 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_34 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 5 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 6 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_33 = yes
			bag_wtp_plan_try_town_38 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 6 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 7 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_39 = yes
			bag_wtp_plan_try_town_45 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 7 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 8 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_29 = yes
			bag_wtp_plan_try_town_4 = yes
			bag_wtp_plan_try_town_30 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 8 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 9 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_46 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 9 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 10 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_28 = yes
			bag_wtp_plan_try_town_31 = yes
			bag_wtp_plan_try_town_47 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 10 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 11 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_46 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 11 }
				}
			}
		}
		if = {
			limit = { var:bag_wtp_rbest_k = 12 }
			# Through the same guarded effects a good goes through, so a bundle
			# whose second good wants a building the first already took gets what
			# is left rather than an impossible list.
			bag_wtp_plan_try_town_39 = yes
			bag_wtp_plan_try_town_45 = yes
			province_definition = {
				every_location_in_province_definition = {
					set_variable = { name = bag_wtp_plan_right value = 12 }
				}
			}
		}
	}
}

# The rounds. Every good takes one town list and one village list, then every
# good takes another, until a whole sweep adds nothing anywhere.
#
# **It runs until the ground is full rather than for a fixed number of rounds.**
# The first build stopped after «зданий на товар» rounds and left a third of
# Wallachia empty at three; a location the plan can feed is never left empty,
# because what a displaced building leaves behind is exactly what should cascade
# into the poorer ground. The sweep counter is a guard against a condition that
# cannot be left, not a design: 12 sweeps is far past the point where the
# lists are full.
# Scope: country
bag_wtp_plan_allocate = {
	set_global_variable = { name = bag_wtp_plan_go value = 1 }
	set_global_variable = { name = bag_wtp_plan_sweeps value = 0 }
	while = {
		limit = {
			global_var:bag_wtp_plan_go = 1
			global_var:bag_wtp_plan_sweeps < 12
		}
		change_global_variable = { name = bag_wtp_plan_sweeps add = 1 }
		set_global_variable = { name = bag_wtp_plan_added value = 0 }
		bag_wtp_plan_pick_1 = yes
		bag_wtp_plan_pick_2 = yes
		bag_wtp_plan_pick_3 = yes
		bag_wtp_plan_pick_4 = yes
		bag_wtp_plan_pick_5 = yes
		bag_wtp_plan_pick_6 = yes
		bag_wtp_plan_pick_7 = yes
		bag_wtp_plan_pick_8 = yes
		bag_wtp_plan_pick_9 = yes
		bag_wtp_plan_pick_10 = yes
		bag_wtp_plan_pick_11 = yes
		bag_wtp_plan_pick_12 = yes
		bag_wtp_plan_pick_13 = yes
		bag_wtp_plan_pick_14 = yes
		bag_wtp_plan_pick_15 = yes
		bag_wtp_plan_pick_16 = yes
		bag_wtp_plan_pick_17 = yes
		bag_wtp_plan_pick_18 = yes
		bag_wtp_plan_pick_19 = yes
		bag_wtp_plan_pick_20 = yes
		bag_wtp_plan_pick_21 = yes
		bag_wtp_plan_pick_22 = yes
		bag_wtp_plan_pick_23 = yes
		bag_wtp_plan_pick_24 = yes
		bag_wtp_plan_pick_25 = yes
		bag_wtp_plan_pick_26 = yes
		bag_wtp_plan_pick_27 = yes
		bag_wtp_plan_pick_28 = yes
		bag_wtp_plan_pick_29 = yes
		bag_wtp_plan_pick_30 = yes
		bag_wtp_plan_pick_31 = yes
		bag_wtp_plan_pick_32 = yes
		bag_wtp_plan_pick_33 = yes
		bag_wtp_plan_pick_34 = yes
		bag_wtp_plan_pick_35 = yes
		bag_wtp_plan_pick_36 = yes
		bag_wtp_plan_pick_37 = yes
		bag_wtp_plan_pick_38 = yes
		bag_wtp_plan_pick_39 = yes
		bag_wtp_plan_pick_40 = yes
		bag_wtp_plan_pick_41 = yes
		bag_wtp_plan_pick_42 = yes
		bag_wtp_plan_pick_43 = yes
		bag_wtp_plan_pick_44 = yes
		bag_wtp_plan_pick_45 = yes
		bag_wtp_plan_pick_46 = yes
		bag_wtp_plan_pick_47 = yes
		if = {
			limit = { global_var:bag_wtp_plan_added = 0 }
			set_global_variable = { name = bag_wtp_plan_go value = 0 }
		}
	}
}

# clay joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_1 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn1 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_1 = yes }
			order_by = bag_wtp_ord1
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_1 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn1 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_1 = yes }
			order_by = bag_wtp_ordr1
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_1 = yes
		}
	}
}

# coal joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_2 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn2 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_2 = yes }
			order_by = bag_wtp_ord2
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_2 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn2 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_2 = yes }
			order_by = bag_wtp_ordr2
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_2 = yes
		}
	}
}

# cotton joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_3 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn3 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_3 = yes }
			order_by = bag_wtp_ord3
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_3 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn3 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_3 = yes }
			order_by = bag_wtp_ordr3
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_3 = yes
		}
	}
}

# dyes joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_4 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn4 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_4 = yes }
			order_by = bag_wtp_ord4
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_4 = yes
		}
	}
}

# fiber_crops joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_5 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn5 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_5 = yes }
			order_by = bag_wtp_ord5
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_5 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn5 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_5 = yes }
			order_by = bag_wtp_ordr5
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_5 = yes
		}
	}
}

# fish joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_6 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn6 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_6 = yes }
			order_by = bag_wtp_ordr6
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_6 = yes
		}
	}
}

# fruit joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_7 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn7 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_7 = yes }
			order_by = bag_wtp_ord7
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_7 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn7 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_7 = yes }
			order_by = bag_wtp_ordr7
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_7 = yes
		}
	}
}

# horses joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_8 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn8 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_8 = yes }
			order_by = bag_wtp_ord8
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_8 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn8 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_8 = yes }
			order_by = bag_wtp_ordr8
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_8 = yes
		}
	}
}

# incense joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_9 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn9 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_9 = yes }
			order_by = bag_wtp_ord9
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_9 = yes
		}
	}
}

# iron joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_10 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn10 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_10 = yes }
			order_by = bag_wtp_ord10
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_10 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn10 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_10 = yes }
			order_by = bag_wtp_ordr10
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_10 = yes
		}
	}
}

# ivory joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_11 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn11 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_11 = yes }
			order_by = bag_wtp_ordr11
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_11 = yes
		}
	}
}

# livestock joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_12 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn12 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_12 = yes }
			order_by = bag_wtp_ordr12
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_12 = yes
		}
	}
}

# lumber joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_13 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn13 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_13 = yes }
			order_by = bag_wtp_ord13
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_13 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn13 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_13 = yes }
			order_by = bag_wtp_ordr13
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_13 = yes
		}
	}
}

# medicaments joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_14 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn14 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_14 = yes }
			order_by = bag_wtp_ord14
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_14 = yes
		}
	}
}

# salt joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_15 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn15 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_15 = yes }
			order_by = bag_wtp_ord15
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_15 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn15 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_15 = yes }
			order_by = bag_wtp_ordr15
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_15 = yes
		}
	}
}

# saltpeter joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_16 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn16 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_16 = yes }
			order_by = bag_wtp_ord16
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_16 = yes
		}
	}
}

# sand joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_17 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn17 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_17 = yes }
			order_by = bag_wtp_ord17
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_17 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn17 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_17 = yes }
			order_by = bag_wtp_ordr17
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_17 = yes
		}
	}
}

# silk joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_18 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn18 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_18 = yes }
			order_by = bag_wtp_ord18
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_18 = yes
		}
	}
}

# silver joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_19 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn19 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_19 = yes }
			order_by = bag_wtp_ord19
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_19 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn19 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_19 = yes }
			order_by = bag_wtp_ordr19
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_19 = yes
		}
	}
}

# stone joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_20 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn20 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_20 = yes }
			order_by = bag_wtp_ord20
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_20 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn20 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_20 = yes }
			order_by = bag_wtp_ordr20
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_20 = yes
		}
	}
}

# sugar joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_21 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn21 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_21 = yes }
			order_by = bag_wtp_ord21
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_21 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn21 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_21 = yes }
			order_by = bag_wtp_ordr21
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_21 = yes
		}
	}
}

# tobacco joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_22 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn22 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_22 = yes }
			order_by = bag_wtp_ord22
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_22 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn22 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_22 = yes }
			order_by = bag_wtp_ordr22
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_22 = yes
		}
	}
}

# wild_game joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_23 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn23 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_23 = yes }
			order_by = bag_wtp_ordr23
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_23 = yes
		}
	}
}

# wine joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_24 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn24 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_24 = yes }
			order_by = bag_wtp_ord24
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_24 = yes
		}
	}
}

# wool joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_25 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn25 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_25 = yes }
			order_by = bag_wtp_ordr25
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_25 = yes
		}
	}
}

# beer joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_26 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn26 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_26 = yes }
			order_by = bag_wtp_ord26
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_26 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn26 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_26 = yes }
			order_by = bag_wtp_ordr26
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_26 = yes
		}
	}
}

# books joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_27 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn27 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_27 = yes }
			order_by = bag_wtp_ord27
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_27 = yes
		}
	}
}

# cannons joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_28 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn28 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_28 = yes }
			order_by = bag_wtp_ord28
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_28 = yes
		}
	}
}

# cloth joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_29 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn29 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_29 = yes }
			order_by = bag_wtp_ord29
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_29 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn29 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_29 = yes }
			order_by = bag_wtp_ordr29
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_29 = yes
		}
	}
}

# fine_cloth joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_30 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn30 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_30 = yes }
			order_by = bag_wtp_ord30
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_30 = yes
		}
	}
}

# firearms joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_31 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn31 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_31 = yes }
			order_by = bag_wtp_ord31
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_31 = yes
		}
	}
}

# furniture joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_32 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn32 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_32 = yes }
			order_by = bag_wtp_ord32
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_32 = yes
		}
	}
}

# glass joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_33 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn33 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_33 = yes }
			order_by = bag_wtp_ord33
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_33 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn33 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_33 = yes }
			order_by = bag_wtp_ordr33
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_33 = yes
		}
	}
}

# jewelry joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_34 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn34 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_34 = yes }
			order_by = bag_wtp_ord34
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_34 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn34 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_34 = yes }
			order_by = bag_wtp_ordr34
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_34 = yes
		}
	}
}

# lacquerware joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_35 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn35 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_35 = yes }
			order_by = bag_wtp_ord35
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_35 = yes
		}
	}
}

# leather joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_36 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn36 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_36 = yes }
			order_by = bag_wtp_ord36
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_36 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn36 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_36 = yes }
			order_by = bag_wtp_ordr36
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_36 = yes
		}
	}
}

# liquor joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_37 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn37 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_37 = yes }
			order_by = bag_wtp_ord37
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_37 = yes
		}
	}
}

# masonry joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_38 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn38 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_38 = yes }
			order_by = bag_wtp_ord38
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_38 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn38 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_38 = yes }
			order_by = bag_wtp_ordr38
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_38 = yes
		}
	}
}

# naval_supplies joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_39 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn39 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_39 = yes }
			order_by = bag_wtp_ord39
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_39 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn39 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_39 = yes }
			order_by = bag_wtp_ordr39
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_39 = yes
		}
	}
}

# paper joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_40 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn40 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_40 = yes }
			order_by = bag_wtp_ord40
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_40 = yes
		}
	}
}

# porcelain joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_41 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn41 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_41 = yes }
			order_by = bag_wtp_ord41
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_41 = yes
		}
	}
}

# pottery joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_42 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn42 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_42 = yes }
			order_by = bag_wtp_ord42
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_42 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn42 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_42 = yes }
			order_by = bag_wtp_ordr42
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_42 = yes
		}
	}
}

# slaves_goods joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_43 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn43 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_43 = yes }
			order_by = bag_wtp_ord43
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_43 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn43 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_43 = yes }
			order_by = bag_wtp_ordr43
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_43 = yes
		}
	}
}

# steel joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_44 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn44 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_44 = yes }
			order_by = bag_wtp_ord44
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_44 = yes
		}
	}
}

# tar joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_45 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn45 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_45 = yes }
			order_by = bag_wtp_ord45
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_45 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn45 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_45 = yes }
			order_by = bag_wtp_ordr45
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_45 = yes
		}
	}
}

# tools joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_46 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn46 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_46 = yes }
			order_by = bag_wtp_ord46
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_46 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn46 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_46 = yes }
			order_by = bag_wtp_ordr46
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_46 = yes
		}
	}
}

# weaponry joins the town list and the village list of the province each side of it
# suits best, or neither.
#
# `max = 1` on an ordered walk is the engine doing the choosing, which is the
# only reason a sweep over 47 goods twice is affordable. Every condition is a
# plain read off the candidate the walk is holding -- the province's counters and
# lists are mirrored onto all of its locations for exactly this reason.
# Scope: country
bag_wtp_plan_pick_47 = {
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn47 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_town_47 = yes }
			order_by = bag_wtp_ord47
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_town_47 = yes
		}
	}
	if = {
		limit = {
			OR = {
				global_var:bag_wtp_plan_max = 0
				global_var:bag_wtp_pn47 < global_var:bag_wtp_plan_max
			}
		}
		ordered_in_global_list = {
			variable = bag_wtp_candidates
			limit = { bag_wtp_plan_can_rural_47 = yes }
			order_by = bag_wtp_ordr47
			max = 1
			check_range_bounds = no
			bag_wtp_plan_try_rural_47 = yes
		}
	}
}

# The province's answer, spent on its locations.
#
# Every location copies its own side's list -- which is already on it -- into the
# one list the window repeats over. That is what makes a province read as one
# answer and leaves nothing the ground can feed standing empty.
#
# Two locations of one province are not always able to build the same thing:
# terrain and a building's own requirements can rule one out. That is not asked
# here, and it is the next thing this pass owes.
# Scope: country
bag_wtp_plan_build = {
	every_in_global_list = {
		variable = bag_wtp_candidates
		save_scope_as = bag_wtp_loc
		clear_variable_list = bag_wtp_plan_goods
		clear_variable_list = bag_wtp_plan_builds
		set_variable = { name = bag_wtp_load value = 0 }
		if = {
			limit = { bag_wtp_plan_is_town = yes }
			# The row is a town's, and its urban right means something. A village
			# row carries the province's right variable too -- everything is
			# mirrored -- and printing it there said a right applies to a village.
			set_variable = { name = bag_wtp_plan_town_row value = 1 }
			every_in_list = {
				variable = bag_wtp_plan_town
				save_scope_as = bag_wtp_good
				scope:bag_wtp_loc = {
					add_to_variable_list = { name = bag_wtp_plan_goods target = scope:bag_wtp_good }
					change_variable = { name = bag_wtp_load add = 1 }
				}
			}
			# The buildings, in the same order and therefore in step with them: a
			# datamodel item sees one scope, so the row draws two lists and lets
			# the fixed cell width line them up.
			every_in_list = {
				variable = bag_wtp_plan_town_b
				save_scope_as = bag_wtp_build
				scope:bag_wtp_loc = {
					add_to_variable_list = { name = bag_wtp_plan_builds target = scope:bag_wtp_build }
				}
			}
		}
		else = {
			remove_variable = bag_wtp_plan_town_row
			every_in_list = {
				variable = bag_wtp_plan_rural
				save_scope_as = bag_wtp_good
				scope:bag_wtp_loc = {
					add_to_variable_list = { name = bag_wtp_plan_goods target = scope:bag_wtp_good }
					change_variable = { name = bag_wtp_load add = 1 }
				}
			}
			# The buildings, in the same order and therefore in step with them: a
			# datamodel item sees one scope, so the row draws two lists and lets
			# the fixed cell width line them up.
			every_in_list = {
				variable = bag_wtp_plan_rural_b
				save_scope_as = bag_wtp_build
				scope:bag_wtp_loc = {
					add_to_variable_list = { name = bag_wtp_plan_builds target = scope:bag_wtp_build }
				}
			}
		}
		change_global_variable = { name = bag_wtp_plan_placed add = var:bag_wtp_load }
	}
}

# The rows: one per location that got anything, its province's locations together.
#
# **Provinces are ranked first and their locations follow.** Under the province
# model most locations of one province carry the same list, and a sort on the
# location's own load would tie nearly all of them -- `ordered_in_global_list`
# promises nothing about ties, so two provinces would interleave. The provinces
# are put in order by how much the plan put in each, every location is told its
# province's place, and the rows sort on that with a province's towns ahead of
# its villages.
#
# A province's load is added up through a global, because a walk over its
# locations cannot write back to the scope that started it.
# Scope: country
bag_wtp_plan_rank = {
	every_in_global_list = {
		variable = bag_wtp_plan_prov_locs
		set_global_variable = { name = bag_wtp_plan_count value = 0 }
		province_definition = {
			every_location_in_province_definition = {
				change_global_variable = { name = bag_wtp_plan_count add = var:bag_wtp_load }
			}
		}
		set_variable = { name = bag_wtp_plan_prov_load value = global_var:bag_wtp_plan_count }
	}
	set_global_variable = { name = bag_wtp_plan_prov_n value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_plan_prov_locs
		order_by = bag_wtp_plan_prov_order
		max = 400
		check_range_bounds = no
		change_global_variable = { name = bag_wtp_plan_prov_n add = 1 }
		province_definition = {
			every_location_in_province_definition = {
				set_variable = { name = bag_wtp_plan_prank value = global_var:bag_wtp_plan_prov_n }
			}
		}
	}
	# Counted before the rows are taken, and separately from them: the walk below
	# stops at 150 and a count that stopped with it would say the plan used
	# exactly as many locations as the window can draw, whatever it really used.
	set_global_variable = { name = bag_wtp_plan_found value = 0 }
	every_in_global_list = {
		variable = bag_wtp_candidates
		limit = { var:bag_wtp_load > 0 }
		change_global_variable = { name = bag_wtp_plan_found add = 1 }
	}
	set_global_variable = { name = bag_wtp_plan_shown value = 0 }
	ordered_in_global_list = {
		variable = bag_wtp_candidates
		limit = { var:bag_wtp_load > 0 }
		order_by = bag_wtp_plan_order
		max = 150
		check_range_bounds = no
		change_global_variable = { name = bag_wtp_plan_shown add = 1 }
		set_variable = { name = bag_wtp_plan_rank value = global_var:bag_wtp_plan_shown }
		add_to_global_variable_list = { name = bag_wtp_plan_ranked target = this }
	}
}

# The window's own list, filled on opening and emptied on closing -- the same
# contract as the other two windows and for the same reason: a scripted widget
# never comes down, so emptying the datamodel is the only thing that frees a row.
# Scope: country
bag_wtp_plan_show = {
	clear_global_variable_list = bag_wtp_plan_results
	ordered_in_global_list = {
		variable = bag_wtp_plan_ranked
		order_by = bag_wtp_plan_rank_order
		max = bag_wtp_show_plan_shown
		check_range_bounds = no
		add_to_global_variable_list = { name = bag_wtp_plan_results target = this }
	}
}

# Scope: country
bag_wtp_plan_hide = {
	clear_global_variable_list = bag_wtp_plan_results
}

# The same thing the window's own scripted GUI does, reachable from an effect,
# because a Mod Menu button is script and script cannot reach the interface's
# variable system. The other two windows are closed on the way in: three tables
# of three different questions, and only one answers the button just pressed.
# Scope: country
bag_wtp_open_plan_window_effect = {
	bag_wtp_plan_show = yes
	remove_variable = bag_wtp_result_open
	remove_variable = bag_wtp_right_open
	bag_wtp_hide_results = yes
	set_variable = { name = bag_wtp_plan_open value = 1 }
}
