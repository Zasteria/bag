# Числа, которые печатают окно и диагностика.
#
# **Каждое из них -- чтение глобальной переменной, и ни одно не считает.**
# Значение, читающее глобалку, которой нет, -- это пустое место на экране и
# строка в `error.log` каждый кадр, поэтому все они рождаются в
# `bag_wtp2_init_counters`, до первого чтения.

bag_wtp2_show_picked = { value = global_var:bag_wtp2_picked_count }
bag_wtp2_show_zone = { value = global_var:bag_wtp2_zone_count }
bag_wtp2_show_region = { value = global_var:bag_wtp2_region_count }
bag_wtp2_show_diag_runs = { value = global_var:bag_wtp2_diag_runs }

# Проба дороги: не читает ничего и обязана напечататься как 12345.
bag_wtp2_dg0 = { value = 12345 }

bag_wtp2_dg1 = { value = global_var:bag_wtp2_zone_count }
bag_wtp2_dg2 = { value = global_var:bag_wtp2_region_count }
bag_wtp2_dg3 = { value = global_var:bag_wtp2_picked_count }
bag_wtp2_dg4 = { value = global_var:bag_wtp2_diag_runs }
