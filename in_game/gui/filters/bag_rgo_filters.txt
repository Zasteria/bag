# Both panels that list buildings ask for the `building` filter tag, so both
# filters below are offered in both. Each is only meaningful in its own panel:
# the building_type one narrows a location's building list, the location one
# narrows the list of places to put a chosen building.
# Localization lives under search_filter_<key>_name / _desc.

bag_rgo_local_bonus = {
	scope = building_type
	tag = building
	group = 9
	exclusive_group = no
	trigger = {
		bag_rgo_has_local_bonus = yes
	}
}

bag_rgo_build_local_bonus = {
	scope = location
	tag = building
	group = 9
	exclusive_group = no
	trigger = {
		bag_rgo_location_suits_build_type = yes
	}
}
