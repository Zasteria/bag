KBU_f = {

	level = 3
	required_locations_fraction = 0.55
	rule = historical

	potential = {
		culture = culture:kanembu_culture
		religion = religion:sunni
		# No empire is proclaimed mid-collapse (2026-08-03 blast-radius
		# audit, user ruling): forming during the Sayfawa dynasty crisis
		# (a KBO-keyed vanilla disaster) would tag-switch its arc dead.
		# The crisis spawns fast on any weak Sayfawa court, so this gate
		# in practice means the crisis runs before the empire rises; a
		# KBO that never weakened has no crisis to skip.
		has_any_active_disaster = no
	}

	allow = {
		owns = location:ngazargamu
		owns = location:njimi
	}

	name = KBU
	flag = KBU
	adjective = KBU_ADJ
	tag = KBU
	color = map_KBU

	areas = {
		bornu_area
		kanem_area
		logone_area
		wadai_area
		air_area
		north_hausa_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_kbu_formed

		}
		trigger_event_non_silently = nd_kbu.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
	}
}
