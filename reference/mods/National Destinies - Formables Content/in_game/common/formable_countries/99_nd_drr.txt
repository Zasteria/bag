DRR_f = {

	level = 3
	required_locations_fraction = 0.5
	rule = historical

	potential = {
		culture = culture:afghan_culture
		religion = religion:sunni
	}

	allow = {
		owns = location:kandahar
		owns = location:kabul
	}

	name = DRR
	flag = DRR
	adjective = DRR_ADJ
	tag = DRR
	color = map_DRR

	regions = {
	}
	areas = {
		sistan_area
		eastern_khorasan_area
		western_khorasan_area
		baluchistan_area
		kerman_area
		quhestan_area
		upper_indus_area
		punjab_area
		sindh_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_drr_formed

		}
		trigger_event_non_silently = nd_drr.1
		add_stability = stability_mild_bonus
		if = {
			limit = {
				country_rank_level < 4
			}
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		set_capital = location:kandahar
	}
}
