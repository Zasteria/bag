MRO_f = {

	level = 2
	required_locations_fraction = 0.8
	rule = plausible

	potential = {
		religion = religion:sunni
		OR = {
			culture = culture:tausug_culture
			culture = culture:maguindanao_culture
			culture = culture:iranun_culture
			culture = culture:tagakaulo_culture
			culture = culture:butuanon_culture
		}
	}

	allow = {
		owns = location:maimbung
		owns = location:kuta_watu
		# OR-of-pairs, not calc_true_if: defense in depth after the
		# Danube 1413 spark bug (2026-08-02). Repeated SCALAR entries
		# (owns x4) demonstrably count, so the old form worked; repeated
		# BLOCK entries (AND x3) evidently do not, and OR-of-pairs is
		# immune to the distinction entirely.
		OR = {
			AND = {
				owns = location:butuan
				owns = location:malabang
			}
			AND = {
				owns = location:butuan
				owns = location:buayan
			}
			AND = {
				owns = location:butuan
				owns = location:samboangan
			}
			AND = {
				owns = location:malabang
				owns = location:buayan
			}
			AND = {
				owns = location:malabang
				owns = location:samboangan
			}
			AND = {
				owns = location:buayan
				owns = location:samboangan
			}
		}
		in_civil_war = no
	}

	name = MRO
	flag = MRO
	adjective = MRO_ADJ
	tag = MRO
	color = map_MRO

	areas = {
		mindanao_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_mro_formed

		}
		trigger_event_non_silently = nd_mro.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
	}
}
