NUM_f = {

	level = 3
	required_locations_fraction = 0.45
	rule = plausible

	potential = {
		culture = { has_culture_group = culture_group:shoshoni_group }
	}

	allow = {
		owns = location:kwahada
		owns = location:penatuka
		in_civil_war = no
	}

	name = NUM
	flag = NUM
	adjective = NUM_ADJ
	tag = NUM
	color = map_NUM

	areas = {
		texas_area
		natahende_area
		high_plains_area
		central_plains_area
		rockies_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_num_formed

		}
		trigger_event_non_silently = nd_num.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		set_capital = location:kwahada
		# Numunu Sookobitu paraibo confederation: multi-band federation
		# under paraibos (peace chiefs) and tekwuniwapi (war chiefs)
		# across the five major divisions. The Council of the Comanche
		# Nation at Council Hill governs by inter-band assembly; no
		# unitary kingdom. Horde-style cohesion via kinship + bison
		# economy.
		add_country_modifier = {
			modifier = nd_num_paraibo_confederation
			years = -1
		}
	}
}
