NIN_f = {

	level = 4
	required_locations_fraction = 0.6
	rule = plausible

	potential = {
		OR = {
			religion = religion:miaphysite
			religion = religion:nestorianism
		}
		OR = {
			has_or_had_tag = BNH
			country_rank = country_rank:rank_kingdom
			country_rank = country_rank:rank_empire
		}
	}

	allow = {
		owns = location:mosul
		owns = location:baghdad
		owns = location:damascus
		owns = location:cairo
		OR = {
			owns = location:shush
			owns = location:tarsus
		}
		in_civil_war = no
	}

	name = NIN
	flag = NIN
	adjective = NIN_ADJ
	tag = NIN
	color = map_NIN

	regions = {
		crescent_region
		egypt_region
	}
	areas = {
		khuzestan_area
		cilicia_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_nin_formed

		}
		set_capital = location:mosul
		change_culture = culture:syriac_culture
		every_owned_location = {
			every_pop = {
				limit = {
					OR = {
						culture = culture:armenian_culture
						culture = culture:iraqi_culture
						culture = culture:kurdish_culture
						culture = culture:levantine_culture
						culture = culture:turkish_culture
						culture = culture:farsi_culture
						culture = culture:coptic_culture
						culture = culture:lower_egyptian_culture
						culture = culture:upper_egyptian_culture
					}
				}
				change_pop_culture = culture:syriac_culture
			}
		}
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		add_stability = stability_severe_bonus
		trigger_event_non_silently = nd_nin.1
	}
}
