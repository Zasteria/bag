BNH_f = {

	level = 2
	required_locations_fraction = 0.4
	rule = plausible

	potential = {
		OR = {
			religion = religion:miaphysite
			religion = religion:nestorianism
		}
	}

	allow = {
		owns = location:mosul
		owns = location:nusaybin
		owns = location:urfa
	}

	name = BNH
	flag = BNH
	adjective = BNH_ADJ
	tag = BNH
	color = map_BNH

	areas = {
		jazira_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_bnh_formed

		}
		set_capital = location:mosul
		change_culture = culture:syriac_culture
		every_owned_location = {
			every_pop = {
				limit = {
					OR = {
						culture = culture:armenian_culture
						culture = culture:iraqi_culture
						culture = culture:kurdish_culture
					}
				}
				change_pop_culture = culture:syriac_culture
			}
		}
		trigger_event_non_silently = nd_bnh.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 2 }
			set_country_rank_effect = { rank = country_rank:rank_duchy }
		}
	}
}
