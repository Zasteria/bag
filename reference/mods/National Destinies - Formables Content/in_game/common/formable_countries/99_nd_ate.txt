ATE_f = {

	level = 2
	required_locations_fraction = 0.9
	rule = historical

	potential = {
		culture = culture:akan
	}

	allow = {
		owns = location:kumasi
	}

	name = ATE
	flag = ATE
	adjective = ATE_ADJ
	tag = ATE
	color = map_ATE

	areas = {
		akan_area
		gold_coast_area
	}
	form_effect = {
		# Formation markers live HERE, not only in the event's options.
		# A country can be formed by script (vanilla forms JAP from the
		# sengoku situation), and then the popup may never be answered,
		# leaving every advance gated on this variable unreachable.
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_ate_formed
		}
		if = {
			limit = {
				country_rank_level < 3
			}
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		trigger_event_non_silently = nd_ate.1
		add_stability = stability_mild_bonus
		# Asantemanhyiamu (Ashanti Council of Chiefs): the Asantehene is
		# elected and can be destooled by the Kotoko Council. Constituent
		# Akan states retain real autonomy under the Golden Stool's ritual
		# unity. Decentralized confederation, not a unitary monarchy.
		add_country_modifier = {
			modifier = nd_ate_kotoko_council
			years = -1
		}
	}
}
