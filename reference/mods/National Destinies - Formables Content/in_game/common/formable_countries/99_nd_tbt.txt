TBT_f = {

	level = 3
	required_locations_fraction = 0.7
	rule = plausible

	potential = {
		culture = { has_culture_group = culture_group:tibetan_group }
		religion.group = religion_group:buddhist
	}

	allow = {
		owns = location:lhasa
		owns = location:shigatse
		# The Yarlung empire's defining feature was its reach beyond the
		# plateau into Tang frontier territory. The full imperial
		# achievement requires recovering BOTH frontier zones in their
		# entirety: the Anxi Four Garrisons (Tibetan-held 670-790) and
		# the historical Hexi Corridor occupation (Tibetan-held 781-848).
		# This is an Empire-tier formable; if you want the Tsenpo's
		# pillar, you take all the territory the historical pillar
		# claimed.
		# Tarim Basin / Anxi Four Garrisons (all four)
		owns = location:khotan
		owns = location:kashgar
		owns = location:kucha
		owns = location:yarkand
		# Hexi Corridor (Tang prefectural names, full historical Tibetan zone)
		owns = location:shazhou
		owns = location:ganzhou
		owns = location:suzhou_gansu
	}

	name = TBT
	flag = TBT
	adjective = TBT_ADJ
	tag = TBT
	color = map_TBT

	regions = {
		tibet_region
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_tbt_formed

		}
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		if = {
			limit = { NOT = { capital = location:lhasa } }
			set_capital = location:lhasa
		}
		trigger_event_non_silently = nd_tbt.1
		add_stability = stability_severe_bonus
	}
}
