# Crown of Aragon (Corona de Aragón): National Destinies new formable
# The Catalan-Aragonese Mediterranean thalassocracy (1162-1716): a multi-kingdom
# personal union of Aragon, Catalonia, Valencia, the Balearics, Sardinia, Sicily
# and Naples, distinct from the Castilian-led Iberian unification (SPA) and the
# Italian-anchored Bourbon Naples-Sicily union (TWS). Required: catalan or
# aragonese culture + Catholic religion. Allow: own the three royal seats
# (Barcelona, Naples, Palermo). Form effect raises rank to Empire (Alfonso V the
# Magnanimous styled himself Rex utriusque Siciliae and held imperial-tier
# multi-kingdom authority).
CDA_f = {

	level = 3
	required_locations_fraction = 0.65
	rule = plausible

	potential = {
		religion = religion:catholic
		OR = {
			culture = culture:catalan
			culture = culture:aragonese
		}
		NOT = { tag = SPA }
		NOT = { tag = TWS }
		# No empire is proclaimed mid-civil-war (2026-08-03 blast-radius
		# audit, user ruling): Aragon carries the War of the Aragonese
		# Union disaster; a tag switch during it strands its event
		# chains mid-arc.
		has_any_active_disaster = no
	}

	allow = {
		owns = location:barcelona
		owns = location:naples
		owns = location:palermo
	}

	name = CDA
	flag = CDA
	adjective = CDA_ADJ
	tag = CDA
	color = map_CDA

	areas = {
		aragon_area
		catalonia_area
		valencia_area
		balearics_area
		sardinia_area
		sicily_area
		campania_area
		apulia_area
		basilicata_area
		abruzzo_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_cda_formed

		}
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		trigger_event_non_silently = nd_cda.1
		add_stability = stability_mild_bonus
	}
}
