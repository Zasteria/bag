NZO_f = {

	level = 3
	required_locations_fraction = 0.6
	rule = plausible

	potential = {
		OR = {
			culture = culture:bai_culture
			culture = culture:yi_culture
		}
	}

	allow = {
		owns = location:taihe_dali
	}

	name = NZO
	flag = NZO
	adjective = NZO_ADJ
	tag = NZO
	color = map_NZO

	areas = {
		dali_area
		yunnan_area
		guizhou_area
		chuannan_area
		kachin_area
		northern_tai_highland_area
	}
	form_effect = {
		# Formation markers live HERE, not only in the event's options.
		# A country can be formed by script (vanilla forms JAP from the
		# sengoku situation), and then the popup may never be answered,
		# leaving every advance gated on this variable unreachable.
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_nzo_formed
		}
		set_capital = location:taihe_dali
		trigger_event_non_silently = nd_nzo.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
	}
}
