PNT_f = {

	level = 2
	required_locations_fraction = 0.85
	rule = plausible

	potential = {
		OR = {
			culture = culture:hiligaynon_culture
			culture = culture:cebuano_culture
			culture = culture:waray_culture
		}
	}

	allow = {
		owns = location:araut
		owns = location:singhapala
		# Breadth requirement: at least two of the four secondary anchor locations
		# (Samar, Bohol, Negros, Masbate) to ensure pan-Visayan unification rather
		# than a Panay+Cebu duopoly. OR-of-pairs, not calc_true_if:
		# defense in depth after the Danube 1413 spark bug (2026-08-02).
		# Repeated SCALAR entries (owns x4) demonstrably count (the
		# 2026-07-28 Pintados Workshop repro proves it), so the old form
		# worked; repeated BLOCK entries (AND x3) evidently do not, and
		# OR-of-pairs is immune to the distinction entirely.
		OR = {
			AND = {
				owns = location:basey
				owns = location:bool
			}
			AND = {
				owns = location:basey
				owns = location:bago_negros
			}
			AND = {
				owns = location:basey
				owns = location:masbate
			}
			AND = {
				owns = location:bool
				owns = location:bago_negros
			}
			AND = {
				owns = location:bool
				owns = location:masbate
			}
			AND = {
				owns = location:bago_negros
				owns = location:masbate
			}
		}
		in_civil_war = no
	}

	name = PNT
	flag = PNT
	adjective = PNT_ADJ
	tag = PNT
	color = map_PNT

	areas = {
		visayas_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_pnt_formed

		}
		trigger_event_non_silently = nd_pnt.1
		add_stability = stability_mild_bonus
	}
}
