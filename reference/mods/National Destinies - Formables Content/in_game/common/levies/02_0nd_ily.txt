# Read-order file: the engine picks the FIRST valid levy across all levy
# files in name order, so the prefix encodes this file's unlock age exactly
# like vanilla (00_revolutions ... 05_traditions). The extra 0 after the
# prefix sorts us before the vanilla file of the same age. Do NOT merge a
# tag's levies into a single 00_ file: that shadows every newer vanilla levy
# forever.
#
# Illyria (ILY), Age 4 levies.

# Haramija: paid frontier foot, raised 1539 from the soldier and peasant
# population of the Croatian and Slavonian forts. Never nobles: the whole
# point of the haramije was that they were cheap, numerous and on foot,
# unlike the banderia the crown could no longer afford to rely on.
levy_nd_ily_haramija = {
	unit = a_nd_ily_haramija
	size = levy_generic_infantry_size
	country_allow = {
		has_advance = nd_ily_haramije
	}
	allowed_pop_type = soldiers
	allowed_pop_type = peasants
}
