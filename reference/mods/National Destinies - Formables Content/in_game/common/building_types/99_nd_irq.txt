# === Iraq: custom buildings ===
#
# Every building here carries a location_potential, and the comment above each
# one says why that reach is the correct one. Iraq's whole economic argument is
# that a specific kind of ground (irrigable, riverine, marshy, saline) does a
# specific kind of work, so a building that could go anywhere would be saying
# the opposite of what this country is about.

# ============================================================
# The Nahrawan head-works. Canal regulators (qanatir): masonry sluices at the
# offtake that let the engineer decide how much Tigris water enters the canal
# and shut it when the flood comes down. Restricted to river locations in the
# Fertile Crescent because a regulator with no river to regulate is nothing,
# and because the Tigris-Euphrates system plus its Levantine sisters is exactly
# the ground where head-works of this kind were built.
# ============================================================
nd_irq_nahrawan_headworks = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = laborers
	max_levels = 1
	employment_size = generic_peasant_building_employment

	category = infrastructure_category

	rural_settlement = yes
	town = yes
	city = no
	megalopolis = no

	build_time = infrastructure_build_time

	country_potential = {
		has_advance = nd_irq_nahrawan_breach
	}

	location_potential = {
		region = region:crescent_region
		OR = {
			has_river = yes
			is_adjacent_to_lake = yes
		}
	}

	modifier = {
		local_monthly_food_modifier = 0.075
		local_population_capacity = 1.0
		local_food_capacity = 150
	}

	unique_production_methods = {
		nd_irq_headworks_maintenance = {
			stone = 0.2
			clay = 0.15
			tools = 0.1
			lumber = 0.1
			category = building_maintenance
		}
	}

	construction_demand = polders_construction
}

# ============================================================
# The kharaj diwan: the register office of the Sawad, where the surveyors'
# ropes and the tax rolls were kept. Limited to towns and cities of the
# Crescent because the misaha cadastre was a fiscal instrument built for
# canal agriculture on measured ground, and it was administered from the
# settlement, not from the field.
# ============================================================
nd_irq_kharaj_diwan = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = 1
	employment_size = generic_burgher_employment

	category = government_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	build_time = government_build_time

	country_potential = {
		has_advance = nd_irq_sawad_cadastre
	}

	location_potential = {
		region = region:crescent_region
	}

	modifier = {
		local_crown_estate_power = 0.25
		local_max_control = 0.05
		local_burghers_max_literacy = 2.5
	}

	unique_production_methods = {
		nd_irq_diwan_maintenance = {
			paper = 0.4
			books = 0.1
			fine_cloth = 0.1
			goods_gold = 0.1
			category = building_maintenance
		}
	}

	construction_demand = local_governor_construction
}

# ============================================================
# The nakhil: the date-palm grove. Deliberately gated to arid river ground with
# desert or sparse cover, which is precisely the ground vanilla's fruit_orchard
# refuses (it wants grassland, farmland, woods or jungle). The palm is the tree
# that fruits on saline soil in an arid climate provided its roots reach water,
# and the Shatt al-Arab belt is the largest such grove on earth. This building
# therefore fills a real hole rather than shadowing the orchard.
# ============================================================
nd_irq_nakhil_grove = {
	audio_tier = 1
	is_special = yes
	is_foreign = no
	pop_type = laborers
	max_levels = rural_building_cap
	startup_ramp_target = rural_startup_ramp_target
	employment_size = rural_peasant_produce_employment

	category = rgo_building_category

	rural_settlement = yes
	town = yes
	city = yes
	megalopolis = yes

	build_time = rural_build_time

	country_potential = {
		has_advance = nd_irq_nakhil_husbandry
	}

	# The climate, vegetation and water gates describe the palm belt. The
	# market gate is the standard rule for a building that produces a raw
	# good: a grove is planted where dates are already a known crop, not
	# conjured onto ground that has never seen one.
	location_potential = {
		region = region:crescent_region
		climate = arid
		is_produced_in_location_market = goods:fruit
		OR = {
			has_river = yes
			is_adjacent_to_lake = yes
		}
		OR = {
			vegetation = desert
			vegetation = sparse
		}
	}

	unique_production_methods = {
		nd_irq_nakhil_harvest = {
			tools = 0.15
			produced = fruit
			output = 0.5399
			category = guild_input
			debug_max_profit = guild_profit_margin
		}
		nd_irq_nakhil_canal_fed = {
			# unlocked by nd_irq_hindiyya_channel
			tools = 0.25
			lumber = 0.15
			produced = fruit
			output = 1.17
			category = guild_input
			debug_max_profit = guild_profit_margin
		}
	}

	construction_demand = farm_constructions
}

# ============================================================
# The Suq al-Warraqin: the stationers' and booksellers' quarter, a market
# rather than a mill. Vanilla already models papermaking and bookbinding as
# industry; this is the trade in copies that grew on top of it, with copyists
# working to order in the shopfronts. Towns and cities only, and only in the
# Crescent, because a book market needs a reading public within walking
# distance and this one is Baghdad's institution, not a generic library.
# ============================================================
nd_irq_suq_al_warraqin = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = 1
	employment_size = generic_burgher_employment

	category = cultural_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	build_time = cultural_building_time

	AI_ignore_available_worker_flag = yes

	country_potential = {
		has_advance = nd_irq_warraqin_quarter
	}

	location_potential = {
		region = region:crescent_region
	}

	modifier = {
		local_max_literacy = 4
		local_monthly_literacy = 0.1
		local_burghers_max_literacy = 2.5
		local_merchant_power = 0.05
		local_migration_attraction = 0.05
	}

	unique_production_methods = {
		nd_irq_warraqin_copying = {
			paper = 0.5
			dyes = 0.15
			leather = 0.1
			beeswax = 0.05
			category = building_maintenance
		}
		nd_irq_warraqin_press = {
			# unlocked by nd_irq_zawra_press
			paper = 0.5
			dyes = 0.2
			tools = 0.15
			lead = 0.05
			category = building_maintenance
		}
	}

	construction_demand = library_construction
}

# ============================================================
# Al-Madrasa al-Mustansiriyya. One specific building on the left bank of the
# Tigris, commissioned 1227, opened 1234, still standing. It is gated to the
# location of Baghdad and nowhere else, because a copy of it in Aleppo would
# not be it. This is the narrowest reach in the whole file and the reason its
# modifier stack can be generous.
# ============================================================
nd_irq_mustansiriya = {
	audio_tier = 5
	is_special = yes
	is_foreign = no
	pop_type = clergy
	max_levels = 1
	employment_size = large_cultural_employment

	category = cultural_category
	graphical_tags = { large_religious }

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	expensive = yes
	build_time = large_cultural_building_time

	AI_ignore_available_worker_flag = yes

	country_potential = {
		has_advance = nd_irq_mustansiriya_revived
	}

	location_potential = {
		this = location:baghdad
	}

	modifier = {
		local_cultural_influence = 0.35
		local_clergy_max_literacy = 10
		local_max_literacy = 5
		local_monthly_literacy = 0.1
		local_institution_growth_modifier = 0.33
		local_life_expectancy = 5
	}

	unique_production_methods = {
		nd_irq_mustansiriya_maintenance = {
			paper = 0.5
			books = 0.25
			medicaments = 0.1
			glass = 0.1
			category = building_maintenance
		}
	}

	construction_demand = university_construction
}

# ============================================================
# The hadra: the walled shrine precinct with its lodging, kitchens and burial
# ground. Restricted to the four shrine cities of Iraq by name (Ali at Kufa,
# Husayn at Karbala, the two imams at Kazimayn inside Baghdad, the Askari
# shrine at Samarra), because a shrine precinct without the tomb is a wall
# around nothing. Written religion-neutral on purpose: Baghdad also holds Abu
# Hanifa and Abd al-Qadir al-Jilani, and this formable admits Sunni and Shia
# rulers alike.
# ============================================================
nd_irq_atabat_hadra = {
	audio_tier = 4
	is_special = yes
	is_foreign = no
	pop_type = clergy
	max_levels = 1
	employment_size = huge_religious_building_employment

	category = religious_category
	graphical_tags = { large_religious }

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	expensive = yes
	build_time = large_religious_building_time

	country_potential = {
		has_advance = nd_irq_atabat_compact
	}

	location_potential = {
		OR = {
			this = location:kufa
			this = location:karbala
			this = location:baghdad
			this = location:samarra
		}
	}

	modifier = {
		local_clergy_estate_power = 0.25
		local_cultural_influence = 0.25
		local_migration_attraction = 0.05
		local_monthly_prosperity = 0.001
	}

	unique_production_methods = {
		nd_irq_hadra_maintenance = {
			incense = 0.15
			cloth = 0.15
			beeswax = 0.1
			marble = 0.05
			category = building_maintenance
		}
	}

	construction_demand = town_building_construction
}

# ============================================================
# The mudhif redoubt: a reed hall on a made island, with the causeways and
# skiff channels that serve it. Gated to wetlands topography, which in the
# Crescent means the Bataih between Kufa, Wasit and Basra and nowhere else.
# This is the terrain that held the Zanj for fourteen years and the Muntafiq
# for three centuries.
# ============================================================
nd_irq_mudhif_redoubt = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = peasants
	max_levels = 1
	employment_size = generic_peasant_building_employment

	category = defense_category

	rural_settlement = yes
	town = yes
	city = no
	megalopolis = no

	build_time = small_fort_building

	country_potential = {
		has_advance = nd_irq_mudhif_halls
	}

	location_potential = {
		region = region:crescent_region
		topography = wetlands
	}

	modifier = {
		local_defensive = 0.25
		local_hostile_attrition = 0.5
		local_unrest = -0.05
		local_tribes_estate_power = 0.5
	}

	unique_production_methods = {
		nd_irq_mudhif_maintenance = {
			fiber_crops = 0.25
			lumber = 0.1
			weaponry = 0.1
			category = building_maintenance
		}
	}

	construction_demand = stockade_construction
}

# ============================================================
# The Ubulla wharf: the transhipment quay at the head of the Gulf, where a
# sea-going hull hands its cargo to a river boat because it cannot take the
# shoals upstream. Coastal only, as any port building must be, and bounded to
# the Crescent so it stays the Basra roadstead rather than a generic dockyard.
# ============================================================
nd_irq_ubulla_wharf = {
	audio_tier = 3
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = 1
	employment_size = dock_employment

	category = naval_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	build_time = medium_port_building_time

	country_potential = {
		has_advance = nd_irq_ubulla_roadstead
	}

	location_potential = {
		region = region:crescent_region
		is_coastal = yes
	}

	modifier = {
		can_build_ships_in_this_location = yes
		local_sailors = 0.15
		local_repair_speed = 0.02
		local_merchant_power = 0.05
		harbor_suitability = 0.01
		max_ships_built_at_same_time = 1
	}

	unique_production_methods = {
		nd_irq_wharf_maintenance = {
			lumber = 0.3
			naval_supplies = 0.15
			fiber_crops = 0.1
			tools = 0.1
			category = building_maintenance
		}
		nd_irq_wharf_bitumen_caulking = {
			# unlocked by nd_irq_bitumen_of_hit
			lumber = 0.3
			tar = 0.2
			fiber_crops = 0.15
			tools = 0.1
			category = building_maintenance
		}
	}

	construction_demand = port_building_construction
}

# ============================================================
# The Attabiya looms. A workshop, not a guild, because attabi is a luxury
# weave and the quarter it is named for was a district of master weavers.
# Gated to markets that actually carry cotton, since the whole point of the
# attabi recipe is that it lets a cotton country with almost no silk of its
# own still put a silk stripe on the cloth.
# ============================================================
nd_irq_attabiya_workshop = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = workshop_max_level
	startup_ramp_target = workshop_startup_ramp_target
	employment_size = workshop_employment

	category = consumer_goods_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	build_time = workshop_build_time

	country_potential = {
		has_advance = nd_irq_attabiya_looms
	}

	location_potential = {
		is_produced_in_location_market = goods:cotton
	}

	unique_production_methods = {
		nd_irq_attabi_weaving = {
			cotton = 0.9
			silk = 0.25
			dyes = 0.15
			produced = fine_cloth
			output = 0.8958
			debug_max_profit = workshop_profit_margin
			category = workshop_input
		}
	}

	construction_demand = workshop_construction

	custom_tags = { workshop }
}

# ===== DESTINY BUILDINGS =====

# ============================================================
# Dar al-Barid. The caliphal post: relay stations a day's ride apart with
# remounts, a courier's room and sealed dispatch bags, and a sahib al-barid
# resident in each one whose actual commission was to report on the governor
# he lived beside. Reach is the trunk-road geography of the old caliphate
# (the Crescent, Egypt, Arabia and Persia) and towns and cities only, because
# a relay needs stabling, a market for fodder and a wall to keep the bags
# behind. It is not buildable on open country, which is the half of the route
# the couriers rode through rather than stopped in.
# ============================================================
nd_irq_dar_al_barid = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = 1
	employment_size = generic_burgher_employment

	category = government_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes

	build_time = government_build_time

	country_potential = {
		has_advance = nd_irq_dest_shadow_caliph
	}

	location_potential = {
		OR = {
			region = region:crescent_region
			region = region:egypt_region
			region = region:arabia_region
			region = region:persia_region
		}
	}

	modifier = {
		local_proximity_source = 20
		local_distance_from_capital_speed_propagation = 0.1
		local_max_control = 0.05
	}

	unique_production_methods = {
		nd_irq_barid_maintenance = {
			horses = 0.25
			paper = 0.15
			leather = 0.1
			tools = 0.1
			category = building_maintenance
		}
	}

	construction_demand = rahdar_construction
}

# ============================================================
# The Dulab Works. A yard that builds and repairs water-lifting machinery:
# the ox-driven dulab, the current-driven naura with its earthenware pots
# lashed to the rim, and the crank-and-piston pumps out of al-Jazari. Bounded
# to Crescent ground that has a river or a lake, because a machine that lifts
# water needs water under it, and separated from the Nahrawan head-works by
# what it does: a regulator decides how much river enters a canal, a dulab
# puts water on ground the canal cannot reach at all.
# ============================================================
nd_irq_dulab_works = {
	audio_tier = 2
	is_special = yes
	is_foreign = no
	pop_type = laborers
	max_levels = 1
	employment_size = generic_peasant_building_employment

	category = infrastructure_category

	rural_settlement = yes
	town = yes
	city = yes
	megalopolis = yes

	build_time = infrastructure_build_time

	country_potential = {
		has_advance = nd_irq_dest_ingenious_devices
	}

	location_potential = {
		region = region:crescent_region
		OR = {
			has_river = yes
			is_adjacent_to_lake = yes
		}
	}

	modifier = {
		local_max_rgo_size = 2
		local_raw_material_output = 0.1
		local_monthly_development_modifier = 0.025
	}

	unique_production_methods = {
		nd_irq_dulab_maintenance = {
			lumber = 0.25
			tools = 0.15
			clay = 0.1
			fiber_crops = 0.1
			category = building_maintenance
		}
	}

	construction_demand = aqueduct_system_construction
}

# ============================================================
# The Marsad. An observatory: a quadrant cut on the meridian, an instrument
# shop that files its own graduated brass, and a room of tables kept for the
# thirty years an observation programme actually takes. Cities only, because
# it wants a patron, a workforce of calculators and an instrument trade within
# walking distance, and Crescent only, because a zij is computed for one
# latitude and is politely wrong everywhere else. Staffed by burghers rather
# than clergy, which is what separates it from the Mustansiriya: the men who
# cut the brass and keep the register are tradesmen, not jurists.
# ============================================================
nd_irq_marsad = {
	audio_tier = 4
	is_special = yes
	is_foreign = no
	pop_type = burghers
	max_levels = 1
	employment_size = cultural_employment

	category = cultural_category

	rural_settlement = no
	town = no
	city = yes
	megalopolis = yes

	expensive = yes
	build_time = large_cultural_building_time

	AI_ignore_available_worker_flag = yes

	country_potential = {
		has_advance = nd_irq_dest_corrected_tables
	}

	location_potential = {
		region = region:crescent_region
	}

	modifier = {
		local_cultural_tradition = 0.15
		local_institution_growth_modifier = 0.25
		local_max_literacy = 3
	}

	unique_production_methods = {
		nd_irq_marsad_maintenance = {
			paper = 0.3
			copper = 0.15
			glass = 0.1
			books = 0.1
			category = building_maintenance
		}
	}

	construction_demand = academy_construction
}
