# Occitania (OCC): age 1 levies
#
# Read-order file: the engine fills each levy slot with the FIRST valid levy
# across all levy files in name order, so the numeric prefix encodes this
# file's unlock age exactly like vanilla (00_revolutions ... 05_traditions).
# The extra 0 after the prefix sorts us before the vanilla file of the same
# age so the special levy beats the same-age generic. Never merge a tag's
# levies into one 00_ file: that shadows every newer vanilla levy forever.

# Bidau: Pyrenean javelin skirmishers, raised off the slope rather than the
# plain. Peasant and soldier pops at the generic infantry share.
levy_nd_occ_bidau = {
	size = levy_generic_infantry_size
	unit = a_nd_occ_bidau

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = OCC
	}
}
