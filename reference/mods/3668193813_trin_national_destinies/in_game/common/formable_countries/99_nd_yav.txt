YAV_f = {

	level = 3
	required_locations_fraction = 0.65
	rule = plausible

	potential = {
		culture = culture:sindhi
		religion = religion:sammitiya
	}

	allow = {
		owns = location:thatta
		owns = location:peshawar
	}

	name = YAV
	flag = YAV
	adjective = YAV_ADJ
	tag = YAV
	color = map_YAV

	areas = {
		sindh_area
		punjab_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_yav_formed

		}
		set_capital = location:peshawar
		trigger_event_non_silently = nd_yav.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
	}
}
