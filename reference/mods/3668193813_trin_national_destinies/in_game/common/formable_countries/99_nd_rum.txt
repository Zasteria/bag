REPLACE:RUM_f = {
	level = 4
	required_locations_fraction = 0.85
	rule = historical
	potential = {
		culture = { has_culture_group = culture_group:turkic_group }
		religion.group = religion_group:muslim
	}
	allow = {

		custom_tooltip = {
			text = concluded_roto_as_not_ottos_tt
			has_variable = rise_of_the_ottomans_winner_variable
		}
	}
	
	name = RUM
	flag = RUM
	adjective = RUM_ADJ
	tag = TUR
	color = map_RUM

	regions = {
		anatolia_region
	}
	form_effect = {
		# Formation markers live HERE, not only in the event's options.
		# A country can be formed by script (vanilla forms JAP from the
		# sengoku situation), and then the popup may never be answered,
		# leaving every advance gated on this variable unreachable.
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_rum_formed
		}
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		trigger_event_non_silently = nd_rum.1
		add_stability = stability_severe_bonus
	}
}
