REPLACE:IRO_f = { # Haudenosaunee / Iroquois Confederacy

	level = 3
	required_locations_fraction = 0.80
	rule = historical

	potential = {
		culture = { has_culture_group = culture_group:haudenosaunee_group }
	}

	allow = {
		culture = { has_culture_group = culture_group:haudenosaunee_group }
	}
	
	name = IRO
	flag = IRO
	adjective = IRO_ADJ
	tag = IRO
	color = map_haudenosaunee

	provinces = {
		onondowaga_province
		gayogohono_province
		onondagega_province
		onyotaaka_province
		kanienkehaka_province
	}
	
	form_effect = {
	
		# Formation markers live HERE, not only in the event's options.
	
		# A country can be formed by script (vanilla forms JAP from the
	
		# sengoku situation), and then the popup may never be answered,
	
		# leaving every advance gated on this variable unreachable.
	
		hidden_effect = {
	
			set_variable = nd_formed
	
			set_variable = nd_iro_formed
	
		}
		set_country_rank_effect = { rank = country_rank:rank_kingdom }
		trigger_event_non_silently = nd_iro.1
		add_stability = stability_mild_bonus
	}
}
