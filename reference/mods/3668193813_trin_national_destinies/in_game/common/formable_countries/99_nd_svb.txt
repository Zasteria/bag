SVB_f = {

	level = 4
	required_locations_fraction = 0.55
	rule = plausible

	potential = {
		religion.group = religion_group:buddhist
		OR = {
			culture = culture:thai_culture
			culture = culture:khmer_culture
			culture = culture:burmese_culture
			culture = culture:mon_culture
			culture = culture:lao_culture
		}
	}

	allow = {
		owns = location:angkor
		owns = location:ayodhya
		owns = location:pagan
		owns = location:vientiane
		in_civil_war = no
	}

	name = SVB
	flag = SVB
	adjective = SVB_ADJ
	tag = SVB
	color = map_SVB

	regions = {
		indochina_region
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_svb_formed

		}
		trigger_event_non_silently = nd_svb.1
		if = {
			limit = {
				country_rank_level < 4
				NOT = { is_member_of_international_organization = international_organization:hre }
			}
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		add_stability = stability_severe_bonus
	}
}
