# Bosporan Kingdom (TDR): Crimean Greco-Gothic successor state.
# Formation event (nd_tdr.1) lets the player choose between
# the Roman/Greek "Regnum Bospori" naming and the Crimean Gothic
# "Kingdom of the Cimmerian Bosporus" naming.

TDR_f = {

	level = 2
	required_locations_fraction = 0.9
	rule = plausible

	potential = {
		culture = { has_culture_group = culture_group:greek_group }
		religion = religion:orthodox
		# The Empire never demotes itself to a Crimean principality
		# (2026-08-03 blast-radius audit, user ruling): Theodoro is the
		# REMNANT road, for the Gothic-Greek principality itself or a
		# Trebizond-style rump migrating to the straits, and BYZ carries
		# the byzantine_succession_crisis plus the largest Greek DHE
		# pool. Trebizond stays eligible: the Gabras dynasty tie to
		# Theodoro is real.
		NOT = { tag = BYZ }
	}

	allow = {
		owns = location:theodoro
	}

	name = TDR
	flag = TDR
	adjective = TDR_ADJ
	tag = TDR
	color = map_TDR

	areas = {
		crimea_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_tdr_formed

		}
		trigger_event_non_silently = nd_tdr.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
	}
}
