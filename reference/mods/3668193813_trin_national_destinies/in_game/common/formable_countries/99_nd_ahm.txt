AHM_f = {

	level = 2
	required_locations_fraction = 0.75
	rule = historical

	potential = {
		OR = {
			culture = culture:tai_nua_culture
			culture = culture:kamarupi
			culture = culture:chutia_culture
			culture = culture:dimasa_culture
			culture = culture:bodo_culture
			culture = culture:koch_culture
			culture = culture:tiwa_culture
			culture = culture:deori_culture
		}
	}

	allow = {
		owns = location:charaideo
		owns = location:guwahati
	}

	name = AHM
	flag = AHM
	adjective = AHM_ADJ
	tag = AHM
	color = map_AHM

	areas = {
		assam_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_ahm_formed

		}
		trigger_event_non_silently = nd_ahm.1
		add_stability = stability_mild_bonus
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		set_capital = location:charaideo
	}
}
