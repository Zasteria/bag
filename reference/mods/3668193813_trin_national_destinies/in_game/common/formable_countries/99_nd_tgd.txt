TGD_f = {

	level = 4
	required_locations_fraction = 0.5
	rule = plausible

	potential = {
		culture = {
			has_culture_group = culture_group:chinese_group
		}
		# CHI never tag-switches away (2026-08-03 blast-radius audit):
		# vanilla cycles new dynasties INSIDE the persistent CHI tag
		# (crisis_of_the_chinese_dynasty), and CHI carries the largest
		# tag-keyed content pool in the game (flavor_chi 80 DHE, red
		# turbans, sinicization). A CHI player proclaiming a new golden
		# age is the vanilla dynasty-change flow, not a tag switch.
		# Mirrors the sibling CSO formable's exclusion.
		NOT = { tag = CHI }
	}

	allow = {
		owns = location:jingzhao
		owns = location:kashgar
		NOT = { is_situation_active = situation:red_turban_rebellions }
		in_civil_war = no
	}

	name = TGD
	flag = TGD
	adjective = TGD_ADJ
	tag = TGD
	color = map_TGD

	regions = {
		west_china_region
		north_china_region
		east_china_region
		xinjiang_region
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_tgd_formed

		}
		trigger_event_non_silently = nd_tgd.1
		add_stability = stability_severe_bonus
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		set_capital = location:jingzhao
	}
}
