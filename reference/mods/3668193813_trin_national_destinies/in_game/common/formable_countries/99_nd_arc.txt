# Cilician Armenia (Armenian Kingdom of Cilicia)
# Plausible alt-history: Mediterranean Armenian trade kingdom that survived the 1375 Mamluk conquest
# and expanded westward into Cyprus, Antioch, and Levantine ports.
# Rubenid/Hethumid heritage merged with Crusader-era diplomatic networks and Italian-style maritime trade.
ARC_f = {

	level = 3
	required_locations_fraction = 0.5
	rule = plausible

	potential = {
		culture = culture:armenian_culture
		religion = religion:miaphysite
		NOT = { tag = ARM }
	}

	allow = {
		owns = location:sis
	}

	name = ARC
	flag = ARC
	adjective = ARC_ADJ
	tag = ARC
	color = map_ARC

	areas = {
		cilicia_area
		levant_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_arc_formed

		}
		trigger_event_non_silently = nd_arc.1
		add_stability = stability_mild_bonus
		if = {
			limit = {
				country_rank_level < 3
			}
			set_country_rank_effect = {
				rank = country_rank:rank_kingdom
			}
		}
		set_capital = location:sis
	}
}
