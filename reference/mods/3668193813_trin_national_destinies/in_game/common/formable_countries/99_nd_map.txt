MAP_f = {

	level = 2
	required_locations_fraction = 0.9
	rule = plausible

	potential = {
		culture = { has_culture_group = culture_group:mapudungun_group }
	}

	allow = {
		owns = location:wenteche
	}

	name = MAP
	flag = MAP
	adjective = MAP_ADJ
	tag = MAP
	color = map_MAP

	areas = {
		ngulumapu_area
		puelmapu_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_map_formed

		}
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		trigger_event_non_silently = nd_map.1
		add_stability = stability_mild_bonus
		# Wallmapu Toki council federation: Aillarewes of nine Lof clans
		# under Lonko peace-chiefs and elected wartime Toki. The Coyaqtun
		# parliament-assembly governs the federation; no hereditary king.
		add_country_modifier = {
			modifier = nd_map_toki_federation
			years = -1
		}
	}
}
