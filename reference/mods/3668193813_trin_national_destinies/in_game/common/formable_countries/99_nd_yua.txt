# the Northern Yuan (YUA) is an event-formed tag: vanilla YUA_f carries
# `potential = { always = no  #Formed by event }` and exists only as the data
# payload for a scripted country rebrand. It therefore has no formation event
# of ours; nd_yua.1 is an advance-gated DHE.
#
# THE BUG (Workshop 2026-08-08): with no file here at all, nothing set
# nd_yua_formed at formation. The only writer was that DHE's options, which
# require an age-3 advance, so the bureaucracy grant in
# main_menu/common/auto_modifiers/99_nd_yua.txt (gated on the variable) was
# locked behind an advance for the whole early and mid game.
#
# Vanilla route: flavor_chi.txt:2516 and red_turban_rebellions.txt:3577
# call `form_country = formable_country:YUA_f`. YUA also exists at game
# start, where the monthly_country_pulse bootstrap already pre-sets the
# markers; this covers a later re-formation.
#
# No trigger_event_non_silently here: nd_yua.1 is a DHE with its own
# advance trigger, and calling it at formation would only fail that trigger.
# The markers are the whole point of this file.

INJECT:YUA_f = {
	form_effect = {
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_yua_formed
		}
	}
}
