YMP_f = {

	level = 5
	required_locations_fraction = 0.7
	rule = plausible

	potential = {
		tag = JAP
	}

	# Hideyoshi's full 1591-1592 stated peak: Japan + Korea + Ming proper
	# (BOTH capitals) + Manchuria (Jurchen lands) + one of three southern
	# projection axes per his letters (Manila, Annam, or southern Ming).
	allow = {
		owns = location:kyoto
		owns = location:namgyeong
		owns = location:dadu
		owns = location:shangyuan
		owns = location:shenyang
		OR = {
			owns = location:maynila
			owns = location:thang_long
			owns = location:guangzhou
		}
		in_civil_war = no
	}

	name = YMP
	flag = YMP
	adjective = YMP_ADJ
	tag = YMP
	color = map_YMP

	regions = {
		japan_region
		korea_region
	}
	areas = {
		liaodong_area
		beiping_area
		shandong_area
		hebei_area
		jiangnan_area
		guangdong_area
		luzon_area
		red_river_delta_area
	}
	form_effect = {
		# Formation markers live HERE, not only in the event's options.
		# A country can be formed by script (vanilla forms JAP from the
		# sengoku situation), and then the popup may never be answered,
		# leaving every advance gated on this variable unreachable.
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_ymp_formed
		}
		trigger_event_non_silently = nd_ymp.1
		if = {
			limit = {
				country_rank_level < 4
			}
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		add_stability = stability_severe_bonus
	}
}
