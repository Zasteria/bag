MCA_f = {

	level = 2
	required_locations_fraction = 0.65
	rule = historical

	potential = {
		culture = culture:muisca_culture
	}

	allow = {
		owns = location:funza
		owns = location:chunsua
	}

	name = MCA
	flag = MCA
	adjective = MCA_ADJ
	tag = MCA
	color = map_MCA

	areas = {
		chunsa_area
		boiaca_area
	}
	form_effect = {
		# Formation markers live HERE, not only in the event's options.
		# A country can be formed by script (vanilla forms JAP from the
		# sengoku situation), and then the popup may never be answered,
		# leaving every advance gated on this variable unreachable.
		hidden_effect = {
			set_variable = nd_formed
			set_variable = nd_mca_formed
		}
		set_capital = location:funza
		trigger_event_non_silently = nd_mca.1
		add_stability = stability_mild_bonus
		# Muisca Confederation governance: dual Zipa-Zaque kingship with
		# Iraca priest-king. Decentralized confederation, not a unitary
		# monarchy.
		add_country_modifier = {
			modifier = nd_mca_zipa_zaque_compact
			years = -1
		}
	}
}
