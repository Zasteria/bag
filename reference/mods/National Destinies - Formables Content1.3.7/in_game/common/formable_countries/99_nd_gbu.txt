GBU_f = {

	level = 3
	required_locations_fraction = 0.5
	rule = plausible

	potential = {
		religion = religion:catholic
		OR = {
			culture = culture:burgundian
			culture = culture:franc_comtois
			culture = culture:lorrain
			culture = culture:walloon
			culture = culture:picard
			culture = culture:low_franconian
			culture = culture:frisian
			culture = culture:french
			tag = BUR
			tag = LOR
		}
	}

	allow = {
		owns = location:bruges
		owns = location:dijon
		OR = {
			owns = location:nancy
			owns = location:besancon
			owns = location:metz
		}
	}

	name = GBU
	flag = GBU
	adjective = GBU_ADJ
	tag = GBU
	color = map_GBU

	areas = {
		flanders_area
		brabant_area
		wallonia_area
		holland_area
		picardy_area
		champagne_area
		lorraine_area
		alsace_area
		franche_comte_area
		burgundy_area
	}

	form_effect = {

		# Formation markers live HERE, not only in the event's options.

		# A country can be formed by script (vanilla forms JAP from the

		# sengoku situation), and then the popup may never be answered,

		# leaving every advance gated on this variable unreachable.

		hidden_effect = {

			set_variable = nd_formed

			set_variable = nd_gbu_formed

		}
		trigger_event_non_silently = nd_gbu.1
		add_stability = stability_mild_bonus
		if = {
			limit = {
				country_rank_level < 3
				nd_can_take_kingdom_rank = yes
			}
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		set_capital = location:brussels
	}
}
