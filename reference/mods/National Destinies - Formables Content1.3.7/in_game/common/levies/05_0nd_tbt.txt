# Read-order file: the engine picks the FIRST valid levy across all levy
# files in name order, so the prefix encodes this file's unlock age
# exactly like vanilla (00_revolutions ... 05_traditions). The extra 0
# after the prefix sorts us before the vanilla file of the same age.
# Do NOT merge tags' levies back into a single 00_ file: that shadows
# every newer vanilla levy forever. Generated by
# scripts/migrate_levy_age_files.py.

# === yul-sde conscript infantry levy ===
# Imperial-era district levy raised by household: peasants and tribesmen
# under noble command, bearing spear and bow.
levy_nd_tbt_yul_sde = {
	size = levy_generic_infantry_size
	unit = a_nd_tbt_yul_sde

	allowed_pop_type = peasants
	allowed_pop_type = soldiers
	allowed_pop_type = tribesmen

	country_allow = {
		has_or_had_tag = TBT
	}
}

# === rgyal-blon noble cavalry levy ===
# The mailed nobility: Tang chronicles called them "men and horses
# completely encased in iron". Raised exclusively from the noble estate.
levy_nd_tbt_rgyal_blon = {
	size = levy_generic_noble_cavalry_size
	unit = a_nd_tbt_rgyal_blon_cavalry

	allowed_pop_type = nobles

	country_allow = {
		has_or_had_tag = TBT
	}
}
