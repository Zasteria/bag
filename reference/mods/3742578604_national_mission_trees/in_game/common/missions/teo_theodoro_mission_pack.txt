teo_theodoro_mission_pack = {
	icon = nmt_theodoro
	repeatable = no
	player_playstyle = military
	chance = 10000000

	visible = {
		game_has_missions_enabled = yes
		has_or_had_tag = FEO
		NOR = { country_type = pop country_type = building }
	}

	enabled = { always = yes }

	on_start = { set_variable = teo_mission_started }
	on_completion = { remove_variable = teo_mission_started }
	on_abort = { remove_variable = teo_mission_started }

	# ==================== ROOT ====================

	teo_rise_of_theodoro = {
		icon = legacy_of_great_empires
		requires = { }
		enabled = { always = yes }
		on_completion = {
			trigger_event_non_silently = teo_events.1
		}
	}

	# ==================== A: 海軍・クリミア ====================

	teo_black_sea_fleet = {
		icon = royal_navy
		requires = { teo_rise_of_theodoro }
		enabled = {
			navy_size >= 6
		}
		on_completion = {
			add_country_modifier = { modifier = teo_black_sea_fleet_mod years = -1 mode = add }
		}
	}

	teo_build_theodoro_harbor = {
		icon = hsa_mastery_of_the_sea
		requires = { teo_black_sea_fleet }
		enabled = {
			location:theodoro = {
				owner = root
				OR = {
					has_building = building_type:dry_dock
					has_building = building_type:shipyard
					has_building = building_type:grand_shipyard
					has_building = building_type:naval_base
				}
			}
		}
		on_completion = {
			location:theodoro = {
				add_location_modifier = { modifier = teo_theodoro_harbor_mod years = -1 mode = add }
			}
		}
	}

	teo_genoese_question = {
		icon = a_magnanimous_empire
		requires = { teo_rise_of_theodoro }
		enabled = {
			gold >= 300
			country_exists = c:GEN
		}
		on_completion = {
			trigger_event_non_silently = teo_events.5
		}
	}

	teo_expel_genoese = {
		icon = glorious_arms
		requires = { teo_genoese_question }
		enabled = {
			any_owned_location = {
				area = area:crimea_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 3
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_crimea_trade_mod years = -1 mode = add }
		}
	}

	teo_dominate_crimea = {
		icon = expansionism
		requires = { teo_expel_genoese }
		enabled = {
			any_owned_location = {
				area = area:crimea_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 12
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_crimea_domination_mod years = -1 mode = add }
		}
	}

	teo_tana_trade = {
		icon = continental_trade
		requires = { teo_dominate_crimea }
		enabled = {
			location:tana = {
				owner = root
			}
		}
		on_completion = {
			location:tana = {
				add_location_modifier = { modifier = teo_tana_mod years = -1 mode = add }
			}
		}
	}

	teo_black_sea_domination = {
		icon = maritime_advance_age_4
		requires = { teo_tana_trade teo_build_theodoro_harbor }
		enabled = {
			navy_tradition >= 50
			navy_size >= 20
			trade_sea_efficiency >= 0.05
		}
		on_completion = {
			add_country_modifier = { modifier = teo_black_sea_dom_mod years = -1 mode = add }
		}
	}

	# ==================== B: ビザンツ遺産 ====================

	teo_greek_heritage = {
		icon = exsurge_domine
		requires = { teo_rise_of_theodoro }
		enabled = {
			dominant_religion = religion:orthodox
			estate_satisfaction:clergy_estate >= 0.65
			location:theodoro = {
				has_building = building_type:orthodox_monastery
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_greek_heritage_mod years = -1 mode = add }
		}
	}

	teo_trebizond_relations = {
		icon = cultural_ties
		requires = { teo_greek_heritage }
		enabled = {
			OR = {
				NOT = { country_exists = c:TRE }
				c:TRE = { is_subject_of = root }
				AND = {
					is_allied_with = { target = c:TRE }
					opinion = { target = c:TRE value >= 180 }
				}
			}
		}
		on_completion = {
			trigger_event_non_silently = teo_events.10
		}
	}

	teo_restore_pontus = {
		icon = crusader_tradition
		requires = { teo_trebizond_relations }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:trebizond
			any_owned_location = {
				area = area:pontus_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_pontus_mod years = -1 mode = add }
		}
	}

	teo_constantinople = {
		icon = glorious_arms
		requires = { teo_trebizond_relations }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:constantinople
		}
		on_completion = {
			trigger_event_non_silently = teo_events.20
		}
	}

	teo_gothia_ascendant = {
		icon = crown_power_advance_renaissance
		requires = { teo_constantinople }
		enabled = {
			prestige >= 60
			num_locations >= 50
		}
		on_completion = {
			add_country_modifier = { modifier = teo_gothia_mod years = -1 mode = add }
		}
	}

	teo_aegean_supremacy = {
		icon = ship_building_advance
		requires = { teo_constantinople }
		enabled = {
			any_owned_location = {
				region = region:balkan_region
				count >= 30
			}
			navy_tradition >= 45
		}
		on_completion = {
			add_country_modifier = { modifier = teo_aegean_mod years = -1 mode = add }
		}
	}

	teo_balkans = {
		icon = offensive_army
		requires = { teo_gothia_ascendant }
		enabled = {
			any_owned_location = {
				region = region:balkan_region
				count >= 60
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_balkans_mod years = -1 mode = add }
		}
	}

	# ==================== C: ステップ・コーカサス ====================

	teo_azov_gateway = {
		icon = expansionism
		requires = { teo_dominate_crimea }
		enabled = {
			any_owned_location = {
				area = area:azov_area
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_azov_mod years = -1 mode = add }
		}
	}

	teo_pryazovia = {
		icon = pioneer_spirit
		requires = { teo_azov_gateway }
		enabled = {
			any_owned_location = {
				area = area:pryazovia_area
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_pryazovia_mod years = -1 mode = add }
		}
	}

	teo_tatar_wars = {
		icon = medieval_military
		requires = { teo_azov_gateway }
		enabled = {
			any_current_war = {
				OR = {
					attacker_leader = root
					defender_leader = root
				}
				any_war_participant = {
					NOT = { this = root }
					NOT = { religion.group = root.religion.group }
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_tatar_wars_mod years = 30 mode = add }
		}
	}

	teo_into_steppes = {
		icon = glorious_arms
		requires = { teo_pryazovia teo_tatar_wars }
		enabled = {
			any_owned_location = {
				region = region:steppes_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 100
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_steppes_mod years = -1 mode = add }
		}
	}

	teo_caucasus_gateway = {
		icon = offensive_army
		requires = { teo_pryazovia }
		enabled = {
			any_owned_location = {
				region = region:caucasus_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 30
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_caucasus_mod years = -1 mode = add }
		}
	}

	teo_caspian_reach = {
		icon = maritime_advance_age_2
		requires = { teo_caucasus_gateway }
		enabled = {
			any_owned_location = {
				region = region:persia_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_caspian_mod years = -1 mode = add }
		}
	}

	teo_russian_question = {
		icon = cultural_ties
		requires = { teo_into_steppes }
		enabled = {
			OR = {
				opinion = { target = c:MOS value >= 120 }
				opinion = { target = c:NOV value >= 120 }
				any_owned_location = {
					region = region:russian_region
					count >= 20
				}
			}
		}
		on_completion = {
			trigger_event_non_silently = teo_events.30
		}
	}

	teo_russian_claim = {
		icon = crusader_tradition
		requires = { teo_russian_question }
		enabled = {
			any_owned_location = {
				region = region:russian_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 60
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_russian_claim_mod years = -1 mode = add }
		}
	}

	teo_ruthenia_conquest = {
		icon = expansionism
		requires = { teo_russian_question }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:kyiv
			any_owned_location = {
				region = region:ruthenia_region
				count >= 40
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_ruthenia_mod years = -1 mode = add }
		}
	}

	# ==================== D: 内政・交易 ====================

	teo_dev_theodoro = {
		icon = city_building_advance
		requires = { teo_rise_of_theodoro }
		enabled = {
			location:theodoro = {
				rgo_level >= 4
				total_building_levels >= 30
			}
		}
		on_completion = {
			location:theodoro = {
				add_development = 5
				add_location_modifier = { modifier = teo_theodoro_dev_mod years = -1 mode = add }
			}
		}
	}

	teo_trade_empire = {
		icon = a_magnanimous_empire
		requires = { teo_dev_theodoro }
		enabled = {
			monthly_income_trade_and_tax >= 80
			any_owned_location = {
				is_market_center = yes
				count >= 2
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_trade_empire_mod years = -1 mode = add }
		}
	}

	teo_silk_road = {
		icon = generic_traditional_economy
		requires = { teo_trade_empire }
		enabled = {
			produced_in_country:silk > 10
			monthly_income_trade_and_tax >= 150
		}
		on_completion = {
			add_country_modifier = { modifier = teo_silk_mod years = -1 mode = add }
		}
	}


	teo_gothic_culture = {
		icon = encouragement_of_philosophers
		requires = { teo_greek_heritage }
		enabled = {
			global_monthly_literacy >= 0.05
			any_owned_location = {
				has_building = building_type:university
				count >= 1
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_gothic_culture_mod years = -1 mode = add }
		}
	}

	teo_orthodox_fortress = {
		icon = defenses_of_novgorod
		requires = { teo_greek_heritage }
		enabled = {
			dominant_religion = religion:orthodox
			any_owned_location = {
				has_building = building_type:temple
				count >= 10
			}
			any_owned_location = {
				OR = {
					has_building = building_type:castle
					has_building = building_type:bastion
					has_building = building_type:fortress
				}
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_orthodox_fortress_mod years = -1 mode = add }
		}
	}


	teo_great_theodoro = {
		icon = city_building_advance
		requires = { teo_trade_empire teo_orthodox_fortress }
		enabled = {
			location:theodoro = {
				development >= 50
				total_building_levels >= 100
				population >= 100
			}
		}
		on_completion = {
			location:theodoro = {
				change_location_rank = location_rank:megalopolis
				add_location_modifier = { modifier = teo_great_theodoro_mod years = -1 mode = add }
			}
		}
	}

	# ==================== E: 追加 ====================

	teo_crimean_wine = {
		icon = generic_trade
		requires = { teo_dominate_crimea }
		enabled = {
			produced_in_country:wine > 10
		}
		on_completion = {
			add_country_modifier = { modifier = teo_wine_mod years = -1 mode = add }
		}
	}

	teo_fortress_mangup = {
		icon = a_fortified_border
		requires = { teo_rise_of_theodoro }
		enabled = {
			location:theodoro = {
				owner = root
				OR = {
				has_building = building_type:city_walls
					has_building = building_type:bastion
					has_building = building_type:fortress
					has_building = building_type:star_fort
				}
			}
		}
		on_completion = {
			location:theodoro = {
				add_location_modifier = { modifier = teo_mangup_fortress_mod years = -1 mode = add }
			}
		}
	}

	teo_anatolian_coast = {
		icon = offensive_army
		requires = { teo_trebizond_relations }
		enabled = {
			any_owned_location = {
				region = region:anatolia_region
				count >= 50
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_anatolia_mod years = -1 mode = add }
		}
	}

	teo_orthodox_patriarch = {
		icon = ties_with_the_holy_see
		requires = { teo_gothic_culture }
		enabled = {
			dominant_religion = religion:orthodox
			estate_satisfaction:clergy_estate >= 0.80
			owns_or_non_sovereign_subject_owns = location:constantinople
			any_owned_location = {
				has_building = building_type:temple
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_patriarch_mod years = -1 mode = add }
		}
	}


	teo_merchant_republic = {
		icon = merchants_and_trade
		requires = { teo_silk_road }
		enabled = {
			monthly_income_trade_and_tax >= 600
			any_owned_location = {
				is_market_center = yes
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teo_merchant_republic_mod years = -1 mode = add }
		}
	}

	teo_dev_trebizond_city = {
		icon = crossroads_of_commerce
		requires = { teo_restore_pontus }
		enabled = {
			location:trebizond = {
				owner = root
				total_building_levels >= 50
			}
		}
		on_completion = {
			location:trebizond = {
				add_development = 3
				add_location_modifier = { modifier = teo_trebizond_city_mod years = -1 mode = add }
			}
		}
	}

	# ==================== 大合流 ====================

	teo_gavras_triumphant = {
		icon = legacy_of_great_empires
		requires = { teo_gothia_ascendant teo_black_sea_domination teo_great_theodoro }
		enabled = {
			monthly_income_trade_and_tax >= 600
			num_locations >= 200
			prestige >= 80
		}
		on_completion = {
			trigger_event_non_silently = teo_events.60
		}
	}

	# ==================== FINAL ====================

	teo_gothia_empire = {
		icon = legacy_of_great_empires
		requires = { teo_gavras_triumphant teo_balkans teo_russian_claim teo_caspian_reach }
		final = yes
		enabled = {
			num_locations >= 1000
			monthly_income_trade_and_tax >= 1200
			prestige >= 95
		}
		on_completion = {
			trigger_event_non_silently = teo_events.99
		}
	}
}
