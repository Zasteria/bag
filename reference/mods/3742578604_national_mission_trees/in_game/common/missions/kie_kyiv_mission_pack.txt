kie_kyiv_mission_pack = {
	icon = nmt_kyiv
	repeatable = no
	player_playstyle = military
	chance = 10000000

	visible = {
		game_has_missions_enabled = yes
		has_or_had_tag = KIE
		NOR = { country_type = pop country_type = building }
	}

	enabled = { always = yes }

	on_start = { set_variable = kie_mission_started }
	on_completion = { remove_variable = kie_mission_started }
	on_abort = { remove_variable = kie_mission_started }

	# ==================== ROOT ====================

	kie_restore_kyiv = {
		icon = exsurge_domine
		requires = { }
		enabled = { always = yes }
		on_completion = {
			trigger_event_non_silently = kie_events.1
		}
	}

	# ==================== A: ルーシ統一 ====================

	kie_dev_kyiv = {
		icon = city_building_advance
		requires = { kie_restore_kyiv }
		enabled = {
			treasury >= 500
			location:kyiv = {
				rgo_level >= 5
			}
		}
		on_completion = {
			location:kyiv = {
				change_location_rank = location_rank:city
				add_development = 5
				add_location_modifier = { modifier = kie_kyiv_dev_mod years = -1 mode = add }
			}
		}
	}

	kie_secure_left_bank = {
		icon = expansionism
		requires = { kie_restore_kyiv }
		enabled = {
			NOT = {
				area:left_bank_ukraine_area = {
					any_location_in_area = {
						owner = root
						NOT = { local_control >= 0.4 }
					}
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_left_bank_mod years = -1 mode = add }
		}
	}

	kie_secure_right_bank = {
		icon = expansionism
		requires = { kie_restore_kyiv }
		enabled = {
			NOT = {
				area:right_bank_ukraine_area = {
					any_location_in_area = {
						owner = root
						NOT = { local_control >= 0.5 }
					}
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_right_bank_mod years = -1 mode = add }
		}
	}

	kie_conquer_chernihiv = {
		icon = offensive_army
		requires = { kie_restore_kyiv }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:chernihiv
			any_owned_location = {
				area = area:severia_area
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_chernihiv_mod years = 30 mode = add }
		}
	}

	kie_conquer_smolensk = {
		icon = offensive_army
		requires = { kie_conquer_chernihiv }
		enabled = {
			any_owned_location = {
				area = area:smolensk_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 20
			}
			owns_or_non_sovereign_subject_owns = location:smolensk
		}
		on_completion = {
			location:smolensk = {
				add_location_modifier = { modifier = kie_smolensk_mod years = -1 mode = add }
			}
		}
	}


	kie_conquer_poltava = {
		icon = offensive_army
		requires = { kie_conquer_vinnytsia }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:poltava
			any_owned_location = {
				area = area:sloboda_ukraine_area
				count >= 10
			}
		}
		on_completion = {
			location:poltava = {
				add_location_modifier = { modifier = kie_poltava_mod years = -1 mode = add }
			}
		}
	}

	kie_kyiv_metropolitan = {
		icon = ties_with_the_holy_see
		requires = { kie_dev_kyiv }
		enabled = {
			dominant_religion = religion:orthodox
			estate_satisfaction:clergy_estate >= 0.70
		}
		on_completion = {
			add_country_modifier = { modifier = kie_metropolitan_mod years = -1 mode = add }
		}
	}

	kie_unite_rus = {
		icon = legacy_of_great_empires
		requires = { kie_conquer_chernihiv }
		enabled = {
			any_owned_location = {
				region = region:ruthenia_region
				count >= 240
			}
			prestige >= 50
		}
		on_completion = {
			add_prestige = 30
			add_country_modifier = { modifier = kie_unite_rus_mod years = -1 mode = add }
		}
	}

	# ==================== B: 西方・ガリツィア ====================

	kie_secure_volhynia = {
		icon = expansionism
		requires = { kie_restore_kyiv }
		enabled = {
			any_owned_location = {
				area = area:volhynia_area
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_volhynia_mod years = -1 mode = add }
		}
	}

	kie_conquer_lutsk = {
		icon = offensive_army
		requires = { kie_secure_volhynia }
		enabled = {
			location:lutsk = {
				owner = root
				OR = {
					location_rank = location_rank:town
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
			}
		}
		on_completion = {
			location:lutsk = {
				add_location_modifier = { modifier = kie_lutsk_mod years = -1 mode = add }
			}
		}
	}

	kie_conquer_galicia = {
		icon = glorious_arms
		requires = { kie_secure_volhynia }
		enabled = {
			any_owned_location = {
				area = area:red_ruthenia_area
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_galicia_mod years = 40 mode = add }
		}
	}


	kie_red_ruthenia = {
		icon = crusader_tradition
		requires = { kie_conquer_galicia }
		enabled = {
			any_owned_location = {
				area = area:red_ruthenia_area
				count >= 25
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_red_ruthenia_mod years = 50 mode = add }
		}
	}

	kie_conquer_lviv = {
		icon = a_magnanimous_empire
		requires = { kie_red_ruthenia }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:lviv
			location:lviv = {
				development >= 20
			}
		}
		on_completion = {
			location:lviv = {
				add_location_modifier = { modifier = kie_lviv_mod years = -1 mode = add }
			}
		}
	}

	kie_conquer_podolia = {
		icon = expansionism
		requires = { kie_restore_kyiv }
		enabled = {
			any_owned_location = {
				area = area:podolia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_podolia_mod years = -1 mode = add }
		}
	}

	kie_conquer_vinnytsia = {
		icon = offensive_army
		requires = { kie_conquer_podolia }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:vinnytsia
		}
		on_completion = {
			location:vinnytsia = {
				add_location_modifier = { modifier = kie_vinnytsia_mod years = -1 mode = add }
			}
		}
	}

	kie_conquer_polesia = {
		icon = crusader_tradition
		requires = { kie_secure_volhynia kie_secure_left_bank }
		enabled = {
			any_owned_location = {
				area = area:polesia_area
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_polesia_mod years = -1 mode = add }
		}
	}

	kie_conquer_minsk = {
		icon = offensive_army
		requires = { kie_conquer_polesia }
		enabled = {
			location:minsk = {
				owner = root
			}
		}
		on_completion = {
			location:minsk = {
				add_location_modifier = { modifier = kie_minsk_mod years = -1 mode = add }
			}
		}
	}

	# ==================== C: ステップ・コサック ====================

	kie_cossack_host = {
		icon = glorious_arms
		requires = { kie_secure_left_bank }
		enabled = {
			army_tradition >= 35
			"estate_satisfaction:cossacks_estate" >= 0.70
			any_owned_location = {
				area = area:zaporizhzhia_area
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_cossack_mod years = -1 mode = add }
		}
	}

	kie_zaporizhzhia = {
		icon = glorious_arms
		requires = { kie_cossack_host }
		enabled = {
			any_owned_location = {
				area = area:zaporizhzhia_area
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_zaporizhzhia_mod years = -1 mode = add }
		}
	}

	kie_tatar_wars = {
		icon = crusader_tradition
		requires = { kie_cossack_host }
		enabled = {
			army_tradition >= 45
			any_current_war = {
				OR = {
					attacker_leader = root
					defender_leader = root
				}
				any_war_participant = {
					NOT = { this = root }
					NOT = { religion.group = root.religion.group }
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_tatar_wars_mod years = 30 mode = add }
		}
	}

	kie_black_sea_steppe = {
		icon = expansionism
		requires = { kie_zaporizhzhia kie_tatar_wars }
		enabled = {
			any_owned_location = {
				region = region:steppes_region
					OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 100
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_steppe_mod years = -1 mode = add }
		}
	}

	kie_conquer_crimea = {
		icon = glorious_arms
		requires = { kie_black_sea_steppe }
		enabled = {
			any_owned_location = {
				area = area:crimea_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 12
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_crimea_mod years = -1 mode = add }
		}
	}

	kie_black_sea_coast = {
		icon = hsa_mastery_of_the_sea
		requires = { kie_black_sea_steppe }
		enabled = {
			navy_tradition >= 30
			any_owned_location = {
				area = area:zaporizhzhia_area
				count >= 25
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_black_sea_mod years = -1 mode = add }
		}
	}

	# ==================== D: 正教・文化・農業 ====================

	kie_build_sevastopol = {
		icon = ship_building_advance
		requires = { kie_conquer_crimea }
		enabled = {
			location:theodoro = {
				owner = root
				OR = {
					has_building = building_type:dry_dock
					has_building = building_type:shipyard
					has_building = building_type:grand_shipyard
					has_building = building_type:naval_base
				}
			}
		}
		on_completion = {
			location:theodoro = {
				add_location_modifier = { modifier = kie_sevastopol_mod years = -1 mode = add }
			}
		}
	}

	kie_kyiv_pechersk = {
		icon = ties_with_the_holy_see
		requires = { kie_kyiv_metropolitan }
		enabled = {
			dominant_religion = religion:orthodox
			any_owned_location = {
				has_building = building_type:orthodox_monastery
				count >= 50
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_lavra_mod years = -1 mode = add }
		}
	}

	kie_orthodox_revival = {
		icon = ties_with_the_holy_see
		requires = { kie_kyiv_pechersk }
		enabled = {
			dominant_religion = religion:orthodox
			any_owned_location = {
				has_building = building_type:temple
				count >= 80
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_orthodox_mod years = -1 mode = add }
		}
	}

	kie_grain_breadbasket = {
		icon = food_advance_renaissance
		requires = { kie_secure_left_bank kie_secure_right_bank }
		enabled = {
			produced_in_country:wheat > 120
		}
		on_completion = {
			add_country_modifier = { modifier = kie_grain_mod years = -1 mode = add }
		}
	}

	kie_trade_routes = {
		icon = a_magnanimous_empire
		requires = { kie_dev_kyiv }
		enabled = {
			monthly_income_trade_and_tax >= 80
			any_owned_location = {
				is_market_center = yes
				count >= 2
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_trade_mod years = -1 mode = add }
		}
	}

	kie_hetmanate = {
		icon = crown_power_advance_renaissance
		requires = { kie_cossack_host kie_unite_rus }
		enabled = {
			OR = {
				"estate_power(estate_type:cossacks_estate)" >= 0.20
				AND = {
					"estate_satisfaction:cossacks_estate" >= 0.80
					"estate_satisfaction:nobles_estate" >= 0.80
				}
			}
		}
		on_completion = {
			trigger_event_non_silently = kie_events.20
		}
	}

	kie_literacy_culture = {
		icon = encouragement_of_philosophers
		requires = { kie_kyiv_metropolitan }
		enabled = {
			any_owned_location = {
				has_building = building_type:university
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_literacy_mod years = -1 mode = add }
		}
	}

	# ==================== E: 追加ミッション ====================


	kie_conquer_zhytomyr = {
		icon = offensive_army
		requires = { kie_secure_right_bank }
		enabled = {
			location:zhytomyr = {
				OR = {
					location_rank = location_rank:town
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
				total_building_levels >= 30
			}
		}
		on_completion = {
			location:zhytomyr = {
				add_location_modifier = { modifier = kie_zhytomyr_mod years = -1 mode = add }
			}
		}
	}

	kie_salt_of_galicia = {
		icon = crossroads_of_commerce
		requires = { kie_conquer_galicia }
		enabled = {
			produced_in_country:salt > 15
		}
		on_completion = {
			add_country_modifier = { modifier = kie_salt_mod years = -1 mode = add }
		}
	}

	kie_dev_poltava = {
		icon = city_building_advance
		requires = { kie_conquer_poltava }
		enabled = {
			location:poltava = {
				development >= 20
				rgo_level >= 4
			}
		}
		on_completion = {
			location:poltava = {
				add_location_modifier = { modifier = kie_poltava_city_mod years = -1 mode = add }
			}
		}
	}

	kie_beyond_donets = {
		icon = pioneer_spirit
		requires = { kie_conquer_poltava }
		enabled = {
			any_owned_location = {
				area = area:sloboda_ukraine_area
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_sloboda_mod years = -1 mode = add }
		}
	}

	kie_conquer_moldavia = {
		icon = glorious_arms
		requires = { kie_black_sea_steppe }
		enabled = {
			any_owned_location = {
				area = area:moldavia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_moldavia_mod years = -1 mode = add }
		}
	}

	kie_build_odesa = {
		icon = ship_building_advance
		requires = { kie_conquer_moldavia }
		enabled = {
			location:khadjibey = {
				owner = root
				OR = {
					has_building = building_type:dry_dock
					has_building = building_type:shipyard
					has_building = building_type:grand_shipyard
					has_building = building_type:naval_base
				}
			}
		}
		on_completion = {
			location:khadjibey = {
				add_location_modifier = { modifier = kie_odesa_mod years = -1 mode = add }
			}
		}
	}

	kie_amber_route = {
		icon = continental_trade
		requires = { kie_conquer_lutsk kie_conquer_lviv }
		enabled = {
			produced_in_country:amber > 5
			monthly_income_trade_and_tax >= 150
		}
		on_completion = {
			add_country_modifier = { modifier = kie_amber_mod years = -1 mode = add }
		}
	}

	kie_fortress_line = {
		icon = a_fortified_border
		requires = { kie_tatar_wars }
		enabled = {
			any_owned_location = {
				OR = {
					has_building = building_type:castle
					has_building = building_type:bastion
					has_building = building_type:fortress
					has_building = building_type:star_fort
				}
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_fortress_mod years = -1 mode = add }
		}
	}


	kie_great_kyiv = {
		icon = city_building_advance
		requires = { kie_dev_kyiv kie_kyiv_pechersk }
		enabled = {
			location:kyiv = {
				development >= 40
				total_building_levels >= 150
				population >= 100
			}
		}
		on_completion = {
			location:kyiv = {
				change_location_rank = location_rank:megalopolis
				add_location_modifier = { modifier = kie_great_kyiv_mod years = -1 mode = add }
			}
		}
	}

	kie_ruthenian_culture = {
		icon = encouragement_of_philosophers
		requires = { kie_unite_rus }
		enabled = {
			total_primary_culture_population >= 3000
		}
		on_completion = {
			add_country_modifier = { modifier = kie_culture_mod years = -1 mode = add }
		}
	}


	kie_dev_chernihiv = {
		icon = city_building_advance
		requires = { kie_conquer_chernihiv }
		enabled = {
			location:chernihiv = {
				development >= 25
				rgo_level >= 4
			}
		}
		on_completion = {
			location:chernihiv = {
				add_location_modifier = { modifier = kie_chernihiv_city_mod years = -1 mode = add }
			}
		}
	}

	kie_conquer_wallachia = {
		icon = glorious_arms
		requires = { kie_conquer_moldavia }
		enabled = {
			any_owned_location = {
				area = area:wallachia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_wallachia_mod years = -1 mode = add }
		}
	}


	kie_cossack_cavalry = {
		icon = offensive_army
		requires = { kie_hetmanate }
		enabled = {
			OR = {
				total_sub_unit_count:army_heavy_cavalry >= 20
				total_sub_unit_count:army_light_cavalry >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_cavalry_mod years = -1 mode = add }
		}
	}

	kie_kyiv_academy = {
		icon = library_advance
		requires = { kie_literacy_culture kie_kyiv_pechersk }
		enabled = {
			location:kyiv = {
				has_building = building_type:university
				development >= 30
			}
		}
		on_completion = {
			location:kyiv = {
				add_location_modifier = { modifier = kie_academy_mod years = -1 mode = add }
			}
		}
	}

	kie_conquer_krakow = {
		icon = glorious_arms
		requires = { kie_conquer_galicia }
		enabled = {
			location:krakow = {
				owner = root
			}
		}
		on_completion = {
			location:krakow = {
				add_location_modifier = { modifier = kie_krakow_mod years = -1 mode = add }
			}
		}
	}

	kie_navy_dnipro = {
		icon = ship_building_advance
		requires = { kie_black_sea_coast }
		enabled = {
			navy_size >= 15
			navy_tradition >= 35
		}
		on_completion = {
			add_country_modifier = { modifier = kie_navy_mod years = -1 mode = add }
		}
	}


	kie_protect_orthodoxy = {
		icon = exsurge_domine
		requires = { kie_orthodox_revival }
		enabled = {
			dominant_religion = religion:orthodox
			any_owned_location = {
				has_building = building_type:temple
				count >= 150
			}
			total_population >= 6000
		}
		on_completion = {
			add_country_modifier = { modifier = kie_protector_mod years = -1 mode = add }
		}
	}

	kie_conquer_vilnius = {
		icon = offensive_army
		requires = { kie_conquer_minsk }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:vilnius
		}
		on_completion = {
			add_country_modifier = { modifier = kie_vilnius_mod years = -1 mode = add }
		}
	}

	kie_western_alliance = {
		icon = a_magnanimous_empire
		requires = { kie_unite_rus }
		enabled = {
			OR = {
				opinion = { target = c:HUN value >= 150 }
				opinion = { target = c:POL value >= 150 }
				AND = {
					NOT = { country_exists = c:HUN }
					NOT = { country_exists = c:POL }
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_diplomacy_mod years = -1 mode = add }
		}
	}

	kie_silver_route = {
		icon = crossroads_of_commerce
		requires = { kie_trade_routes }
		enabled = {
			monthly_income_trade_and_tax >= 300
			any_owned_location = {
				has_building = building_type:customs_house
				count >= 2
			}
		}
		on_completion = {
			add_country_modifier = { modifier = kie_silver_route_mod years = -1 mode = add }
		}
	}

	kie_ruthenian_army = {
		icon = medieval_military
		requires = { kie_tatar_wars }
		enabled = {
			army_tradition >= 55
			regular_army_size >= 20
		}
		on_completion = {
			add_country_modifier = { modifier = kie_ruthenian_army_mod years = -1 mode = add }
		}
	}

	kie_conquer_warsaw = {
		icon = glorious_arms
		requires = { kie_conquer_krakow }
		enabled = {
			location:warsaw = {
				owner = root
			}
		}
		on_completion = {
			location:warsaw = {
				add_location_modifier = { modifier = kie_warsaw_mod years = -1 mode = add }
			}
		}
	}

	kie_beeswax_honey = {
		icon = crossroads_of_commerce
		requires = { kie_trade_routes }
		enabled = {
			produced_in_country:beeswax > 15
		}
		on_completion = {
			add_country_modifier = { modifier = kie_beeswax_mod years = -1 mode = add }
		}
	}



	# ==================== 大合流 ====================

	kie_form_ruthenia = {
		icon = legacy_of_great_empires
		requires = { kie_unite_rus kie_conquer_lviv kie_hetmanate kie_orthodox_revival }
		enabled = {
			any_owned_location = {
				region = region:ruthenia_region
				count >= 300
			}
			prestige >= 65
			monthly_income_trade_and_tax >= 200
		}
		on_completion = {
			trigger_event_non_silently = kie_events.60
		}
	}

	# ==================== FINAL ====================

	kie_grand_principality = {
		icon = legacy_of_great_empires
		requires = { kie_form_ruthenia kie_ruthenian_culture }
		final = yes
		enabled = {
			num_locations >= 800
			monthly_income_trade_and_tax >= 600
			prestige >= 80
		}
		on_completion = {
			trigger_event_non_silently = kie_events.99
		}
	}
}
