por_portuguese_mission_pack = {
	icon = nmt_iberia
	repeatable = no
	player_playstyle = military
	chance = 10000000

	visible = {
		game_has_missions_enabled = yes
		OR = {
			has_or_had_tag = POR
			is_portuguese_spain = yes
		}
		NOR = { country_type = pop country_type = building }
	}

	enabled = { always = yes }

	on_start = { set_variable = por_mission_started }
	on_completion = { remove_variable = por_mission_started }
	on_abort = { remove_variable = por_mission_started }

	# ==================== ROOT ====================

	por_infante_henrique = {
		icon = hsa_global_reach
		requires = { }
		enabled = { always = yes }
		on_completion = {
			add_country_modifier = { modifier = por_henrique_mod years = 50 mode = add }
		}
	}

	# ==================== BRANCH A: 国内整備 ====================

	por_dev_lisbon = {
		icon = city_building_advance
		requires = { por_infante_henrique }
		enabled = {
			location:lisbon = {
				total_building_levels >= 60
			}
		}
		on_completion = {
			location:lisbon = {
				add_location_modifier = { modifier = por_lisbon_mod years = -1 mode = add }
			}
		}
	}

	por_dev_porto = {
		icon = city_building_advance
		requires = { por_infante_henrique }
		enabled = {
			location:porto = {
				development >= 25
				rgo_level >= 7
			}
		}
		on_completion = {
			location:porto = {
				add_location_modifier = { modifier = por_porto_mod years = -1 mode = add }
			}
		}
	}

	por_university_coimbra = {
		icon = encouragement_of_philosophers
		requires = { por_dev_lisbon }
		enabled = {
			location:coimbra = {
				has_building = building_type:university
				development >= 25
			}
		}
		on_completion = {
			location:coimbra = {
				add_location_modifier = { modifier = por_coimbra_mod years = -1 mode = add }
			}
		}
	}

	por_lisbon_entrepot = {
		icon = a_magnanimous_empire
		requires = { por_dev_lisbon }
		enabled = {
			location:lisbon = {
				has_building = building_type:trading_hub
				development >= 40
			}
			monthly_income_trade_and_tax >= 200
		}
		on_completion = {
			location:lisbon = {
				add_location_modifier = { modifier = por_lisbon_trade_mod years = -1 mode = add }
			}
		}
	}

	por_avis_dynasty = {
		icon = crown_power_advance_renaissance
		requires = { por_infante_henrique }
		enabled = {
			"estate_power(estate_type:crown_estate)" >= 0.25
		}
		on_completion = {
			add_country_modifier = { modifier = por_avis_mod years = -1 mode = add }
		}
	}

	por_ordenacoes_afonsinas = {
		icon = fra_code_civil
		requires = { por_avis_dynasty }
		enabled = {
			"estate_power(estate_type:crown_estate)" >= 0.35
		}
		on_completion = {
			add_country_modifier = { modifier = por_ordenacoes_mod years = -1 mode = add }
		}
	}

	por_dev_evora = {
		icon = city_building_advance
		requires = { por_avis_dynasty }
		enabled = {
			location:evora = {
				development >= 25
				rgo_level >= 5
			}
		}
		on_completion = {
			location:evora = {
				add_location_modifier = { modifier = por_evora_mod years = -1 mode = add }
			}
		}
	}

	por_algarve_fisheries = {
		icon = crossroads_of_commerce
		requires = { por_infante_henrique }
		enabled = {
			produced_in_country:fish > 30
		}
		on_completion = {
			add_country_modifier = { modifier = por_fisheries_mod years = -1 mode = add }
		}
	}

	# ==================== BRANCH B: 北アフリカ ====================

	por_ceuta_gateway = {
		icon = crusader_tradition
		requires = { por_infante_henrique }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:ceuta
		}
		on_completion = {
			add_country_modifier = { modifier = por_ceuta_mod years = -1 mode = add }
		}
	}

	por_conquer_tangier = {
		icon = crusader_tradition
		requires = { por_ceuta_gateway }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:tangier
		}
		on_completion = {
			add_country_modifier = { modifier = por_tangier_mod years = 50 mode = add }
		}
	}

	por_moroccan_fortress = {
		icon = crusader_tradition
		requires = { por_conquer_tangier }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:agadir
			any_owned_location = {
				area = area:morocco_area
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_morocco_mod years = 30 mode = add }
		}
	}

	# ==================== BRANCH C: 大西洋島嶼・西アフリカ ====================

	por_madeira_settlement = {
		icon = hsa_global_reach
		requires = { por_infante_henrique }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:funchal
		}
		on_completion = {
			location:funchal = {
				add_location_modifier = { modifier = por_madeira_mod years = -1 mode = add }
			}
		}
	}

	por_azores_settlement = {
		icon = hsa_global_reach
		requires = { por_madeira_settlement }
		enabled = {
			any_owned_location = {
				area = area:azores_area
				count >= 4
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_azores_mod years = -1 mode = add }
		}
	}

	por_guinea_trade = {
		icon = crossroads_of_commerce
		requires = { por_madeira_settlement }
		enabled = {
			any_owned_location = {
				region = region:guinea_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_guinea_trade_mod years = -1 mode = add }
		}
	}

	por_gold_coast = {
		icon = crossroads_of_commerce
		requires = { por_guinea_trade }
		enabled = {
			any_owned_location = {
				region = region:guinea_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_gold_coast_mod years = -1 mode = add }
		}
	}

	por_sao_tome_sugar = {
		icon = crossroads_of_commerce
		requires = { por_guinea_trade }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:sao_tome
			produced_in_country:sugar > 5
		}
		on_completion = {
			location:sao_tome = {
				change_raw_material = goods:sugar
				add_location_modifier = { modifier = por_sao_tome_mod years = -1 mode = add }
			}
		}
	}

	por_angola_colony = {
		icon = hsa_global_reach
		requires = { por_sao_tome_sugar }
		enabled = {
			any_owned_location = {
				region = region:kongo_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_angola_mod years = -1 mode = add }
		}
	}

	# ==================== BRANCH D: 東アフリカ・インド洋 ====================

	por_cape_of_good_hope = {
		icon = hsa_global_reach
		requires = { por_guinea_trade }
		enabled = {
			navy_tradition >= 40
			any_owned_location = {
				region = region:southern_africa_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 1
			}
		}
		on_completion = {
			trigger_event_non_silently = por_events.10
		}
	}

	por_east_africa_trading_posts = {
		icon = hsa_mastery_of_the_sea
		requires = { por_cape_of_good_hope }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:mozambique
			owns_or_non_sovereign_subject_owns = location:sofala
		}
		on_completion = {
			add_country_modifier = { modifier = por_east_africa_mod years = -1 mode = add }
		}
	}

	por_mombasa_fort = {
		icon = hsa_mastery_of_the_sea
		requires = { por_east_africa_trading_posts }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:mombasa
			owns_or_non_sovereign_subject_owns = location:malindi
		}
		on_completion = {
			location:mombasa = {
				add_location_modifier = { modifier = por_mombasa_mod years = -1 mode = add }
			}
		}
	}

	por_vasco_da_gama = {
		icon = hsa_global_reach
		requires = { por_cape_of_good_hope }
		enabled = {
			any_owned_location = {
				region = region:deccan_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 1
			}
		}
		on_completion = {
			trigger_event_non_silently = por_events.20
		}
	}

	por_conquer_goa = {
		icon = hsa_mastery_of_the_sea
		requires = { por_vasco_da_gama }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:goa
		}
		on_completion = {
			location:goa = {
				add_location_modifier = { modifier = por_goa_mod years = -1 mode = add }
			}
		}
	}

	por_malabar_trade = {
		icon = crossroads_of_commerce
		requires = { por_vasco_da_gama }
		enabled = {
			any_owned_location = {
				area = area:malabar_area
				count >= 5
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_malabar_mod years = -1 mode = add }
		}
	}

	por_hormuz_control = {
		icon = hsa_mastery_of_the_sea
		requires = { por_east_africa_trading_posts }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:hormuz
		}
		on_completion = {
			add_country_modifier = { modifier = por_hormuz_mod years = -1 mode = add }
		}
	}

	por_conquer_malacca = {
		icon = hsa_global_reach
		requires = { por_malabar_trade }
		enabled = {
			owns_or_non_sovereign_subject_owns = location:malacca
		}
		on_completion = {
			trigger_event_non_silently = por_events.30
		}
	}

	por_spice_monopoly = {
		icon = crossroads_of_commerce
		requires = { por_conquer_malacca por_hormuz_control }
		enabled = {
			monthly_income_trade_and_tax >= 300
		}
		on_completion = {
			add_country_modifier = { modifier = por_spice_monopoly_mod years = -1 mode = add }
		}
	}

	por_indian_ocean_empire = {
		icon = legacy_of_great_empires
		requires = { por_conquer_goa por_mombasa_fort por_hormuz_control }
		enabled = {
			navy_size >= 60
			navy_tradition >= 60
			any_colonial_country = {
				is_subject_of = root
				count >= 3
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_indian_ocean_mod years = -1 mode = add }
		}
	}

	# ==================== BRANCH E: ブラジル ====================

	por_brazil_discovery = {
		icon = hsa_global_reach
		requires = { por_azores_settlement }
		enabled = {
			any_owned_location = {
				region = region:brazil_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 1
			}
		}
		on_completion = {
			trigger_event_non_silently = por_events.40
		}
	}

	por_brazil_colony = {
		icon = hsa_global_reach
		requires = { por_brazil_discovery }
		enabled = {
			any_owned_location = {
				region = region:brazil_region
				count >= 20
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_brazil_mod years = -1 mode = add }
		}
	}

	por_brazil_sugar = {
		icon = crossroads_of_commerce
		requires = { por_brazil_colony }
		enabled = {
			any_owned_location = {
				region = region:brazil_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 40
			}
			produced_in_country:sugar > 30
		}
		on_completion = {
			add_country_modifier = { modifier = por_brazil_sugar_mod years = -1 mode = add }
		}
	}

	por_brazil_empire = {
		icon = legacy_of_great_empires
		requires = { por_brazil_sugar }
		enabled = {
			any_owned_location = {
				region = region:brazil_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 100
			}
			any_colonial_country = {
				is_subject_of = root
				capital = { region = region:brazil_region }
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_brazil_empire_mod years = -1 mode = add }
		}
	}

	# ==================== 追加: 海軍・内政 ====================

	por_naval_supremacy = {
		icon = hsa_mastery_of_the_sea
		requires = { por_ceuta_gateway }
		enabled = {
			navy_tradition >= 50
			navy_size >= 30
		}
		on_completion = {
			add_country_modifier = { modifier = por_naval_mod years = -1 mode = add }
		}
	}

	por_caravel = {
		icon = hsa_mastery_of_the_sea
		requires = { por_madeira_settlement }
		enabled = {
			navy_tradition >= 35
			navy_size >= 20
		}
		on_completion = {
			add_country_modifier = { modifier = por_caravel_mod years = -1 mode = add }
		}
	}

	por_padroes = {
		icon = hsa_global_reach
		requires = { por_cape_of_good_hope }
		enabled = {
			any_owned_location = {
				region = region:southern_africa_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 30
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_padroes_mod years = -1 mode = add }
		}
	}

	por_royal_navy = {
		icon = hsa_mastery_of_the_sea
		requires = { por_naval_supremacy por_caravel }
		enabled = {
			navy_tradition >= 65
			navy_size >= 50
		}
		on_completion = {
			add_country_modifier = { modifier = por_royal_navy_mod years = -1 mode = add }
		}
	}

	por_dev_setubal = {
		icon = city_building_advance
		requires = { por_dev_lisbon }
		enabled = {
			location:setubal = {
				development >= 20
				rgo_level >= 4
			}
		}
		on_completion = {
			location:setubal = {
				add_location_modifier = { modifier = por_setubal_mod years = -1 mode = add }
			}
		}
	}

	por_order_of_christ = {
		icon = eternal_crusade
		requires = { por_ceuta_gateway }
		enabled = {
			dominant_religion = religion:catholic
			has_chivalric_order = yes
			army_tradition >= 40
		}
		on_completion = {
			add_country_modifier = { modifier = por_order_christ_mod years = -1 mode = add }
		}
	}

	por_treaty_tordesillas = {
		icon = dan_danish_legislation
		requires = { por_brazil_discovery por_vasco_da_gama }
		enabled = {
			OR = {
				situation:treaty_of_tordesillas = { situation_is_active = yes }
				situation:treaty_of_tordesillas = { situation_has_ended = yes }
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_tordesillas_mod years = -1 mode = add }
		}
	}

	por_colonial_administration = {
		icon = fra_code_civil
		requires = { por_ordenacoes_afonsinas }
		enabled = {
			any_colonial_country = {
				is_subject_of = root
				count >= 3
			}
			"estate_power(estate_type:crown_estate)" >= 0.30
		}
		on_completion = {
			add_country_modifier = { modifier = por_colonial_admin_mod years = -1 mode = add }
		}
	}

	por_estado_da_india = {
		icon = legacy_of_great_empires
		requires = { por_conquer_goa por_colonial_administration }
		enabled = {
			any_colonial_country = {
				is_subject_of = root
				capital = { region = region:india }
			}
			monthly_income_trade_and_tax >= 400
		}
		on_completion = {
			add_country_modifier = { modifier = por_estado_india_mod years = -1 mode = add }
		}
	}

	por_dev_tomar = {
		icon = city_building_advance
		requires = { por_order_of_christ }
		enabled = {
			location:tomar = {
				development >= 20
				has_building = building_type:castle
			}
		}
		on_completion = {
			location:tomar = {
				add_location_modifier = { modifier = por_tomar_mod years = -1 mode = add }
			}
		}
	}

	por_wine_trade = {
		icon = crossroads_of_commerce
		requires = { por_algarve_fisheries }
		enabled = {
			produced_in_country:wine > 20
		}
		on_completion = {
			add_country_modifier = { modifier = por_wine_mod years = -1 mode = add }
		}
	}

	por_dev_faro = {
		icon = city_building_advance
		requires = { por_algarve_fisheries }
		enabled = {
			location:faro = {
				development >= 20
				rgo_level >= 4
			}
		}
		on_completion = {
			location:faro = {
				add_location_modifier = { modifier = por_faro_mod years = -1 mode = add }
			}
		}
	}


	por_cape_verde = {
		icon = hsa_global_reach
		requires = { por_azores_settlement }
		enabled = {
			any_owned_location = {
				area = area:cape_verde_area
				count >= 3
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_cape_verde_mod years = -1 mode = add }
		}
	}

	por_lisbon_megalopolis = {
		icon = city_building_advance
		requires = { por_lisbon_entrepot }
		enabled = {
			location:lisbon = {
				development >= 50
				total_building_levels >= 150
			}
		}
		on_completion = {
			location:lisbon = {
				change_location_rank = location_rank:megalopolis
				add_location_modifier = { modifier = por_lisbon_mega_mod years = -1 mode = add }
			}
		}
	}

	por_casa_india = {
		icon = a_magnanimous_empire
		requires = { por_malabar_trade }
		enabled = {
			monthly_income_trade_and_tax >= 300
			any_owned_location = {
				is_market_center = yes
				count >= 3
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_casa_india_mod years = -1 mode = add }
		}
	}

	por_brazil_wood = {
		icon = crossroads_of_commerce
		requires = { por_brazil_discovery }
		enabled = {
			any_owned_location = {
				region = region:brazil_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = por_brazil_wood_mod years = -1 mode = add }
		}
	}

	por_atlantic_domination = {
		icon = hsa_mastery_of_the_sea
		requires = { por_royal_navy por_azores_settlement }
		enabled = {
			navy_tradition >= 70
			navy_size >= 60
		}
		on_completion = {
			add_country_modifier = { modifier = por_atlantic_mod years = -1 mode = add }
		}
	}

	# ==================== 大合流: 海の覇者 ====================

	por_empire_of_seas = {
		icon = legacy_of_great_empires
		requires = { por_spice_monopoly por_brazil_empire por_indian_ocean_empire por_lisbon_entrepot }
		final = yes
		enabled = {
			navy_tradition >= 70
			monthly_income_trade_and_tax >= 500
			any_colonial_country = {
				is_subject_of = root
				count >= 6
			}
		}
		on_completion = {
			trigger_event_non_silently = por_events.99
		}
	}
}
