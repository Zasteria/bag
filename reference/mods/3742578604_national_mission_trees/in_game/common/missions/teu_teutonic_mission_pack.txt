teu_teutonic_mission_pack = {
	icon = nmt_teutonic
	repeatable = no
	player_playstyle = military
	chance = 10000000

	visible = {
		game_has_missions_enabled = yes
		has_or_had_tag = TEU
		NOR = { country_type = pop country_type = building }
	}

	enabled = { always = yes }

	on_start = { set_variable = teu_mission_started }
	on_completion = { remove_variable = teu_mission_started }
	on_abort = { remove_variable = teu_mission_started }

	# ===== ROOT =====

	teu_crusaders_legacy = {
		icon = eternal_crusade
		requires = { }
		enabled = {
			NOT = {
				is_at_war_with = c:LIT
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_legacy_mod years = 30 mode = add }
		}
	}

	# ===== A: 共通ブランチ =====

	teu_secure_prussia = {
		icon = a_fortified_border
		requires = { teu_crusaders_legacy }
		enabled = {
			NOT = {
				any_owned_location = {
					OR = {
						area = area:danzig_province
						area = area:tuchola_province
					}
					NOT = { integration_level = integrated }
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_prussia_mod years = -1 mode = add }
		}
	}

	teu_christianize_prussia = {
		icon = a_neverending_crusade
		requires = { teu_secure_prussia }
		enabled = {
			dominant_religion = religion:catholic
			religion_percentage_in_country = {
				religion = religion:catholic
				value >= 0.60
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_christian_mod years = -1 mode = add }
		}
	}

	teu_subdue_lithuania = {
		icon = glorious_arms
		requires = { teu_secure_prussia }
		enabled = {
			any_owned_location = {
				area = area:samogitia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 14
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_lithuania_mod years = 30 mode = add }
		}
	}

	teu_subdue_poland = {
		icon = glorious_arms
		requires = { teu_secure_prussia }
		enabled = {
			any_owned_location = {
				OR = {
					area = area:central_poland_area
					area = area:greater_poland_area
				}
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 10
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_poland_mod years = 30 mode = add }
		}
	}

	teu_order_or_prussia = {
		icon = war_bonfires
		requires = { teu_subdue_lithuania teu_subdue_poland teu_christianize_prussia }
		enabled = { always = yes }
		on_completion = {
			trigger_event_non_silently = teu_events.10
		}
	}

	# ===== B: 十字軍ルート（独立ROOT） =====

	teu_crusade_path = {
		icon = nmt_teu_crusade_path
		requires = { }
		enabled = {
			has_variable = teu_path_crusade
		}
		on_completion = {
			trigger_event_non_silently = teu_events.11
		}
	}

	teu_livonian_alliance = {
		icon = lat_emperor_latins_advance
		requires = { teu_crusade_path }
		enabled = {
			has_variable = teu_path_crusade
			NOT = { country_exists = c:LIV }
			any_owned_location = {
				area = area:baltic_area
				owner = root
				count >= 30
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_livonia_mod years = -1 mode = add }
		}
	}


	teu_crusade_east = {
		icon = nmt_teu_crusade_path
		requires = { teu_crusade_path }
		enabled = {
			has_variable = teu_path_crusade
			any_current_war = {
				OR = {
					attacker_leader = root
					defender_leader = root
				}
				any_war_participant = {
					NOT = { this = root }
					OR = {
						dominant_religion = religion:orthodox
						religion.group = religion_group:muslim
						dominant_religion = religion:romuva
					}
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_east_crusade_mod years = -1 mode = add }
		}
	}

	teu_riga_crusade = {
		icon = a_fortified_border
		requires = { teu_livonian_alliance }
		enabled = {
			has_variable = teu_path_crusade
			owns_or_non_sovereign_subject_owns = location:riga
			army_tradition >= 40
		}
		on_completion = {
			location:riga = {
				add_location_modifier = { modifier = teu_riga_mod years = -1 mode = add }
			}
		}
	}

	teu_march_to_orthodox = {
		icon = divine_supremacy
		requires = { teu_crusade_east teu_livonian_alliance }
		enabled = {
			has_variable = teu_path_crusade
			any_owned_location = {
				region = region:russian_region
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 30
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_orthodox_march_mod years = -1 mode = add }
		}
	}

	teu_northern_crusade_final = {
		icon = nmt_teu_crusade_path
		requires = { teu_march_to_orthodox teu_riga_crusade }
		enabled = {
			has_variable = teu_path_crusade
			num_locations >= 400
			army_tradition >= 50
		}
		on_completion = {
			trigger_event_non_silently = teu_events.50
		}
	}

	teu_constantinople_fall = {
		icon = legacy_of_great_empires
		requires = { teu_northern_crusade_final }
		enabled = {
			has_variable = teu_path_crusade
			location:constantinople = {
				owner = root
			}
		}
		on_completion = {
			trigger_event_non_silently = teu_events.55
		}
	}

	teu_jerusalem_reclaimed = {
		icon = eternal_crusade
		requires = { teu_constantinople_fall }
		enabled = {
			has_variable = teu_path_crusade
			location:jerusalem = {
				owner = root
			}
		}
		on_completion = {
			trigger_event_non_silently = teu_events.56
		}
	}

	# ===== C: プロイセン/HREルート（独立ROOT） =====

	teu_secularize_path = {
		icon = nmt_teu_icon_2
		requires = { }
		enabled = {
			has_variable = teu_path_secular
		}
		on_completion = {
			add_country_modifier = { modifier = teu_secular_start_mod years = 60 mode = add }
		}
	}

	teu_prussian_duchy = {
		icon = crown_power_advance_renaissance
		requires = { teu_secularize_path }
		enabled = {
			has_variable = teu_path_secular
			has_or_had_tag = PRU
		}
		on_completion = {
			add_country_modifier = { modifier = teu_duchy_mod years = -1 mode = add }
		}
	}

	teu_control_pomerania = {
		icon = expansionism
		requires = { teu_secularize_path }
		enabled = {
			has_variable = teu_path_secular
			any_owned_location = {
				area = area:pomerania_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_pomerania_mod years = 30 mode = add }
		}
	}

	teu_protestant_reformation = {
		icon = national_churches
		requires = { teu_prussian_duchy }
		enabled = {
			has_variable = teu_path_secular
			situation:reformation = {
				OR = {
					situation_is_active = yes
					situation_has_ended = yes
				}
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_protestant_mod years = -1 mode = add }
		}
	}

	teu_dominate_hre = {
		icon = lat_emperor_latins_advance
		requires = { teu_prussian_duchy teu_control_pomerania }
		enabled = {
			has_variable = teu_path_secular
			OR = {
				is_leader_of_international_organization = international_organization:hre
				country_rank_level >= 3
			}
			prestige >= 50
		}
		on_completion = {
			add_country_modifier = { modifier = teu_hre_mod years = -1 mode = add }
		}
	}

	teu_north_german_empire = {
		icon = legacy_of_great_empires
		requires = { teu_dominate_hre teu_protestant_reformation }
		enabled = {
			has_variable = teu_path_secular
			any_owned_location = {
				OR = {
					region = region:north_german_region
					region = region:south_german_region
				}
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 100
			}
		}
		on_completion = {
			trigger_event_non_silently = teu_events.60
		}
	}

	# ===== D: 内政・都市 =====

	teu_malbork_citadel = {
		icon = fortified_towns
		requires = { teu_crusaders_legacy }
		enabled = {
			location:malbork = {
				owner = root
				OR = {
					location_rank = location_rank:town
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
				OR = {
					has_building = building_type:castle
					has_building = building_type:bastion
				}
			}
		}
		on_completion = {
			location:malbork = {
				add_location_modifier = { modifier = teu_malbork_mod years = -1 mode = add }
			}
		}
	}

	teu_konigsberg_dev = {
		icon = city_building_advance
		requires = { teu_malbork_citadel }
		enabled = {
			location:konigsberg = {
				owner = root
				OR = {
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
			}
		}
		on_completion = {
			location:konigsberg = {
				add_location_modifier = { modifier = teu_konigsberg_mod years = -1 mode = add }
			}
		}
	}

	teu_danzig_port = {
		icon = hsa_mastery_of_the_sea
		requires = { teu_crusaders_legacy }
		enabled = {
			location:gdansk = {
				owner = root
				integration_level = core
			}
		}
		on_completion = {
			location:gdansk = {
				add_location_modifier = { modifier = teu_danzig_mod years = -1 mode = add }
			}
		}
	}

	teu_teutonic_law = {
		icon = legalism_advance
		requires = { teu_malbork_citadel }
		enabled = {
			legislative_efficiency >= 0.40
			has_advance = teu_prussian_bishops
		}
		on_completion = {
			add_country_modifier = { modifier = teu_law_mod years = -1 mode = add }
		}
	}

	teu_amber_trade = {
		icon = crossroads_of_commerce
		requires = { teu_danzig_port }
		enabled = {
			monthly_income_trade_and_tax >= 60
			any_owned_location = {
				is_market_center = yes
				count >= 1
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_amber_mod years = 40 mode = add }
		}
	}

	teu_hanseatic_ties = {
		icon = hsa_hanseatic_ties
		requires = { teu_amber_trade }
		enabled = {
			monthly_income_trade_and_tax >= 200
			OR = {
				owns_or_non_sovereign_subject_owns = location:lubeck
				is_allied_with = { target = c:HSA }
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_hanseatic_mod years = -1 mode = add }
		}
	}

	teu_colonize_prussia = {
		icon = pioneer_spirit
		requires = { teu_teutonic_law }
		enabled = {
			num_locations >= 200
		}
		on_completion = {
			add_country_modifier = { modifier = teu_colonize_mod years = -1 mode = add }
		}
	}

	teu_torun_city = {
		icon = city_building_advance
		requires = { teu_konigsberg_dev }
		enabled = {
			location:torun = {
				owner = root
				OR = {
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
			}
		}
		on_completion = {
			location:torun = {
				add_location_modifier = { modifier = teu_torun_mod years = -1 mode = add }
			}
		}
	}

	teu_memel_fortress = {
		icon = a_fortified_border
		requires = { teu_secure_prussia }
		enabled = {
			location:memel = {
				owner = root
				OR = {
					has_building = building_type:castle
					has_building = building_type:bastion
				}
			}
		}
		on_completion = {
			location:memel = {
				add_location_modifier = { modifier = teu_memel_mod years = -1 mode = add }
			}
		}
	}


	# ===== A追加: 共通ブランチ補強 =====


	teu_mazovia_subjugation = {
		icon = fra_expansion_of_france
		requires = { teu_subdue_poland }
		enabled = {
			any_owned_location = {
				area = area:mazovia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 15
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_mazovia_mod years = -1 mode = add }
		}
	}

	# ===== B追加: 十字軍ルート補強 =====

	teu_narva_fortress = {
		icon = a_fortified_border
		requires = { teu_livonian_alliance }
		enabled = {
			has_variable = teu_path_crusade
			location:narva = {
				owner = root
				OR = {
					has_building = building_type:castle
					has_building = building_type:bastion
				}
			}
		}
		on_completion = {
			location:narva = {
				add_location_modifier = { modifier = teu_narva_mod years = -1 mode = add }
			}
		}
	}

	# ===== C追加: プロイセンルート補強 =====

	teu_berlin_dev = {
		icon = city_building_advance
		requires = { teu_control_pomerania }
		enabled = {
			has_variable = teu_path_secular
			location:berlin = {
				owner = root
				OR = {
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
			}
		}
		on_completion = {
			location:berlin = {
				add_location_modifier = { modifier = teu_berlin_mod years = -1 mode = add }
			}
		}
	}

	teu_junker_system = {
		icon = court_accounting
		requires = { teu_prussian_duchy }
		enabled = {
			has_variable = teu_path_secular
			estate_satisfaction:nobles_estate >= 0.90
		}
		on_completion = {
			add_country_modifier = { modifier = teu_junker_mod years = -1 mode = add }
		}
	}

	teu_silesia_claim = {
		icon = continental_trade
		requires = { teu_dominate_hre }
		enabled = {
			has_variable = teu_path_secular
			any_owned_location = {
				area = area:silesia_area
				OR = {
					owner = root
					owner = { is_subject_of = root }
				}
				count >= 45
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_silesia_mod years = -1 mode = add }
		}
	}

	# ===== D追加: 内政補強 =====

	teu_cathedral_network = {
		icon = church_guidance
		requires = { teu_christianize_prussia }
		enabled = {
			any_owned_location = {
				has_building = building_type:temple
				count >= 20
			}
			estate_satisfaction:clergy_estate >= 0.75
		}
		on_completion = {
			add_country_modifier = { modifier = teu_cathedral_mod years = -1 mode = add }
		}
	}

	teu_elbing_dev = {
		icon = hsa_mastery_of_the_sea
		requires = { teu_danzig_port }
		enabled = {
			location:elblag = {
				owner = root
				OR = {
					location_rank = location_rank:city
					location_rank = location_rank:megalopolis
				}
				rgo_level >= 6
			}
		}
		on_completion = {
			location:elblag = {
				add_location_modifier = { modifier = teu_elbing_mod years = -1 mode = add }
			}
		}
	}


	# ===== 十字軍ルートEXTRA FINAL =====

	teu_mongol_empire = {
		icon = legacy_of_great_empires
		requires = { teu_grand_master }
		enabled = {
			has_variable = teu_path_crusade
			num_locations >= 2000
			owns_or_non_sovereign_subject_owns = location:sarai_al_jadid
			owns_or_non_sovereign_subject_owns = location:kazan
			owns_or_non_sovereign_subject_owns = location:samarkand
			owns_or_non_sovereign_subject_owns = location:kaffa
			owns_or_non_sovereign_subject_owns = location:dadu
			owns_or_non_sovereign_subject_owns = location:karakorum
		}
		on_completion = {
			trigger_event_non_silently = teu_events.70
		}
	}

	teu_sacred_nomads = {
		icon = nmt_teu_icon_1
		requires = { teu_mongol_empire }
		final = yes
		enabled = {
			has_variable = teu_path_crusade
			num_locations >= 3000
		}
		on_completion = {
			trigger_event_non_silently = teu_events.80
		}
	}


	# ===== FINAL =====

	teu_steppe_crusade = {
		icon = a_neverending_crusade
		requires = { teu_northern_crusade_final }
		enabled = {
			has_variable = teu_path_crusade
			any_owned_location = {
				region = region:steppes_region
				owner = root
				count >= 220
			}
		}
		on_completion = {
			add_country_modifier = { modifier = teu_steppe_crusade_mod years = -1 mode = add }
		}
	}


	teu_grand_master = {
		icon = eternal_crusade
		requires = { teu_steppe_crusade }
		enabled = {
			has_variable = teu_path_crusade
			num_locations >= 500
			prestige >= 75
		}
		on_completion = {
			trigger_event_non_silently = teu_events.99
		}
	}

	teu_prussian_king = {
		icon = nmt_teu_icon_2
		requires = { teu_north_german_empire }
		final = yes
		enabled = {
			has_variable = teu_path_secular
			OR = {
				has_or_had_tag = GER
				has_or_had_tag = HRE
			}
		}
		on_completion = {
			trigger_event_non_silently = teu_events.99
		}
	}
}
