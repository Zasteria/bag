namespace = sav_events

# =============================================================================
# Event 1: サヴォイア家の野望
# =============================================================================
sav_events.1 = {
	type = country_event
	category = mission_event
	title = sav_events.1.t
	desc  = sav_events.1.d
	fire_only_once = yes
	illustration_tags = { 10 = interior 10 = regular }
	immediate = {
		event_illustration_estate_effect = {
			foreground = estate_type:nobles_estate
			background = estate_type:nobles_estate
		}
	}
	option = {
		name = sav_events.1.a
	}
}

# =============================================================================
# Event 2: トリノの栄光
# =============================================================================
sav_events.2 = {
	type = country_event
	category = mission_event
	title = sav_events.2.t
	desc  = sav_events.2.d
	fire_only_once = yes
	illustration_tags = { 10 = interior 10 = regular }
	immediate = {
		event_illustration_estate_effect = {
			foreground = estate_type:burghers_estate
			background = estate_type:burghers_estate
		}
	}
	option = {
		name = sav_events.2.a
		location:turin = {
			add_location_modifier = { modifier = sav_glory_of_turin_mod years = -1 mode = add }
		}
	}
}

# =============================================================================
# Event 3: サルデーニャ・ピエモンテの形成
# =============================================================================
sav_events.3 = {
	type = country_event
	category = mission_event
	title = sav_events.3.t
	desc  = sav_events.3.d
	fire_only_once = yes
	illustration_tags = { 10 = interior 10 = regular }
	immediate = {
		event_illustration_estate_effect = {
			foreground = estate_type:nobles_estate
			background = estate_type:nobles_estate
		}
	}
	option = {
		name = sav_events.3.a
		add_country_modifier = { modifier = sav_sardinia_union_mod years = -1 mode = add }
		set_country_rank_effect = { rank = country_rank:rank_kingdom }
		form_country = formable_country:SAR_f
	}
}
