# Root scope: country.
sakuya_cmm_sittings_register_cmf_mod = {
	# 通用设置 (general) Tab
	## 重置修正 (reset) Group
	cmm_register_button_setting = {
		mod_id = sakuya_cmm_sittings
		setting_id = reset_modifier_country
		tab_id = general
		group_id = reset
	}
}

# Callback handler for cmf_on_callback.
# var:cmf_callback is the flag of the setting, alert, or action bar element that was interacted with.
# Scope: country
sakuya_cmm_sittings_handle_cmf_callback = {
	switch = {
		trigger = var:cmf_callback
		# flag:sakuya_cmm_sittings__my_setting = {
		# 	# Custom side effects when this setting changes
		# }
	}
}
