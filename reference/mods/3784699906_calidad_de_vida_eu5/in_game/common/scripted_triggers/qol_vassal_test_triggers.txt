# Empirical test: scripted trigger form of country_interaction_acceptance
# (same syntax used in game/common/country_interactions/hre.txt).
# If this compiles, the trigger form works; the debug event shows its tooltip.
qol_test_would_accept_vassal = {
	country_interaction_acceptance = {
		type = country_interaction:offer_vassal
		target = scope:recipient
		value > 0
	}
}
