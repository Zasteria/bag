# Clears the gate variable after the parliament gate widget has opened the view.
# Scope: country
qol_clear_open_parliament = {
	scope = country

	effect = {
		remove_variable = qol_open_parliament
	}
}
