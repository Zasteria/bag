# Pause automatically when a parliament session is about to end.
# The Community Mod Menu can override this rule mid-game when CMF is present.
qol_parliament_auto_pause_rule = {
	default = qol_parliament_auto_pause_if_fail

	qol_parliament_auto_pause_off = {
		flag = general_rule
	}

	qol_parliament_auto_pause_always = {
		flag = general_rule
	}

	qol_parliament_auto_pause_if_fail = {
		flag = general_rule
	}
}

# Remind (and pause) when a new parliament session can be called.
qol_parliament_availability_rule = {
	default = qol_parliament_availability_enabled

	qol_parliament_availability_enabled = {
		flag = general_rule
	}

	qol_parliament_availability_disabled = {
		flag = general_rule
	}
}
