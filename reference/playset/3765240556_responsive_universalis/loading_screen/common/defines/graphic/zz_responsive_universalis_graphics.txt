# Responsive Universalis - map-clutter / render-work reduction (visual only, no gameplay effect)
# Trade wagons & ships are ambient map objects; a running session easily has 800+ of them
# (see "Total number of Trade wagons" in performance_degradation.log).

NTradeGraphics = {
	WAGONS_BY_MARKET = 6									# vanilla 15
	WAGONS_TO_TRADECENTERS = 6								# vanilla 14
	MAX_COLONY_CHARTER_WAGONS = 40							# vanilla 100
	MAX_EXPLORATION_WAGONS = 40								# vanilla 100
	MAX_PROVINCES_TO_LOOK_FOR_PRODUCTION_BY_FRAME = 10		# vanilla 25 (per-frame scan budget)
}
